/**
 * lib/audit/anomaly-detector.ts
 *
 * STUB — will be fully implemented in Task 2.
 * Exported here so lib/audit/index.ts re-export resolves at type-check time.
 */

export async function runDetection(_sourceDir: string): Promise<void> {
  throw new Error("anomaly-detector: implementation pending (Task 2)");
}
