---
phase: 01-brain-spine-synthetic-seed
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - scripts/smoke-gate.sh
  - scripts/concurrent-smoke.ts
  - scripts/assert-anomalies.ts
autonomous: false
requirements:
  - DATA-10

must_haves:
  truths:
    - "scripts/smoke-gate.sh exits 0 ONLY when all of: gbrain graph-query, gbrain orphans, gbrain query 'what was weird about last month?' (naming all 3 anomalies), AND scripts/concurrent-smoke.ts all succeed against brains/seed/ (DATA-10 + Phase 1 success criterion #3, #4, #5)"
    - "scripts/concurrent-smoke.ts fires ≥ 3 parallel gbrain query calls via lib/gbrain/client.ts and asserts no PGLite lock errors AND no GbrainError thrown — proves HARN-04 mutex serialization end-to-end"
    - "When the gbrain query response does NOT mention all 3 anomalies (Beanstalk/price hike, Square/duplicate, 7shifts/ghost), the gate fails LOUD with the actual response printed to stderr"
    - "Smoke gate runs in <2 minutes end-to-end on the demo laptop (sets a coarse timeout for safety)"
  artifacts:
    - path: scripts/smoke-gate.sh
      provides: "Phase 1 DONE gate — fails loud if any DATA-10 subcondition fails"
      contains: "graph-query beanstalk-roasters"
    - path: scripts/concurrent-smoke.ts
      provides: "Concurrency proof — calls spawnGBrain 3× in parallel; HARN-04 verification"
    - path: scripts/assert-anomalies.ts
      provides: "Parses gbrain query response and asserts all 3 planted anomalies are named"
  key_links:
    - from: scripts/smoke-gate.sh
      to: brains/seed/
      via: "GBRAIN_HOME=brains/seed gbrain ..."
      pattern: "GBRAIN_HOME=brains/seed"
    - from: scripts/concurrent-smoke.ts
      to: lib/gbrain/client.ts
      via: "imports spawnGBrain from lib/gbrain/client"
      pattern: "spawnGBrain"
    - from: scripts/smoke-gate.sh
      to: scripts/assert-anomalies.ts
      via: "pipes query stdout into asserter"
      pattern: "assert-anomalies"
---

<objective>
The Phase 1 done-gate. This script tells the operator (and any future Claude executor) whether Phase 1's invariants are intact. It MUST pass before Phase 2 starts; it MUST be re-runnable to catch regressions after edits to the dataset, detector, or harness. The gate composes three signal types: gbrain graph health (graph-query + orphans), gbrain query semantic correctness (names all 3 anomalies), and application-layer concurrency safety (mutex prevents PGLite lock deadlocks).

Purpose: DATA-10 is the explicit smoke gate requirement. Phase 1 success criteria #3 (graph-query ≥3 neighbors), #4 (gbrain query names all 3 anomalies), and #5 (mutex serializes concurrent calls) all flow through this script. Without this gate, "Phase 1 done" is a vibe, not a verified state.

Output: `scripts/smoke-gate.sh` (bash orchestrator) + `scripts/concurrent-smoke.ts` (HARN-04 proof) + `scripts/assert-anomalies.ts` (DATA-10 semantic check). Wired as `bun run smoke` in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md
@lib/gbrain/client.ts
@scripts/seed.sh

<interfaces>
<!-- Gate composition. -->

```
scripts/smoke-gate.sh stages:
  1. Assert brains/seed/ exists (else: "Run bun run seed first")
  2. GBRAIN_HOME=brains/seed gbrain graph-query beanstalk-roasters --depth 2 | wc -l   # ≥3 lines or fail
  3. GBRAIN_HOME=brains/seed gbrain orphans                                              # capture; no length assertion, log only
  4. GBRAIN_HOME=brains/seed gbrain query "what was weird about last month?" > /tmp/qb-response.md
  5. bun run scripts/assert-anomalies.ts /tmp/qb-response.md                              # parses + asserts 3 anomaly names
  6. bun run scripts/concurrent-smoke.ts                                                  # 3× parallel via mutex; no errors

Exit 0 on all-pass with "OK: smoke gate passed (DATA-10 + HARN-04)".
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: scripts/assert-anomalies.ts — semantic assertion on gbrain query response (DATA-10 part 1)</name>
  <files>scripts/assert-anomalies.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DATA-10 — gbrain query "what was weird about last month?" names all 3 planted anomalies in one response)
    - .planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md (specifics: planted anomalies — Beanstalk price hike, Square duplicate $79, ghost 7shifts SaaS)
  </read_first>
  <action>
    Create `scripts/assert-anomalies.ts` — a Bun-runnable TypeScript script that takes a file path as `process.argv[2]`, reads the file as UTF-8, and asserts the response names all 3 planted anomalies.

    Parsing strategy: substring matching with a small set of synonyms per anomaly (gbrain's LLM answer may phrase things differently across runs — be permissive enough to not false-negative on the operator's terminal, strict enough to catch a genuinely-empty answer).

    Define three required "anomaly signals", each is a set of substrings; the anomaly is considered NAMED if at least ONE substring per signal matches the response (case-insensitive).

    - **Beanstalk price hike** signals: `['beanstalk', 'price hike', '22%', '+22', 'roaster pricing']` — match ≥ 1.
    - **Square duplicate charge** signals: `['square', 'duplicate', 'double charge', '$79', 'twice']` — match ≥ 1. Plus an enhanced check: must match `'square'` AND any one of `['duplicate', 'twice', '$79', 'double']` (vendor + duplicate-concept both required).
    - **Ghost 7shifts SaaS** signals: `['7shifts', 'seven-shifts', 'ghost', 'unused subscription', '$29', 'recurring']` — match ≥ 1. Plus: must match `'7shifts'` (case-insensitive matches both `7shifts` and `seven-shifts` after normalizing hyphen).

    On success: print `OK: response names all 3 anomalies` to stdout and exit 0.
    On failure: print to stderr:
       ```
       FAIL: missing anomaly signal(s):
         - <name>: did not match any of [<signals>]
       ---
       Response was:
       <full response>
       ---
       ```
       Then `process.exit(1)`.

    NOTE: `"assert:anomalies": "bun run scripts/assert-anomalies.ts"` is already declared in `package.json` from plan 01-01. No edit needed.

    Also include a CLI sanity check at the top of the script: if `process.argv[2]` is missing, print usage and exit 2.
  </action>
  <verify>
    <automated>test -f scripts/assert-anomalies.ts && grep -q 'beanstalk' scripts/assert-anomalies.ts && grep -q 'square' scripts/assert-anomalies.ts && grep -q '7shifts\|seven-shifts' scripts/assert-anomalies.ts && bunx tsc --noEmit && (TMP=$(mktemp) && printf '%s\n' 'Beanstalk Roasters raised prices 22% in March. Square POS double-charged $79. 7shifts is a ghost SaaS at $29/mo.' > "$TMP" && bun run scripts/assert-anomalies.ts "$TMP" && rm "$TMP") && (TMP=$(mktemp) && printf '%s\n' 'Nothing weird happened.' > "$TMP" && ! bun run scripts/assert-anomalies.ts "$TMP" && rm "$TMP")</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/assert-anomalies.ts` exists and is TypeScript-clean (`bunx tsc --noEmit`).
    - Positive test: a synthesized response naming all 3 anomalies exits 0 with `OK:` line.
    - Negative test: a response saying "Nothing weird happened" exits non-zero.
    - The 3 anomaly signal groups are all present in the source (verified by grep for `beanstalk`, `square`, `7shifts|seven-shifts`).
    - The script file exists at `scripts/assert-anomalies.ts` and is invocable via the pre-declared `bun run assert:anomalies`.
  </acceptance_criteria>
  <done>The semantic gate is in place. Task 3 will pipe `gbrain query` output through this.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: scripts/concurrent-smoke.ts — HARN-04 mutex proof (Phase 1 success criterion #5)</name>
  <files>scripts/concurrent-smoke.ts</files>
  <read_first>
    - lib/gbrain/client.ts (spawnGBrain — the function we're exercising)
    - lib/gbrain/mutex.ts (withTenantLock — the serialization primitive)
    - .planning/REQUIREMENTS.md (HARN-04 — mutex queue keyed by tenantId; DATA-10 implicit — no PGLite lock errors)
    - .planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md (specifics: scripts/concurrent-smoke.ts fires 3 gbrain query calls in parallel through lib/gbrain/client.ts; asserts no PGLite lock errors — proves HARN-04)
    - .planning/research/PITFALLS.md (Pitfall 3 — without mutex, the second call hits "database is locked" PGLite error within a few ms)
  </read_first>
  <action>
    Create `scripts/concurrent-smoke.ts`:

    1. `import { spawnGBrain, GbrainError } from '../lib/gbrain/client';`
    2. Const tenantId = `'seed'` (the seeded brain from plan 01-05).
    3. Define 3 lightweight query payloads — short, cache-friendly questions that exercise the spawn path without burning time/tokens:
       - `"What are Mara's top vendors?"`
       - `"Summarize March 2026 expenses."`
       - `"What recurring charges does Mara have?"`
    4. `const start = Date.now();`
    5. `const results = await Promise.allSettled([
         spawnGBrain(['query', q1], { tenantId }),
         spawnGBrain(['query', q2], { tenantId }),
         spawnGBrain(['query', q3], { tenantId }),
       ]);`
    6. `const totalMs = Date.now() - start;`
    7. Assertions:
       a. EVERY result must be `status === 'fulfilled'`. If any is `'rejected'`, print the rejection's `stderr` + `code` (cast `reason` to `GbrainError | Error`) and exit 1 with message `FAIL: concurrent gbrain query rejected — likely PGLite lock contention if mutex broken`.
       b. NONE of the stdouts may contain the substring `database is locked` (case-insensitive) — if any does, exit 1 with same FAIL message.
       c. Print `OK: 3 concurrent gbrain query calls succeeded via mutex (totalMs=${totalMs}, expected ≥ 3 × singleCall — proves serialization)`. Do NOT enforce a hard lower-bound on totalMs (single-call duration varies wildly with embedding cache state); just log it.
    8. Exit 0 on success.

    NOTE: `"smoke:concurrent": "bun run scripts/concurrent-smoke.ts"` is already declared in `package.json` from plan 01-01. No edit needed.

    NOTE: This script requires `brains/seed/` to exist (built by plan 01-05) and API keys exported. If `brains/seed/` is missing, the gbrain query will fail with a clear error — that's an acceptable failure mode for a smoke script. (No need to pre-check.)
  </action>
  <verify>
    <automated>test -f scripts/concurrent-smoke.ts && grep -q "import.*spawnGBrain" scripts/concurrent-smoke.ts && grep -q "Promise.allSettled" scripts/concurrent-smoke.ts && grep -q "database is locked" scripts/concurrent-smoke.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/concurrent-smoke.ts` exists and is TypeScript-clean.
    - Imports `spawnGBrain` from `lib/gbrain/client` (relative import path verified by grep).
    - Uses `Promise.allSettled` with 3 parallel calls (verified by grep).
    - Asserts on "database is locked" substring absence (verified by grep — proves the mutex is actually being tested).
    - The script file exists at `scripts/concurrent-smoke.ts` and is invocable via the pre-declared `bun run smoke:concurrent`.
    - When run against the seeded brain with API keys exported, exits 0 with `OK:` line.
  </acceptance_criteria>
  <done>HARN-04 (mutex) and Phase 1 success criterion #5 are verifiable by a single command. Any future regression in `lib/gbrain/mutex.ts` will be caught by this script.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: scripts/smoke-gate.sh — full DATA-10 orchestration (Phase 1 done-gate)</name>
  <files>scripts/smoke-gate.sh</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DATA-10 — graph-query ≥3 neighbors + orphans short list + query names all 3 anomalies)
    - .planning/ROADMAP.md (Phase 1 success criteria #3, #4, #5)
    - scripts/seed.sh (Stage labels pattern — match the style)
    - scripts/assert-anomalies.ts (Task 1 — invoked here)
    - scripts/concurrent-smoke.ts (Task 2 — invoked here)
  </read_first>
  <action>
    Create `scripts/smoke-gate.sh` with `#!/usr/bin/env bash` and `set -euo pipefail`. Add a `trap` printing `FAIL at stage: $CURRENT_STAGE`.

    Stages (CURRENT_STAGE set before each, `[stage N/6]` printed to stdout):

    **Stage 1** — Preflight:
    - `[ -d brains/seed ]` else fail with "Run `bun run seed` first to build brains/seed/".
    - Export `GBRAIN_HOME="$(pwd)/brains/seed"`.

    **Stage 2** — `gbrain graph-query beanstalk-roasters --depth 2`:
    - Capture stdout to a variable.
    - Count non-empty output lines: `lines=$(echo "$OUT" | grep -v '^[[:space:]]*$' | wc -l)`.
    - Fail if `lines < 3`. (Per Phase 1 success criterion #3 — at least 3 neighbors: invoices, the price-hike email, the anomaly concept page.)
    - Print `[stage 2/6] graph-query returned $lines neighbors (≥3 required)`.

    **Stage 3** — `gbrain orphans`:
    - Capture stdout. Log line count to stdout. Do NOT fail on a specific count — DATA-10 says "returns a short list" which is qualitative. If the count is > 20, print a `WARN: $count orphans — link coverage may be thin`. Continue.

    **Stage 4** — `gbrain query "what was weird about last month?"`:
    - Set a coarse safety timeout: prefix with `timeout 60` (60s ceiling — the demo query should finish in ~10–30s; if it's running >60s, something's wrong).
    - Redirect stdout to `/tmp/qb-smoke-response.md`.
    - Fail on non-zero exit code.

    **Stage 5** — Anomaly assertion:
    - `bun run scripts/assert-anomalies.ts /tmp/qb-smoke-response.md`.
    - On fail, the script prints the response — preserve that to the operator. Exit propagates.

    **Stage 6** — Concurrency proof:
    - `bun run scripts/concurrent-smoke.ts`. Inherits exit code.

    On full pass print `OK: smoke gate passed (DATA-10 + HARN-04)` and exit 0.

    NOTE: `"smoke": "bash scripts/smoke-gate.sh"` and `"phase1:done": "bash scripts/seed.sh && bash scripts/smoke-gate.sh"` are already declared in `package.json` from plan 01-01. No package.json edit needed.
  </action>
  <verify>
    <automated>test -x scripts/smoke-gate.sh && bash -n scripts/smoke-gate.sh && grep -q '^set -euo pipefail' scripts/smoke-gate.sh && grep -q 'graph-query beanstalk-roasters --depth 2' scripts/smoke-gate.sh && grep -q 'gbrain orphans' scripts/smoke-gate.sh && grep -q 'what was weird about last month' scripts/smoke-gate.sh && grep -q 'assert-anomalies' scripts/smoke-gate.sh && grep -q 'concurrent-smoke' scripts/smoke-gate.sh && grep -q 'timeout 60' scripts/smoke-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/smoke-gate.sh` exists, executable, bash-syntax-clean.
    - All 6 stages verifiable by grep (graph-query, orphans, query "what was weird about last month?", assert-anomalies, concurrent-smoke, timeout safety).
    - Empirically (Task 4 below) the script exits 0 against the seeded brain.
  </acceptance_criteria>
  <done>The gate exists as code. Task 4 confirms it actually passes end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator runs `bun run smoke` (or `phase1:done`) and confirms green (DATA-10 final gate)</name>
  <read_first>
    - scripts/smoke-gate.sh (Task 3 output)
    - .planning/ROADMAP.md (Phase 1 success criteria — all 5 must hold)
  </read_first>
  <what-built>
    Task 3 created the smoke-gate orchestrator. This checkpoint runs it end-to-end against the seeded brain (from plan 01-05) to confirm Phase 1 is actually done. There's no Claude automation that can substitute — the gate's value is precisely that it must run in the operator's real demo environment with real API keys.
  </what-built>
  <how-to-verify>
    Operator runs ONE of these two commands in the demo shell (API keys exported, brains/seed/ already built from plan 01-05):

    **Option A — if you already ran `bun run seed`:**
    ```
    bun run smoke
    ```

    **Option B — full Phase 1 done-check from scratch (~2 min total):**
    ```
    rm -rf brains/seed
    bun run phase1:done
    ```

    Expect stdout flow (approximately):
    ```
    [stage 1/6] preflight (brains/seed/ + GBRAIN_HOME)
    [stage 2/6] graph-query returned N neighbors (≥3 required)
    [stage 3/6] orphans: M items
    [stage 4/6] running canonical demo query (timeout 60s)...
    [stage 5/6] asserting 3 anomalies in response
    OK: response names all 3 anomalies
    [stage 6/6] concurrency proof (3× parallel)
    OK: 3 concurrent gbrain query calls succeeded via mutex (totalMs=X)
    OK: smoke gate passed (DATA-10 + HARN-04)
    ```
    And exit code 0.

    If any stage fails:
    - Stage 2 fail → graph link coverage is broken — likely DATA-03 wikilink omission. Re-inspect `data/maras-coffee/originals/*.md` files for missing `[[beanstalk-roasters]]` links.
    - Stage 4 fail → gbrain query timed out or crashed — likely API key issue OR PGLite lock from a prior aborted run. `rm -rf brains/seed && bun run seed && bun run smoke` to retry from clean state.
    - Stage 5 fail → query response did NOT name all 3 anomalies. Operator inspects `/tmp/qb-smoke-response.md` and reports it. Likely root causes: (a) detector didn't run / concept pages empty, (b) gbrain's expansion didn't pull the anomaly page, (c) anomaly wording in the dataset doesn't match the LLM's vocab. Fix in 01-04 (detector) or 01-03 (dataset).
    - Stage 6 fail → "database is locked" appears in stdout or a spawn rejected. The mutex is broken. Re-inspect `lib/gbrain/mutex.ts`.

    On any failure operator pastes the relevant stage output back, the executor diagnoses + fixes, then re-runs.
  </how-to-verify>
  <acceptance_criteria>
    - Operator types "approved" only when the final stdout line is `OK: smoke gate passed (DATA-10 + HARN-04)`.
    - All Phase 1 success criteria (1–5 in ROADMAP.md) are satisfied:
      - #1 demo-check exits 0 (already verified in 01-01)
      - #2 bun run seed produces brains/seed/ (verified in 01-05)
      - #3 graph-query returns ≥ 3 neighbors (Stage 2 here)
      - #4 query names all 3 anomalies (Stage 4 + 5 here)
      - #5 concurrent calls serialize (Stage 6 here)
    - If any subcondition is shaky (e.g. "passes 4 out of 5 times"), surface that — do NOT approve. Phase 1 must be DETERMINISTIC because Phase 2 + 3 build on it.
  </acceptance_criteria>
  <resume-signal>Type "approved" when smoke gate exits 0. Or paste the failing stage output for diagnosis.</resume-signal>
</task>

</tasks>

<verification>
End-of-plan verification (run from project root, ONLY after Task 4 checkpoint passes):
1. `bun run smoke` — exits 0 with `OK: smoke gate passed (DATA-10 + HARN-04)`.
2. `bun run phase1:done` — same, but rebuilds the brain first; useful regression check.
3. `bun test` — all unit tests across plans 01-02, 01-04 still green.
4. `bunx tsc --noEmit` — exits 0.
</verification>

<success_criteria>
- DATA-10 fully satisfied via composable smoke-gate stages, each independently fail-loud.
- Phase 1 success criteria #3, #4, #5 verifiable in <2 minutes by a single command.
- The gate is the regression-detection mechanism for the rest of the hackathon — any future change (Phase 2 chat, Phase 3 reset) that subtly breaks the data path is caught by re-running `bun run smoke`.
</success_criteria>

<output>
Create `.planning/phases/01-brain-spine-synthetic-seed/01-06-SUMMARY.md` when done.
</output>
