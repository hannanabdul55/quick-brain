---
phase: 01-brain-spine-synthetic-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - bun.lock
  - .gitignore
  - README.md
  - scripts/demo-check.sh
  - .env.example
  - next.config.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - postcss.config.mjs
autonomous: false
requirements:
  - HARN-01
  - HARN-02
user_setup:
  - service: gbrain
    why: "gbrain CLI is the brain engine; demo requires real CLI invocations"
    install_steps:
      - task: "Clone gbrain to a SIBLING directory of quick-brain (NOT inside the repo)"
        command: "cd .. && git clone https://github.com/garrytan/gbrain"
      - task: "Install + link gbrain via Bun (postinstall runs PGLite migration)"
        command: "cd ../gbrain && bun install && bun link"
      - task: "Verify gbrain on PATH"
        command: "gbrain --version"
    forbidden:
      - "bun install -g github:garrytan/gbrain (postinstall blocked, PGLite never migrates)"
      - "npm install -g gbrain (pulls squatted unrelated package)"
  - service: openai
    why: "gbrain uses OPENAI_API_KEY for text-embedding-3-small (vector search)"
    env_vars:
      - name: OPENAI_API_KEY
        source: "https://platform.openai.com/api-keys"
  - service: anthropic
    why: "gbrain uses ANTHROPIC_API_KEY for query expansion + chat output"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "https://console.anthropic.com/settings/keys"

must_haves:
  truths:
    - "Operator can install gbrain from a pinned commit via clone+link path (HARN-01)"
    - "Operator can run scripts/demo-check.sh and it exits 0 ONLY when gbrain + both API keys + brains/ write access are all present"
    - "Operator gets a non-zero exit with a descriptive message when any preflight item is missing"
    - "package.json declares ALL Phase 1 npm scripts up front so downstream plans only edit code (no package.json conflicts in same wave)"
  artifacts:
    - path: package.json
      provides: "Bun + Next.js 15 + TypeScript scaffolding; declares ALL Phase 1 scripts (check, test, validate:data, seed, detect:anomalies, smoke, smoke:concurrent, assert:anomalies, phase1:done)"
      contains: '"name": "quick-brain"'
    - path: tsconfig.json
      provides: "TypeScript 5.5+ strict config, lib/ + scripts/ in include"
    - path: scripts/demo-check.sh
      provides: "Preflight checks for gbrain + env vars + filesystem"
      contains: "gbrain --version"
    - path: README.md
      provides: "gbrain install instructions (clone+link, NOT bun -g)"
      contains: "bun install && bun link"
    - path: .env.example
      provides: "Names the two required API keys for the operator to fill in"
      contains: "OPENAI_API_KEY"
  key_links:
    - from: scripts/demo-check.sh
      to: "gbrain CLI on PATH"
      via: "command -v gbrain"
      pattern: "command -v gbrain"
    - from: scripts/demo-check.sh
      to: "OPENAI_API_KEY + ANTHROPIC_API_KEY env vars"
      via: "shell variable check"
      pattern: '\\$OPENAI_API_KEY'
    - from: README.md
      to: "gbrain repository"
      via: "documented clone command"
      pattern: "github.com/garrytan/gbrain"
---

<objective>
Bootstrap the QuickBrain repo with the minimum scaffolding needed for every later plan in Phase 1 — Bun + TypeScript + a top-level `package.json` that PRE-DECLARES every npm script downstream plans need (so plans 01-02 through 01-06 only edit code, never `package.json`, eliminating same-wave merge conflicts) — plus the operator-facing preflight check (`scripts/demo-check.sh`) and gbrain install documentation. No application code, no UI, no API routes.

Purpose: every downstream task (gbrain client, dataset, detector, seed pipeline, smoke gate) needs `bun run` to work and needs to know that gbrain + both API keys are reachable. This plan establishes that ground state AND locks the npm-script surface so Wave 2 plans (01-02 and 01-03) can run in parallel without touching `package.json` simultaneously.

Output: A runnable repo where `bun install` succeeds, `bun run check` invokes `scripts/demo-check.sh`, the README documents the exact gbrain install procedure (clone + `bun link`, NOT global install), and the package.json `scripts` object lists every Phase 1 entry point (most as placeholders pointing to files that don't yet exist — that's fine; the scripts get filled in when their files arrive).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- No existing interfaces — greenfield repo. This plan defines the project scaffolding others import. -->
<!-- Downstream plans will assume: bun runtime, "scripts" object in package.json with ALL Phase 1 entries pre-declared, lib/ directory may be created, scripts/ directory exists. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Gate — operator confirms gbrain install path and API keys</name>
  <read_first>
    - CLAUDE.md (Installation section — copy-paste install commands)
    - .planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md (specifics: Operational prerequisites)
  </read_first>
  <what-built>
    Nothing yet — this is the preflight gate before any file is written. It confirms the two operator-side prerequisites called out in CONTEXT.md specifics that Claude cannot do itself.
  </what-built>
  <how-to-verify>
    Operator must confirm the following two preconditions are TRUE before proceeding:

    1. gbrain CLI installed via clone+link (NOT bun -g):
       - From the parent directory of quick-brain: `git clone https://github.com/garrytan/gbrain` (sibling dir)
       - `cd ../gbrain && bun install && bun link`
       - `gbrain --version` returns a version string from any directory
       - Do NOT use `bun install -g github:garrytan/gbrain` — its postinstall is blocked and PGLite migration never runs.

    2. API keys exported in the demo shell:
       - `echo "$OPENAI_API_KEY"` prints a non-empty `sk-...` style value
       - `echo "$ANTHROPIC_API_KEY"` prints a non-empty `sk-ant-...` style value
       - These must be exported in the SAME shell that will later run `bun dev` and `bun run seed`.
  </how-to-verify>
  <acceptance_criteria>
    - Operator types "approved" only after both `gbrain --version` and both `echo "$KEY"` commands succeed in their shell.
    - If either fails, operator replies with the specific failure (e.g. "gbrain: command not found") so Claude can document a fix before proceeding.
  </acceptance_criteria>
  <resume-signal>Type "approved" when both checks pass, or describe the failure.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold Bun + Next.js 15 + TypeScript project + pre-declare ALL Phase 1 scripts</name>
  <files>
    package.json, tsconfig.json, .gitignore, bun.lock, .env.example, next.config.ts, app/layout.tsx, app/page.tsx, postcss.config.mjs, app/globals.css
  </files>
  <read_first>
    - CLAUDE.md (TL;DR table — Bun 1.2+, Next.js 15 App Router, React 19, TypeScript 5.5+, Tailwind v4)
    - .planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md (code_context: Integration Points — lib/gbrain/client.ts must not couple to Next.js types)
    - .planning/phases/01-brain-spine-synthetic-seed/01-02-PLAN.md, 01-03-PLAN.md, 01-04-PLAN.md, 01-05-PLAN.md, 01-06-PLAN.md (skim "Add to package.json scripts" lines — every script name they expect is pre-declared here so they never touch package.json)
  </read_first>
  <action>
    Scaffold the minimum Next.js 15 + Bun project. Use `bunx create-next-app@latest .` with defaults (TypeScript=yes, ESLint=yes, Tailwind=yes, App Router=yes, no `src/`, no Turbopack-only flag, no import alias change) inside the existing `quick-brain` directory — answer prompts non-interactively where possible. After scaffold:

    **Edit `package.json` ONCE to add ALL Phase 1 scripts under the existing `scripts` object** (merge with create-next-app's defaults; do NOT overwrite `dev`, `build`, `start`, `lint`). Required entries:
    - `"check": "bash scripts/demo-check.sh"`
    - `"test": "bun test"`
    - `"validate:data": "bun run scripts/validate-dataset.ts"`
    - `"detect:anomalies": "bun run scripts/detect-anomalies.ts"`
    - `"seed": "bash scripts/seed.sh"`
    - `"smoke": "bash scripts/smoke-gate.sh"`
    - `"smoke:concurrent": "bun run scripts/concurrent-smoke.ts"`
    - `"assert:anomalies": "bun run scripts/assert-anomalies.ts"`
    - `"phase1:done": "bash scripts/seed.sh && bash scripts/smoke-gate.sh"`

    NOTE: many of these scripts reference files that don't yet exist (e.g. `scripts/seed.sh` is created in plan 01-05). That's intentional — pre-declaring them now means downstream Wave 2+ plans never have to edit `package.json`, which prevents same-wave merge conflicts. Each downstream plan adds its file; the script entry already points to it.

    Write `.env.example` with two non-secret placeholder lines:
    - `OPENAI_API_KEY=sk-...`
    - `ANTHROPIC_API_KEY=sk-ant-...`

    Extend `.gitignore` (if not already covered by create-next-app defaults) with these entries on their own lines:
    - `brains/` (per-tenant brain dirs — must NOT be committed; only `brains/seed/` would be, and even that is generated on demand)
    - `.env`
    - `.env.local`

    Do NOT add `lib/` files in this plan — `lib/gbrain/client.ts` is created in 01-02. Do NOT add `data/` or `scripts/seed.sh` here — those come in 01-03 and 01-05 respectively.

    Use Bun for everything: do NOT run `npm install` or `yarn install` at any point. Run only `bun install` if the scaffold did not already do so.

    **Install ALL downstream Phase 1 npm dependencies up front** (same reasoning as pre-declaring scripts — Wave 2 plans 01-02 and 01-03 don't touch package.json):
    - `bun add zod` (runtime — used by lib/gbrain/slug.ts in plan 01-02)
    - `bun add -d gray-matter` (dev — used by scripts/validate-dataset.ts in plan 01-03 and scripts/detect-anomalies.ts in plan 01-04 for YAML frontmatter parsing)
    Verify both land in the correct dependency section (zod in `dependencies`, gray-matter in `devDependencies`).
  </action>
  <verify>
    <automated>bun install --frozen-lockfile && bunx tsc --noEmit && test -f package.json && test -f tsconfig.json && test -f .env.example && grep -q '"check":' package.json && grep -q '"test":' package.json && grep -q '"validate:data":' package.json && grep -q '"detect:anomalies":' package.json && grep -q '"seed":' package.json && grep -q '"smoke":' package.json && grep -q '"smoke:concurrent":' package.json && grep -q '"assert:anomalies":' package.json && grep -q '"phase1:done":' package.json && grep -q '"zod"' package.json && grep -q '"gray-matter"' package.json && grep -q 'OPENAI_API_KEY' .env.example && grep -q 'ANTHROPIC_API_KEY' .env.example && grep -q '^brains/' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists and contains a `"scripts"` object with ALL 9 Phase 1 script keys (check, test, validate:data, detect:anomalies, seed, smoke, smoke:concurrent, assert:anomalies, phase1:done) PLUS the create-next-app defaults (dev, build, start, lint).
    - `bunx tsc --noEmit` exits 0 (TypeScript compiles cleanly with create-next-app defaults).
    - `bun install` is reproducible (`bun.lock` committed).
    - `zod` is in `dependencies`; `gray-matter` is in `devDependencies` (both pre-installed so Wave 2 plans 01-02 and 01-03 do not touch `package.json`).
    - `.env.example` contains exactly the two API-key names (no real secrets).
    - `.gitignore` excludes `brains/`, `.env`, and `.env.local`.
    - No `node_modules/` is committed.
  </acceptance_criteria>
  <done>The repo is a valid Next.js 15 + TypeScript + Bun project that compiles cleanly. Every Phase 1 npm script is declared up front, so Wave 2+ plans only edit code (no package.json edits in any later plan).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write scripts/demo-check.sh preflight (HARN-02)</name>
  <files>scripts/demo-check.sh</files>
  <read_first>
    - .planning/REQUIREMENTS.md (HARN-02 acceptance — exits non-zero if any of: `gbrain --version`, `gbrain doctor --fast`, `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, write-access to `./brains/` is missing)
    - .planning/phases/01-brain-spine-synthetic-seed/01-CONTEXT.md (decisions: Claude's Discretion — exact error wording is at planner discretion, just must exit non-zero on failure)
    - CLAUDE.md (Installation section — confirms env-var names and the `gbrain doctor` command exists in CLI surface)
  </read_first>
  <action>
    Create `scripts/demo-check.sh` as an executable bash script (set `set -euo pipefail` at top, `chmod +x` the file) implementing HARN-02. The script MUST perform these 5 checks in order and exit non-zero on the FIRST failure (fail-fast, with a descriptive message to stderr including the check name):

    1. `command -v gbrain >/dev/null` — fail with "gbrain CLI not found on PATH. See README for install instructions."
    2. `gbrain --version` — capture output, fail if exit code non-zero.
    3. `gbrain doctor --fast` — fail if exit code non-zero (gbrain's own self-diagnostic).
    4. `[ -n "${OPENAI_API_KEY:-}" ]` — fail with "OPENAI_API_KEY not exported". Then `[ -n "${ANTHROPIC_API_KEY:-}" ]` with analogous message.
    5. Filesystem write access to `./brains/`: `mkdir -p ./brains && touch ./brains/.demo-check-write-test && rm ./brains/.demo-check-write-test` — fail with "Cannot write to ./brains/".

    On full success print a single line `OK: demo-check passed` and exit 0.

    Use `bash` only (NOT `zsh`/`sh`/`fish`) — `#!/usr/bin/env bash` shebang. Do not require any non-stdlib utilities beyond `command`, `mkdir`, `touch`, `rm`.

    Wire it as the `bun run check` script (already declared in package.json by Task 2).
  </action>
  <verify>
    <automated>test -x scripts/demo-check.sh && bash -n scripts/demo-check.sh && grep -q '^set -euo pipefail' scripts/demo-check.sh && grep -q 'gbrain --version' scripts/demo-check.sh && grep -q 'gbrain doctor --fast' scripts/demo-check.sh && grep -q 'OPENAI_API_KEY' scripts/demo-check.sh && grep -q 'ANTHROPIC_API_KEY' scripts/demo-check.sh && grep -q './brains/' scripts/demo-check.sh && (OPENAI_API_KEY= ANTHROPIC_API_KEY= bash scripts/demo-check.sh; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/demo-check.sh` is executable and `bash -n` syntax-checks clean.
    - Script contains all 5 required checks (verified by grep).
    - Running with both env vars unset MUST exit non-zero (last grep+test in verify).
    - Running with `bun run check` from the project root invokes the script.
    - On a fully-configured machine (operator confirmed Task 1), `bash scripts/demo-check.sh` exits 0 and prints `OK: demo-check passed`.
  </acceptance_criteria>
  <done>HARN-02 fully satisfied: a single command (`bun run check`) tells the operator pass/fail on every Phase 1 prerequisite, with no false greens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Document gbrain install in README.md (HARN-01)</name>
  <files>README.md</files>
  <read_first>
    - CLAUDE.md (Installation section — exact clone+link commands, gbrain repo URL, forbidden global install)
    - .planning/PROJECT.md (Core Value — used as the README intro)
    - .planning/REQUIREMENTS.md (HARN-01 wording — "pinned git SHA via `git clone + bun install && bun link`, `bun install -g` documented as broken")
  </read_first>
  <action>
    Write `README.md` from scratch. Required sections, in order:

    1. `# QuickBrain` heading + one-paragraph Core Value blurb (copy from PROJECT.md "What This Is").
    2. `## Prerequisites` — Bun 1.2+, gbrain CLI installed (link to install steps below), `OPENAI_API_KEY` + `ANTHROPIC_API_KEY` exported in the shell.
    3. `## Install gbrain (CRITICAL — DO NOT skip)` — exact commands as four numbered steps:
       a. `cd ..` (so gbrain clones as a sibling, not nested inside quick-brain)
       b. `git clone https://github.com/garrytan/gbrain`
       c. `cd gbrain && bun install && bun link`
       d. `gbrain --version` to verify
       Below the steps, an explicit "DO NOT use `bun install -g github:garrytan/gbrain` — postinstall is blocked, PGLite never migrates, runtime crashes with `Aborted()`. Documented gotcha in gbrain INSTALL_FOR_AGENTS.md." callout (use a `> Warning:` blockquote or `## Why not bun -g?` subsection).
    4. `## Configure API keys` — `cp .env.example .env.local`, then export both keys in the shell that runs the dev server. Note both are REQUIRED (no fallback search-only mode for the demo).
    5. `## Verify your setup` — `bun install` then `bun run check` should print `OK: demo-check passed`.
    6. `## Run the demo` — placeholder section: `bun run seed` (Phase 1, implemented in plan 01-05), then `bun dev` (Phase 2). It is fine to mark these as "coming in next phase" if seed.sh doesn't exist yet at write time.

    Keep README under ~120 lines — operator-facing only, not a design doc.
  </action>
  <verify>
    <automated>test -f README.md && grep -qF 'git clone https://github.com/garrytan/gbrain' README.md && grep -qF 'bun install && bun link' README.md && grep -qF 'bun install -g' README.md && grep -qF 'OPENAI_API_KEY' README.md && grep -qF 'ANTHROPIC_API_KEY' README.md && grep -qF 'bun run check' README.md && grep -qF 'QuickBrain' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root.
    - Contains the exact clone command `git clone https://github.com/garrytan/gbrain` (verified by grep -F).
    - Contains the link command `bun install && bun link` (verified by grep -F).
    - Contains an explicit warning against `bun install -g` (verified by grep -F).
    - Documents both required env var names.
    - Documents the `bun run check` verification step.
    - A new operator can follow the README from scratch and arrive at a state where `bun run check` exits 0.
  </acceptance_criteria>
  <done>HARN-01 satisfied: README provides a copy-pasteable, correct install path for gbrain and explicitly forbids the broken bun-global path.</done>
</task>

</tasks>

<verification>
End-of-plan verification (run from project root):
1. `bun install --frozen-lockfile` — exits 0.
2. `bunx tsc --noEmit` — exits 0.
3. `bun run check` — exits 0 (presumes operator completed Task 1).
4. Negative test: `OPENAI_API_KEY= ANTHROPIC_API_KEY= bash scripts/demo-check.sh` — exits NON-zero with descriptive stderr.
5. `git status` — `node_modules/` is NOT staged; `.env` / `.env.local` not staged.
6. `node -e "console.log(Object.keys(require('./package.json').scripts).sort().join(','))"` — output contains all 9 Phase 1 scripts.
</verification>

<success_criteria>
- Repo compiles (`tsc --noEmit` clean) with create-next-app defaults plus our additions.
- `scripts/demo-check.sh` is the single source of truth for "is this machine ready" — covers gbrain binary, gbrain doctor, both API keys, and `./brains/` write access.
- README walks a fresh operator from zero to a green `bun run check` in under 5 minutes.
- All Phase 1 npm scripts are pre-declared in `package.json` — downstream plans only add code files, eliminating package.json edit-conflicts across same-wave parallel plans.
- No application code (lib/, app/api/, components/) added — that is for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/01-brain-spine-synthetic-seed/01-01-SUMMARY.md` when done.
</output>
