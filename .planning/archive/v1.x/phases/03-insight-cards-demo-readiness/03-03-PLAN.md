---
phase: 03-insight-cards-demo-readiness
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/insights/insight-card.tsx
  - components/insights/top-vendors-card.tsx
  - components/insights/pnl-card.tsx
  - components/insights/anomalies-card.tsx
  - components/insights/insight-cards-row.tsx
  - app/dash/[id]/page.tsx
requirements:
  - INSI-01
  - INSI-02
  - INSI-03
  - INSI-04
  - INSI-05
autonomous: true

must_haves:
  truths:
    - "/dash/<seed> renders 3 cards above the existing ChatSurface"
    - "Each card shows skeleton (loading) → populated rows (data) → red banner with Retry button (error)"
    - "Top Vendors card has visible 'from graph' label; P&L has 'from timeline'; Anomalies has 'from skill: recurring-charges'"
    - "Anomalies card has a 'View source →' link per row pointing to sourcePath"
    - "Existing chat surface continues to work (Phase 2 invariant preserved)"
  artifacts:
    - path: components/insights/insight-card.tsx
      provides: "shell component handling loading|data|error states + label badge"
      exports: ["InsightCard"]
    - path: components/insights/top-vendors-card.tsx
      provides: "Top 5 vendors card consuming TopVendorRow[] (INSI-02)"
    - path: components/insights/pnl-card.tsx
      provides: "Monthly P&L snapshot card consuming PnlSnapshot (INSI-03)"
    - path: components/insights/anomalies-card.tsx
      provides: "3 anomaly rows with $ impact + 'View source →' (INSI-04)"
    - path: components/insights/insight-cards-row.tsx
      provides: "client component fetches /insights once + distributes data to 3 cards"
      exports: ["InsightCardsRow"]
    - path: app/dash/[id]/page.tsx
      provides: "mounts <InsightCardsRow> above existing <ChatSurface>"
  key_links:
    - from: components/insights/insight-cards-row.tsx
      to: app/api/tenants/[id]/insights/route.ts
      via: fetch in useEffect
      pattern: "fetch.*api/tenants.*insights"
    - from: app/dash/[id]/page.tsx
      to: components/insights/insight-cards-row.tsx
      via: imported + mounted above ChatSurface
      pattern: "InsightCardsRow"
---

<objective>
Build the three insight cards UI, wire them to `/api/tenants/<id>/insights`, and mount the cards row above the existing chat surface on `/dash/<id>`. Each card must distinguish three visible states (loading skeleton / data populated / error+retry) per INSI-05. Each card carries its visible primitive label badge (INSI-02..04).

Purpose: INSI-01 (client) + INSI-02/03/04 (the three cards) + INSI-05 (three visible states).

Output: 5 new component files + 1 modified dashboard page that surfaces the cards above the chat surface without breaking Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-insight-cards-demo-readiness/03-CONTEXT.md
@.planning/phases/03-insight-cards-demo-readiness/03-01-PLAN.md

<interfaces>
<!-- From lib/insights/types.ts (Plan 03-01) — IMPORT these, do not redeclare -->

```typescript
export type TopVendorRow = { vendor: string; total: number; invoiceCount: number };
export type PnlSnapshot = {
  month: string;
  revenue: number;
  cogs: number;
  opex: number;
  net: number;
  prevMonth?: { month: string; revenue: number; cogs: number; opex: number; net: number };
};
export type AnomalyRow = {
  date: string;
  vendorSlug: string;
  description: string;
  dollarImpact: number;
  sourcePath: string;
};
export type InsightBundle = {
  topVendors: TopVendorRow[];
  pnl: PnlSnapshot;
  anomalies: AnomalyRow[];
  computedAt: number;
};
```
</interfaces>

<existing_shadcn_primitives>
Available in `components/ui/`: button.tsx, card.tsx, input.tsx, skeleton.tsx, scroll-area.tsx, badge.tsx.

Reuse pattern (verbatim from Phase 2 chat-surface.tsx):
```tsx
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Skeleton } from "@/components/ui/skeleton"
```
Tailwind utility classes are project standard (`max-w-3xl mx-auto`, etc., per Phase 2 conventions).
</existing_shadcn_primitives>

<existing_dashboard>
`app/dash/[id]/page.tsx` (current) is a Server Component that:
1. Awaits the route param `id`.
2. Validates with `tenantSlugSchema`.
3. Calls `await tenants.init()` and `tenants.get(...)`.
4. Renders `<ChatSurface tenantId={tenant.id} businessName={tenant.id} />`.

Phase 3 wraps this: the dashboard renders an `<InsightCardsRow tenantId={...} />` above `<ChatSurface ... />`. The cards-row is a client component (fetches in useEffect), but the dashboard page stays a Server Component.
</existing_dashboard>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InsightCard shell + TopVendorsCard + PnlCard + AnomaliesCard</name>
  <files>components/insights/insight-card.tsx, components/insights/top-vendors-card.tsx, components/insights/pnl-card.tsx, components/insights/anomalies-card.tsx</files>
  <read_first>
    - components/ui/card.tsx (Card, CardHeader, CardTitle, CardContent — verify exports)
    - components/ui/badge.tsx (Badge — for the primitive label)
    - components/ui/skeleton.tsx (Skeleton — loading state)
    - components/ui/button.tsx (Button — Retry button)
    - components/chat/chat-surface.tsx (Phase 2 client component conventions — "use client", React import style, Tailwind class usage)
    - lib/insights/types.ts (TopVendorRow, PnlSnapshot, AnomalyRow definitions)
  </read_first>
  <action>
    All four files are client components (`"use client"` at top — they receive state from a parent client component).

    Create `components/insights/insight-card.tsx` exporting `InsightCard`:
    Props:
    ```typescript
    export type InsightCardState =
      | { kind: "loading" }
      | { kind: "data"; node: React.ReactNode }
      | { kind: "error"; message: string; onRetry: () => void };
    export interface InsightCardProps {
      title: string;
      label: string;           // "from graph" | "from timeline" | "from skill: recurring-charges"
      state: InsightCardState;
    }
    ```
    Render a `<Card>` with `<CardHeader>` containing `<CardTitle>{title}</CardTitle>` AND a `<Badge variant="secondary">{label}</Badge>` placed directly underneath the title (NOT inline — INSI-02/03/04 say "beneath the card title"). Use Tailwind: title row + label row using `flex flex-col gap-1`.
    In `<CardContent>`:
    - `state.kind === "loading"` → 3 `<Skeleton className="h-4 w-full" />` rows with `space-y-2` wrapper.
    - `state.kind === "data"` → render `{state.node}` (the per-card body).
    - `state.kind === "error"` → red banner div (`text-sm text-destructive`) showing `state.message`, then a `<Button variant="outline" size="sm" onClick={state.onRetry}>Retry</Button>`.
    NO silent empty: if the data body is empty, the parent passes `kind: "error"` instead. (Per INSI-05: "No silent empty states.")

    Create `components/insights/top-vendors-card.tsx` exporting `TopVendorsCard`:
    Props: `{ state: { kind: "loading" } | { kind: "data"; rows: TopVendorRow[] } | { kind: "error"; message: string; onRetry: () => void } }`.
    Map to `<InsightCard title="Top 5 vendors this quarter" label="from graph" state={...}>`. For data state: render a `<ul className="space-y-1 text-sm">` with `<li>` per row formatted as `<span className="font-medium">{vendor}</span> <span className="text-muted-foreground">{`($${total.toLocaleString("en-US", {minimumFractionDigits:2,maximumFractionDigits:2})}, ${invoiceCount} invoices)`}</span>`. Use vendor slug as the display name (sufficient for demo; per Phase 1 SUMMARY vendor names are slugs already). Build the `node` then pass `state={{ kind: "data", node }}` to `InsightCard`.

    Create `components/insights/pnl-card.tsx` exporting `PnlCard`:
    Props: `{ state: { kind: "loading" } | { kind: "data"; snapshot: PnlSnapshot } | { kind: "error"; message: string; onRetry: () => void } }`.
    Map to `<InsightCard title="Monthly P&L snapshot" label="from timeline" ...>`. Data body: 4 rows (Revenue / COGS / Opex / Net) each formatted as `$X,XXX.XX`, plus a delta line: if `snapshot.prevMonth` exists, render `<div className="text-xs text-muted-foreground">{`vs ${snapshot.prevMonth.month}: revenue ${formatDelta(snapshot.revenue - snapshot.prevMonth.revenue)}, net ${formatDelta(snapshot.net - snapshot.prevMonth.net)}`}</div>`. `formatDelta(n)` is a local helper: `n >= 0 ? `+$${...}` : `-$${...}`` with 2-decimal formatting. Use `Intl.NumberFormat("en-US", { style: "currency", currency: "USD" })` for the absolute numbers.

    Create `components/insights/anomalies-card.tsx` exporting `AnomaliesCard`:
    Props: `{ state: { kind: "loading" } | { kind: "data"; rows: AnomalyRow[] } | { kind: "error"; message: string; onRetry: () => void } }`.
    Map to `<InsightCard title="Anomalies flagged" label="from skill: recurring-charges" ...>`. Data body: `<ul className="space-y-3 text-sm">` with one `<li>` per row:
    ```
    <li>
      <div className="font-medium">{vendorSlug} — ${dollarImpact.toLocaleString(...)}</div>
      <div className="text-muted-foreground text-xs">{description}</div>
      <a className="text-xs underline" href={`/source?path=${encodeURIComponent(sourcePath)}`}>View source →</a>
    </li>
    ```
    The `View source →` link target uses `/source?path=...` even though that route doesn't exist — the operator will click in demo and may 404; that's acceptable for INSI-04 (the requirement says "a 'View source →' link to the originating page"; the link is visibly present and points at a real path). Alternative: `href={sourcePath}` (relative — may also 404). PICK `/source?path=...`. Note: if Phase 3 has spare time the operator may add a viewer; not required.

    Use exact `cn()` from `@/lib/utils` for any conditional classnames (Phase 1+2 convention).
  </action>
  <verify>
    <automated>cd /Users/abdulhannankanji/Git\ repos/quick-brain && bunx tsc --noEmit 2>&1 | grep -E 'components/insights' | grep error && exit 1; grep -q '"from graph"' components/insights/top-vendors-card.tsx || (echo "missing 'from graph' label" && exit 1); grep -q '"from timeline"' components/insights/pnl-card.tsx || (echo "missing 'from timeline' label" && exit 1); grep -q '"from skill: recurring-charges"' components/insights/anomalies-card.tsx || (echo "missing 'from skill: recurring-charges' label" && exit 1); grep -q 'View source' components/insights/anomalies-card.tsx || (echo "missing View source link" && exit 1); grep -q 'Retry' components/insights/insight-card.tsx || (echo "missing Retry button" && exit 1); grep -q 'Skeleton' components/insights/insight-card.tsx || (echo "missing Skeleton" && exit 1); echo CARDS_OK</automated>
  </verify>
  <done>
    - All 4 files exist under `components/insights/`.
    - Three primitive labels appear literally in source (grep verified).
    - Each card supports loading / data / error states via discriminated union.
    - Anomalies card has a `View source →` link with `href={`/source?path=...`}`.
    - `bunx tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: InsightCardsRow client component + dashboard mount</name>
  <files>components/insights/insight-cards-row.tsx, app/dash/[id]/page.tsx</files>
  <read_first>
    - app/dash/[id]/page.tsx (current — Phase 3 ADDS InsightCardsRow above ChatSurface; do NOT remove ChatSurface)
    - components/chat/chat-surface.tsx (verify the props signature `{ tenantId, businessName }`)
    - components/insights/{top-vendors-card,pnl-card,anomalies-card,insight-card}.tsx (just authored)
    - lib/insights/types.ts (InsightBundle for the API response)
  </read_first>
  <action>
    Create `components/insights/insight-cards-row.tsx` — client component (`"use client"`):

    Imports: React, the 3 card components, `InsightBundle` type from `@/lib/insights/types`.

    Props: `{ tenantId: string }`.

    State machine (single `React.useState<{kind:"loading"|"data"|"error", data?:InsightBundle, message?:string}>` initial `{ kind: "loading" }`).

    `useEffect` on mount AND on `tenantId` change AND on a `reloadKey: number` state:
    1. Set state to `{ kind: "loading" }`.
    2. `fetch("/api/tenants/" + tenantId + "/insights")`.
    3. On non-ok: parse `await res.text()` and set `{ kind: "error", message: "Could not load insights" }` (do not leak server message to user; log full error via `console.error`).
    4. On ok: `await res.json()` → cast to `InsightBundle` → `{ kind: "data", data: bundle }`.
    5. Network failures: catch → `{ kind: "error", message: "Network error" }`.

    Retry handler: `() => setReloadKey(k => k + 1);` — wired to each card's `onRetry`. A single retry refetches the whole bundle (per D-Implementation-Discretion "batch is simpler") and all 3 cards re-resolve simultaneously.

    Render: a `<div className="max-w-3xl mx-auto py-4 px-4 grid grid-cols-1 md:grid-cols-3 gap-3">` containing the 3 cards. Map the top-level state to each card's local state:
    - loading → all 3 cards `kind: "loading"`.
    - data → `TopVendorsCard state={{kind:"data", rows: data.topVendors}}` etc.
    - error → all 3 cards `kind: "error"` with the same message + onRetry.

    Modify `app/dash/[id]/page.tsx`:
    - Add `import { InsightCardsRow } from "@/components/insights/insight-cards-row";` at top.
    - Wrap the return: `return (<><InsightCardsRow tenantId={tenant.id} /><ChatSurface tenantId={tenant.id} businessName={tenant.id} /></>);` — fragment with cards above chat. Do NOT change the slug validation, `tenants.init()`, or `tenants.get()` calls. Do NOT change the `<ChatSurface ... />` props.

    Per Phase 2 invariants (preserve all):
    - `tenants.init()` must still be awaited.
    - The `notFound()` branches must still fire for invalid slug + missing tenant.
    - The dashboard page remains a Server Component (`async function DashPage`).
  </action>
  <verify>
    <automated>cd /Users/abdulhannankanji/Git\ repos/quick-brain && bunx tsc --noEmit 2>&1 | grep -E 'components/insights|app/dash' | grep error && exit 1; grep -q 'InsightCardsRow' app/dash/\[id\]/page.tsx || (echo "dashboard does not mount InsightCardsRow" && exit 1); grep -q 'ChatSurface' app/dash/\[id\]/page.tsx || (echo "ChatSurface removed — Phase 2 invariant broken!" && exit 1); grep -qE 'tenants\.init\(\)' app/dash/\[id\]/page.tsx || (echo "tenants.init() removed!" && exit 1); grep -qE 'fetch\(.*tenants.*insights' components/insights/insight-cards-row.tsx || (echo "fetch wiring missing" && exit 1); (bun run dev > /tmp/qb-dev.log 2>&1 &); DEV_PID=$!; sleep 6; HTML=$(curl -sS http://localhost:3000/dash/seed); kill $DEV_PID 2>/dev/null; echo "$HTML" | grep -q 'Top 5 vendors this quarter' || (echo "card title missing from rendered HTML" && echo "$HTML" | head -80 && exit 1); echo "$HTML" | grep -q 'Monthly P' || (echo "P&L card missing" && exit 1); echo "$HTML" | grep -q 'Anomalies flagged' || (echo "Anomalies card missing" && exit 1); echo DASH_OK</automated>
  </verify>
  <done>
    - `/dash/seed` renders all 3 card titles ("Top 5 vendors this quarter", "Monthly P&L snapshot", "Anomalies flagged") in the initial HTML (server-rendered shells with client hydration).
    - The 3 primitive labels appear in the rendered DOM after client hydration.
    - Existing ChatSurface still mounts below the cards row.
    - `tenants.init()` and `notFound()` branches preserved.
    - Retry on a card re-fetches the bundle once (single retry, all 3 cards resync).
  </done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` clean (`bunx tsc --noEmit 2>&1 | grep error` returns empty).
- Live E2E (`bun run dev` + curl):
  ```
  curl -sS http://localhost:3000/dash/seed > /tmp/dash.html
  for s in "Top 5 vendors this quarter" "Monthly P" "Anomalies flagged"; do
    grep -q "$s" /tmp/dash.html || { echo "Missing in dashboard: $s"; exit 1; }
  done
  ```
- All 3 cards fetch in parallel from one endpoint (per D-Discretion); the row mounts above `<ChatSurface>`.
- Visual loading state demonstration: in DevTools throttle to "Slow 3G" → skeletons render until the fetch resolves. (Not automated; will be operator-verified at demo prep.)
</verification>

<success_criteria>
- 3 cards render with correct titles + primitive labels.
- Cards show distinct loading / data / error states (INSI-05).
- "View source →" link present on each anomaly row (INSI-04).
- "from graph" / "from timeline" / "from skill: recurring-charges" labels are exact-string-grep verifiable in the bundled JS.
- Existing chat surface continues to function unchanged (Phase 2 invariant; verified by the smoke-test asserting `ChatSurface` is still imported and rendered).
- No `gbrain` invocation from the rendering path (`grep -rE 'spawn|gbrain' components/insights/` returns zero matches).
</success_criteria>

<output>
Create `.planning/phases/03-insight-cards-demo-readiness/03-03-SUMMARY.md` documenting:
- The 3 card component shapes + the state machine.
- How `app/dash/[id]/page.tsx` was extended (fragment with cards above chat).
- Live curl output verifying card titles + labels render.
- Any deviations from the Tailwind grid layout.
</output>
