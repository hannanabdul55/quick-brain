---
phase: 02-onboarding-theater-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - next-env.d.ts
  - postcss.config.mjs
  - eslint.config.mjs
  - components.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - app/onboard/page.tsx
  - lib/utils.ts
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/input.tsx
  - components/ui/skeleton.tsx
  - components/ui/scroll-area.tsx
  - components/ui/badge.tsx
  - .gitignore
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-08]
must_haves:
  truths:
    - "Operator visits / and sees a 'Start your business brain' CTA"
    - "Clicking the CTA navigates to /onboard which shows a 3-field form (business name, business type, owner name)"
    - "No screen on / or /onboard asks for API key, signup, or payment"
    - "bun run dev starts Next.js 15 App Router on port 3000 and renders / without runtime errors"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing CTA page"
      contains: "Start your business brain"
    - path: "app/onboard/page.tsx"
      provides: "Onboarding form skeleton (3 fields)"
      contains: "business name"
    - path: "components.json"
      provides: "shadcn/ui config"
    - path: "components/ui/button.tsx"
      provides: "shadcn Button primitive"
    - path: "components/ui/card.tsx"
      provides: "shadcn Card primitive"
    - path: "components/ui/input.tsx"
      provides: "shadcn Input primitive"
    - path: "components/ui/scroll-area.tsx"
      provides: "shadcn ScrollArea primitive (needed by Wave 4)"
    - path: "components/ui/skeleton.tsx"
      provides: "shadcn Skeleton primitive (needed by Wave 4)"
  key_links:
    - from: "app/page.tsx"
      to: "/onboard"
      via: "next/link CTA"
      pattern: "href=\"/onboard\""
    - from: "package.json scripts.dev"
      to: "next dev"
      via: "Next.js dev server"
      pattern: "next dev"
    - from: "tsconfig.json paths"
      to: "@/*"
      via: "import alias"
      pattern: "\"@/\\*\""
---

<objective>
Scaffold a Next.js 15 App Router project over the existing Phase 1 codebase. Install shadcn/ui primitives. Replace the create-next-app default landing with the QuickBrain CTA. Add a `/onboard` form skeleton (form fields only; submit handler and SSE consumption come in Plan 04).

Purpose: Phase 1 was terminal-only. Every subsequent Phase 2 plan needs `app/`, `components/ui/*`, `react-markdown` to exist. This plan creates that foundation in one focused pass so Waves 2–4 can run independently.

Output: Working Next.js dev server, shadcn primitives copied into `components/ui/*`, landing page with CTA → `/onboard`, form skeleton (no submit wiring yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md
@.planning/phases/01-brain-spine-synthetic-seed/01-SUMMARY.md
@CLAUDE.md
@package.json
@tsconfig.json
</context>

<interfaces>
Phase 1 surface that must be preserved through the scaffold:

From `package.json` (current scripts to KEEP — merge, do not overwrite):
- `seed`, `demo-check`, `detect-anomalies`, `concurrent-smoke`, `mutex-smoke`

From `package.json` (current deps to KEEP):
- `zod ^3.23.8` (dependency)
- `@types/bun`, `@types/node`, `typescript` (devDependencies)

From `tsconfig.json` (settings to PRESERVE through the merge):
- `"strict": true`
- `"noUncheckedIndexedAccess": true`
- `"types": ["bun"]` — keep alongside the Next.js-added types
- `"paths": { "@/*": ["./*"] }`
- `"include"` must keep `lib/**/*.ts` and `scripts/**/*.ts` and add `app/**/*.ts(x)` + `components/**/*.ts(x)`
- `"exclude"` must keep `brains` (large per-tenant dirs that must not be type-checked)

From `lib/gbrain/` (must remain importable via `@/lib/gbrain` after scaffold):
- `lib/gbrain/index.ts` re-exports `spawnGBrain`, `query`, `withTenantLock`, tenant CRUD
- Path alias must continue to resolve `@/lib/gbrain` from `app/api/...` route handlers in later plans

Existing repo files (do not touch in this plan):
- `lib/gbrain/*.ts` (Phase 1 — Wave 2+ will import from here)
- `scripts/*.ts`, `scripts/*.sh` (Phase 1)
- `data/maras-coffee/**`, `brains/**`, `README.md`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 + merge with Phase 1 package.json and tsconfig.json</name>
  <files>package.json, tsconfig.json, next.config.ts, next-env.d.ts, postcss.config.mjs, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, .gitignore</files>
  <read_first>
    - /Users/abdulhannankanji/Git repos/quick-brain/package.json (existing Phase 1 scripts + deps that MUST survive the merge)
    - /Users/abdulhannankanji/Git repos/quick-brain/tsconfig.json (existing strict+bun+@-alias settings that MUST survive the merge)
    - /Users/abdulhannankanji/Git repos/quick-brain/.gitignore (existing entries; append, do not overwrite)
    - /Users/abdulhannankanji/Git repos/quick-brain/CLAUDE.md (per CLAUDE.md "Installation (First 30 Minutes)": exact non-interactive scaffold command)
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md (Decisions → Next.js Scaffold + Project Shell)
  </read_first>
  <action>
    Run `bunx create-next-app@latest . --use-bun --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"` non-interactively (per CONTEXT D-scaffold). The command will conflict with existing `package.json` / `tsconfig.json` / `.gitignore`; accept create-next-app's writes for new files (next.config.ts, next-env.d.ts, postcss.config.mjs, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css) but immediately reconcile the three conflict files: (a) merge `package.json` so all Phase 1 scripts (`seed`, `demo-check`, `detect-anomalies`, `concurrent-smoke`, `mutex-smoke`) AND the new Next.js scripts (`dev`, `build`, `start`, `lint`) coexist; keep Phase 1 deps (`zod`, `@types/bun`, `@types/node`, `typescript`) and add Next.js deps the scaffold wants (`next`, `react`, `react-dom`, `@types/react`, `@types/react-dom`, `tailwindcss`, `@tailwindcss/postcss`, `eslint`, `eslint-config-next`); (b) merge `tsconfig.json` so `strict`, `noUncheckedIndexedAccess`, `types: ["bun"]` (keep), `paths: {"@/*": ["./*"]}`, and Phase 1's `include` for `lib/**/*.ts` + `scripts/**/*.ts` survive alongside the Next.js-required `jsx`, `incremental`, `plugins: [{"name": "next"}]`, and the auto-added `app/**/*.ts(x)` + `components/**/*.ts(x)` patterns; (c) append `.next/`, `next-env.d.ts` (let next regenerate it), `node_modules` (idempotent), `.env*.local` to `.gitignore` without removing existing Phase 1 entries (`brains/`, `data/maras-coffee/.gbrain*` if present). Use Bun's runtime (`bun install` runs automatically as part of create-next-app with `--use-bun`). Document any post-merge tweak in the task done criteria.
  </action>
  <acceptance_criteria>
    - `bun run dev` starts Next.js on port 3000 with no compile errors when checked via `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` returning `200` within 15s of boot
    - `bun run seed`, `bun run demo-check`, `bun run mutex-smoke` scripts still resolve (run `bun run --help` and confirm script names listed; do NOT execute seed because it touches `brains/`)
    - `tsconfig.json` contains both `"strict": true` AND `"noUncheckedIndexedAccess": true` AND `"paths": { "@/*": ["./*"] }`
    - `tsconfig.json` `include` array contains all three: `app/**/*.ts(x)`, `lib/**/*.ts`, `scripts/**/*.ts` (or globs that cover them)
    - `tsconfig.json` `types` array contains `"bun"` AND does not break Next.js (the Next.js plugin entry under `plugins` must be present)
    - `package.json` `dependencies` contains `next`, `react`, `react-dom`, `zod`; `devDependencies` contains `@types/bun`, `@types/node`, `typescript`, `tailwindcss`
    - `package.json` `scripts` contains both Phase 1 scripts (`seed`, `demo-check`, `detect-anomalies`, `concurrent-smoke`, `mutex-smoke`) AND Next.js scripts (`dev`, `build`, `start`, `lint`)
    - `.gitignore` contains entries `node_modules`, `.next/`, `brains/`
    - `app/layout.tsx`, `app/page.tsx`, `app/globals.css` all exist
    - `bunx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <verify>
    <automated>bun run dev &amp; DEV_PID=$!; sleep 12; curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ | grep -q 200; RC=$?; kill $DEV_PID 2&gt;/dev/null; bunx tsc --noEmit &amp;&amp; [ $RC -eq 0 ]</automated>
  </verify>
  <done>
    Next.js dev server boots, returns 200 on /, tsc passes with zero errors, all Phase 1 npm scripts intact, both Phase 1 and Next.js dependencies present in package.json.
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and add primitive components</name>
  <files>components.json, lib/utils.ts, components/ui/button.tsx, components/ui/card.tsx, components/ui/input.tsx, components/ui/skeleton.tsx, components/ui/scroll-area.tsx, components/ui/badge.tsx, app/globals.css, package.json</files>
  <read_first>
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md (Decisions → "bunx shadcn@latest init -d" + "bunx shadcn@latest add card input button skeleton scroll-area badge")
    - /Users/abdulhannankanji/Git repos/quick-brain/CLAUDE.md (shadcn install procedure with Tailwind v4 + React 19)
    - /Users/abdulhannankanji/Git repos/quick-brain/app/globals.css (created by Task 1; shadcn extends this)
  </read_first>
  <action>
    Run `bunx shadcn@latest init -d` non-interactively (use the `-d` flag for default config — Tailwind v4, neutral base color, CSS variables). If the CLI requires choices not covered by `-d`, accept the New York style + neutral palette. Verify `components.json` and `lib/utils.ts` (with the `cn()` helper using `clsx` + `tailwind-merge`) appear. Then run `bunx shadcn@latest add button card input skeleton scroll-area badge` to copy the 6 primitives into `components/ui/`. Confirm `package.json` now has `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`, `@radix-ui/react-scroll-area`, `@radix-ui/react-slot` (or equivalents the shadcn CLI installs). Do NOT customize the components beyond what the CLI emits. If the CLI fails because Tailwind config is missing/different, run `bunx shadcn@latest init -d --force` or follow the CLI's printed remediation; document the resolution in the task summary.
  </action>
  <acceptance_criteria>
    - `components.json` exists with `"style"`, `"rsc": true`, `"tsx": true`, `"tailwind": { "config": "..." }`, `"aliases": { "components": "@/components", "utils": "@/lib/utils" }` keys present
    - `lib/utils.ts` exists and exports a `cn` function (verifiable via `grep -q "export.*cn" lib/utils.ts`)
    - All six primitive files exist: `components/ui/button.tsx`, `components/ui/card.tsx`, `components/ui/input.tsx`, `components/ui/skeleton.tsx`, `components/ui/scroll-area.tsx`, `components/ui/badge.tsx`
    - Each primitive file exports a default or named React component matching its filename (e.g., `button.tsx` exports `Button`)
    - `package.json` `dependencies` contains `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`
    - `bunx tsc --noEmit` exits 0
    - `bun run dev` boots without errors when an import like `import { Button } from "@/components/ui/button"` is resolved
  </acceptance_criteria>
  <verify>
    <automated>test -f components.json &amp;&amp; test -f lib/utils.ts &amp;&amp; test -f components/ui/button.tsx &amp;&amp; test -f components/ui/card.tsx &amp;&amp; test -f components/ui/input.tsx &amp;&amp; test -f components/ui/skeleton.tsx &amp;&amp; test -f components/ui/scroll-area.tsx &amp;&amp; test -f components/ui/badge.tsx &amp;&amp; grep -q "export" components/ui/button.tsx &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    Six shadcn primitives are present and exporting components; `cn()` helper exists in `lib/utils.ts`; `components.json` is configured; tsc passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write the landing CTA at app/page.tsx and form skeleton at app/onboard/page.tsx</name>
  <files>app/page.tsx, app/onboard/page.tsx</files>
  <read_first>
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/REQUIREMENTS.md (ONBD-01, ONBD-02, ONBD-08 — "no login, no API-key field, no payment")
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md (Discretion: RSC for landing, raw HTML form acceptable for 3 fields)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/ui/button.tsx (created by Task 2 — shadcn Button API)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/ui/card.tsx (created by Task 2)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/ui/input.tsx (created by Task 2)
  </read_first>
  <action>
    Replace the create-next-app default `app/page.tsx` with a React Server Component (no `"use client"`) that renders the landing screen: an `h1` headline ("QuickBrain" or "Your business brain in 60 seconds"), one paragraph of subhead copy explaining the value, and a primary CTA button using shadcn `<Button>` (wrapped in `<Link href="/onboard">` from `next/link`) reading "Start your business brain". Use Tailwind utility classes only; one bold tracking-tight font for the headline, neutral palette. The page must visibly contain the literal string "Start your business brain" (matched by ONBD-01). Create `app/onboard/page.tsx` as a Client Component (`"use client"`) that renders a shadcn `<Card>` containing an HTML `<form>` with exactly three labeled `<Input>` fields: `business-name`, `business-type`, `owner-name` (`name` attributes must be exact-case `businessName`, `businessType`, `ownerName`; HTML id/label uses the kebab-case form). Add a Submit `<Button type="submit">` reading "Create my brain". The `onSubmit` handler in this plan is a no-op (or `event.preventDefault()` + `console.log` of FormData) — the real submit logic and SSE consumption land in Plan 04. NO API-key input, NO email/password, NO "sign in" or "create account" affordance anywhere — per ONBD-08. Components stay vertically centered in a max-w-md container on a neutral background.
  </action>
  <acceptance_criteria>
    - `app/page.tsx` does NOT contain `"use client"` (must be RSC)
    - `app/page.tsx` contains the literal string `"Start your business brain"`
    - `app/page.tsx` contains an import for `next/link` and a `Link` with `href="/onboard"`
    - `app/onboard/page.tsx` contains `"use client"` as its first non-blank line
    - `app/onboard/page.tsx` form contains exactly three `<input>` (or shadcn `<Input>`) elements with `name="businessName"`, `name="businessType"`, `name="ownerName"`
    - Grep for forbidden tokens passes (zero matches required): `grep -iE "(api[ _-]?key|password|sign[ _-]?in|sign[ _-]?up|payment|credit[ _-]?card|stripe)" app/page.tsx app/onboard/page.tsx` returns no lines (must be empty, exit code 1 from grep, or 0 line count)
    - `bunx tsc --noEmit` exits 0
    - `bun run dev` followed by `curl -s http://localhost:3000/ | grep -q "Start your business brain"` and `curl -s http://localhost:3000/onboard | grep -q "businessName"` both succeed
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Start your business brain" app/page.tsx &amp;&amp; ! grep -q '^"use client"' app/page.tsx &amp;&amp; head -1 app/onboard/page.tsx | grep -q "use client" &amp;&amp; grep -q 'name="businessName"' app/onboard/page.tsx &amp;&amp; grep -q 'name="businessType"' app/onboard/page.tsx &amp;&amp; grep -q 'name="ownerName"' app/onboard/page.tsx &amp;&amp; [ "$(grep -ciE '(api[ _-]?key|password|sign[ _-]?in|sign[ _-]?up|payment|credit[ _-]?card|stripe)' app/page.tsx app/onboard/page.tsx | grep -v ':0$' | wc -l | tr -d ' ')" = "0" ] &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    Landing page renders the CTA linking to `/onboard`; onboard page renders the 3-field form skeleton with correct `name` attributes; no forbidden affordances (auth/payment/api-key); tsc passes.
  </done>
</task>

</tasks>

<verification>
End-to-end check after all three tasks:
1. `bun run dev` boots the Next.js dev server on port 3000 with no compile errors.
2. `curl http://localhost:3000/` returns HTTP 200 and the HTML contains "Start your business brain".
3. `curl http://localhost:3000/onboard` returns HTTP 200 and the HTML contains the three input `name` attributes (`businessName`, `businessType`, `ownerName`).
4. `bunx tsc --noEmit` exits 0.
5. Phase 1 scripts intact: `bun run --help` lists `seed`, `demo-check`, `mutex-smoke`, `concurrent-smoke`, `detect-anomalies`.
6. shadcn primitives importable: `import { Button } from "@/components/ui/button"` resolves under tsc.
</verification>

<success_criteria>
- Next.js 15 App Router scaffold layered on top of Phase 1 codebase without breaking Phase 1 scripts
- shadcn/ui primitives (button, card, input, skeleton, scroll-area, badge) copied into `components/ui/`
- Landing page at `/` with the literal "Start your business brain" CTA linking to `/onboard`
- `/onboard` shows a 3-field form skeleton (logic deferred to Plan 04)
- No login, no API-key field, no payment affordance anywhere on either page (ONBD-08)
- `tsconfig.json` retains Phase 1's strict settings AND accepts Next.js requirements
- `package.json` retains all Phase 1 scripts and adds Next.js scripts
- All requirements ONBD-01, ONBD-02, ONBD-08 are partially or fully satisfied by these surfaces
</success_criteria>

<output>
Create `.planning/phases/02-onboarding-theater-chat/02-01-SUMMARY.md` when done.
</output>
