---
phase: 02-onboarding-theater-chat
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/onboard/page.tsx
  - components/onboard/onboarding-progress.tsx
  - components/onboard/error-banner.tsx
autonomous: true
requirements: [ONBD-02, ONBD-04, ONBD-06, ONBD-07, ONBD-08]
must_haves:
  truths:
    - "Submitting the form at /onboard POSTs JSON to /api/tenants and on 201 opens an EventSource to /api/tenants/<id>/onboard"
    - "The page renders the 5 stage labels as they arrive, with a visible progress bar for the current stage"
    - "On 'done' event, the browser navigates to /dash/<tenantId> via router.push"
    - "On 'error' event, the page renders a red banner with the error message and a 'Try again' button that resets the form"
    - "Client-side zod validation rejects empty fields before POST; submit button shows loading state during POST"
    - "No login, no API-key input, no payment screen appears at any point in the flow"
  artifacts:
    - path: "app/onboard/page.tsx"
      provides: "Onboarding page — form + SSE consumer + redirect"
    - path: "components/onboard/onboarding-progress.tsx"
      provides: "Stage label + progress bar component, fed by EventSource events"
      exports: ["OnboardingProgress"]
    - path: "components/onboard/error-banner.tsx"
      provides: "Red banner with retry button"
      exports: ["ErrorBanner"]
  key_links:
    - from: "app/onboard/page.tsx"
      to: "/api/tenants"
      via: "fetch POST"
      pattern: "fetch.*api/tenants"
    - from: "app/onboard/page.tsx"
      to: "/api/tenants/<id>/onboard"
      via: "EventSource"
      pattern: "EventSource.*api/tenants"
    - from: "app/onboard/page.tsx"
      to: "/dash/<id>"
      via: "router.push on done"
      pattern: "router.push.*dash"
    - from: "app/onboard/page.tsx"
      to: "lib/onboarding/schemas.ts"
      via: "import { createTenantBodySchema }"
      pattern: "createTenantBodySchema"
---

<objective>
Replace the form skeleton at `/onboard` (built in Plan 01) with the full client flow: client-side zod validation, POST to `/api/tenants`, open EventSource to `/api/tenants/<id>/onboard`, render the live 5-stage progress, redirect to `/dash/<id>` when `done` arrives, render a red banner with retry on `error`.

Purpose: The orchestrator + SSE route (Plan 03) is invisible without a client. This is the visual half of ONBD-04 and the only path that satisfies ONBD-06 (auto-redirect to dashboard) and ONBD-07 (consistent 30–60s wall-clock from submit to interactive dashboard).

Output: A complete onboarding experience: form → POST → SSE stream → dashboard redirect, with graceful failure UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md
@.planning/phases/02-onboarding-theater-chat/02-02-PLAN.md
@.planning/phases/02-onboarding-theater-chat/02-03-PLAN.md
@CLAUDE.md
</context>

<interfaces>
Surface this plan consumes from earlier plans:

From `lib/onboarding/schemas.ts` (Plan 02):
- `createTenantBodySchema` — zod schema (reused for client-side validation)
- `type CreateTenantBody`

From the API surface (Plan 02 + Plan 03):
- `POST /api/tenants` → `201 { tenantId, slug }` | `400 { error, issues }` | `500 { error, message }`
- `GET /api/tenants/<id>/onboard` → SSE with frames:
  - `event: stage\ndata: { "stage": "creating-brain"|"reading-invoices"|"building-graph"|"indexing"|"ready", "label": "...", "progress": 0..1 }`
  - `event: done\ndata: { "tenantId": "..." }`
  - `event: error\ndata: { "stage": "...", "message": "..." }`

From `next/navigation`:
- `useRouter()` for `router.push("/dash/<id>")`

From `@/components/ui/*` (Plan 01):
- `Button`, `Card`, `CardHeader`, `CardTitle`, `CardContent`, `Input`, `Skeleton`

New surface this plan emits:

From `components/onboard/onboarding-progress.tsx`:
- `interface OnboardingProgressProps { stage: string; label: string; progress: number; allStages: { id: string; label: string }[] }`
- `function OnboardingProgress(props): JSX.Element`

From `components/onboard/error-banner.tsx`:
- `interface ErrorBannerProps { message: string; onRetry: () => void }`
- `function ErrorBanner(props): JSX.Element`

Form `name` attributes (locked in Plan 01, used by this plan's submit handler):
- `businessName`, `businessType`, `ownerName`

The 5 stage labels (locked in CONTEXT.md, must match server-emitted labels):
1. creating-brain — "Creating your brain"
2. reading-invoices — "Reading your invoices and emails"
3. building-graph — "Building the knowledge graph"
4. indexing — "Indexing for search"
5. ready — "Ready"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Progress + ErrorBanner components</name>
  <files>components/onboard/onboarding-progress.tsx, components/onboard/error-banner.tsx</files>
  <read_first>
    - /Users/abdulhannankanji/Git repos/quick-brain/components/ui/card.tsx (shadcn Card API)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/ui/button.tsx (shadcn Button API)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/ui/skeleton.tsx (shadcn Skeleton API)
    - /Users/abdulhannankanji/Git repos/quick-brain/lib/utils.ts (cn helper)
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md (Specifics → "Visual style: clean, neutral, looks like a SaaS — shadcn defaults")
  </read_first>
  <action>
    Create `components/onboard/onboarding-progress.tsx` as a Client Component (`"use client"`). Export `OnboardingProgress({ stage, label, progress, allStages })`. Render: a `<Card>` containing the current stage's `label` as the heading, a horizontal progress bar (a `<div>` with `bg-neutral-200` background and a child `<div>` with `bg-neutral-900` width set to `${progress * 100}%` via inline style or Tailwind's arbitrary value), and a stacked checklist of all 5 stages where each item shows a check (✓) if its index ≤ current stage index, a spinner-emulating ring (use `lucide-react`'s `Loader2` with `animate-spin`) if it equals current, and grey-dim text if it's after current. Compute current index as `allStages.findIndex(s =&gt; s.id === stage)`. Use `cn()` from `@/lib/utils` for conditional classes. Create `components/onboard/error-banner.tsx` exporting `ErrorBanner({ message, onRetry })`. Render a `<div>` with `bg-red-50 border border-red-200 text-red-900 p-4 rounded-md` containing the `message` text and a shadcn `<Button variant="outline" onClick={onRetry}>Try again</Button>`. Both files MUST start with `"use client"`. Use only the icons from `lucide-react` already installed by Plan 01's shadcn add.
  </action>
  <acceptance_criteria>
    - `components/onboard/onboarding-progress.tsx` and `components/onboard/error-banner.tsx` both exist
    - Both files have `"use client"` as the first non-comment line
    - Both export the named components (verifiable via `grep -E "^export.*function (OnboardingProgress|ErrorBanner)"`)
    - `bunx tsc --noEmit` exits 0
    - The progress component is parametric on `stage`, `label`, `progress`, `allStages` (no hardcoded stage list)
    - ErrorBanner accepts and calls `onRetry` on button click (verifiable by inspecting the source for `onClick={onRetry}`)
  </acceptance_criteria>
  <verify>
    <automated>head -1 components/onboard/onboarding-progress.tsx | grep -q "use client" &amp;&amp; head -1 components/onboard/error-banner.tsx | grep -q "use client" &amp;&amp; grep -qE "^export.*OnboardingProgress" components/onboard/onboarding-progress.tsx &amp;&amp; grep -qE "^export.*ErrorBanner" components/onboard/error-banner.tsx &amp;&amp; grep -q "onRetry" components/onboard/error-banner.tsx &amp;&amp; bunx tsc --noEmit</automated>
  </verify>
  <done>
    Both UI helper components compile and export the expected names; ErrorBanner wires the retry callback; OnboardingProgress accepts dynamic stage data; tsc passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full onboard page — form, POST, EventSource, redirect</name>
  <files>app/onboard/page.tsx</files>
  <read_first>
    - /Users/abdulhannankanji/Git repos/quick-brain/app/onboard/page.tsx (current skeleton from Plan 01 — REPLACE)
    - /Users/abdulhannankanji/Git repos/quick-brain/lib/onboarding/schemas.ts (createTenantBodySchema — reused for client-side parse)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/onboard/onboarding-progress.tsx (created by Task 1)
    - /Users/abdulhannankanji/Git repos/quick-brain/components/onboard/error-banner.tsx (created by Task 1)
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/REQUIREMENTS.md (ONBD-04, ONBD-06, ONBD-07, ONBD-08)
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/phases/02-onboarding-theater-chat/02-03-PLAN.md (server SSE event names + payload shape)
    - /Users/abdulhannankanji/Git repos/quick-brain/.planning/phases/02-onboarding-theater-chat/02-CONTEXT.md (Specifics → EventSource only supports GET, fetch+manual reader for POST)
  </read_first>
  <action>
    Replace `app/onboard/page.tsx` with a Client Component. State machine: `"form" | "submitting" | "streaming" | "error"`. Render branches: (a) `"form"`: shadcn `<Card>` with `<form onSubmit={handleSubmit}>` containing 3 labeled `<Input>` fields (`businessName`, `businessType`, `ownerName` — same names as Plan 01) and a Submit button "Create my brain"; (b) `"submitting"`: same form rendered disabled with a "Creating tenant..." spinner under the button; (c) `"streaming"`: render `<OnboardingProgress>` with the live event data; (d) `"error"`: render `<ErrorBanner message={errorMsg} onRetry={() =&gt; setState("form")} />`. `handleSubmit`: prevent default; read FormData into a plain object; call `createTenantBodySchema.safeParse(obj)` — on failure, set state to `"error"` with `errorMsg = "Please fill in all three fields"`; on success, set state to `"submitting"`, POST JSON to `/api/tenants` via `fetch`; on 201 read `{ tenantId }`, set state to `"streaming"`, open `new EventSource('/api/tenants/' + tenantId + '/onboard')`. Bind `eventSource.addEventListener('stage', e =&gt; setProgressFromJSON(e.data))`, `addEventListener('done', e =&gt; { router.push('/dash/' + JSON.parse(e.data).tenantId); })`, `addEventListener('error', e =&gt; { setState('error'); setErrorMsg(e.data ? JSON.parse(e.data).message : 'Stream closed unexpectedly'); eventSource.close(); })`. Define `ALL_STAGES` constant inside the component matching the 5 stage ids/labels (locked above) and pass to `<OnboardingProgress allStages={ALL_STAGES} ...>`. Use `useRouter()` from `next/navigation`. On unmount, close the EventSource (return cleanup from useEffect). The page must NOT request or display API keys, login, sign-up, or payment (ONBD-08). Maximum container width: `max-w-md mx-auto`, vertical padding so the card centers.
  </action>
  <acceptance_criteria>
    - `app/onboard/page.tsx` starts with `"use client"`
    - The file contains `fetch(` targeting `/api/tenants` (verifiable via grep)
    - The file contains `new EventSource(` targeting `/api/tenants/.+/onboard` (verifiable via grep with regex `EventSource.*api/tenants`)
    - The file contains `router.push` targeting `/dash/` (verifiable via grep)
    - The file imports `createTenantBodySchema` from `@/lib/onboarding/schemas` (or relative path) — verifiable via grep
    - The file has zero matches for `api[ _-]?key|password|sign[ _-]?in|sign[ _-]?up|payment|stripe|credit[ _-]?card` (case-insensitive) — ONBD-08
    - `bunx tsc --noEmit` exits 0
    - With `bun run dev` running and a valid tenant being created via the form (curl-simulated): the page transitions through `submitting → streaming → redirect`. Verified by checking the rendered HTML at `/onboard` contains the form, then triggering a POST + SSE consumption returns a successful tenantId-bearing flow.
  </acceptance_criteria>
  <verify>
    <automated>head -1 app/onboard/page.tsx | grep -q "use client" &amp;&amp; grep -q 'fetch(' app/onboard/page.tsx &amp;&amp; grep -qE 'EventSource.*api/tenants' app/onboard/page.tsx &amp;&amp; grep -qE 'router\.push.*dash' app/onboard/page.tsx &amp;&amp; grep -q "createTenantBodySchema" app/onboard/page.tsx &amp;&amp; [ "$(grep -ciE '(api[ _-]?key|password|sign[ _-]?in|sign[ _-]?up|payment|stripe|credit[ _-]?card)' app/onboard/page.tsx)" = "0" ] &amp;&amp; bunx tsc --noEmit &amp;&amp; bun run dev &amp; DEV_PID=$!; sleep 12; curl -s http://localhost:3000/onboard | grep -q 'businessName' &amp;&amp; STATUS=$?; kill $DEV_PID 2&gt;/dev/null; [ $STATUS -eq 0 ]</automated>
  </verify>
  <done>
    The onboard page handles form input, POSTs to /api/tenants, consumes the SSE stream with typed event listeners, renders live progress, redirects to /dash/<id> on done, shows the error banner with retry on error; no forbidden auth/payment affordances; tsc clean.
  </done>
</task>

</tasks>

<verification>
End-to-end browser smoke (manual or scripted curl-based):
1. `bun run dev` boots. Open `http://localhost:3000/`. Click "Start your business brain" → navigates to `/onboard`.
2. Fill 3 fields, click "Create my brain" → form shows submitting state.
3. Within ~2s, the page transitions to the streaming state showing the 5 stage labels and progressing through them in order.
4. After ~30–45s, the page redirects to `/dash/<tenantId>` (404 expected in this plan — `/dash/[id]` is built in Plan 05).
5. If the SSE emits an error event mid-stream (simulate by killing the dev server child or by sending a request with a non-existent tenant id), the page renders the red banner with "Try again" → clicking returns to form state.
6. Three consecutive runs from form submit to redirect take 30–60s wall-clock (ONBD-07).
7. No screen between `/` and `/dash/<id>` requests API keys, login, sign-up, or payment (ONBD-08).
8. `bunx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- ONBD-02 finalized: 3-field form with submit handler, zod-validated, POST-to-API wired.
- ONBD-04: 5 stage labels rendered live as SSE arrives, with a visible progress bar.
- ONBD-06: `done` event triggers automatic navigation to `/dash/<tenantId>`.
- ONBD-07: Total wall-clock from form submit to dashboard navigation is 30–60s (server stream is 30–45s + ~1s POST + redirect).
- ONBD-08: No login, no API-key field, no payment screen anywhere on the page; grep gate confirms.
- Error path: stream errors produce a red banner with "Try again" that resets the form (matches CONTEXT.md decision).
</success_criteria>

<output>
Create `.planning/phases/02-onboarding-theater-chat/02-04-SUMMARY.md` when done.
</output>
