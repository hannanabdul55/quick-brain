---
phase: 03-in-process-gbrain-refactor
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/integration/gbrain/inprocess-query.test.ts
  - tests/integration/gbrain/concurrent-smoke.test.ts
autonomous: true
requirements: [INPROC-06]

must_haves:
  truths:
    - "bun run test exits 0 — the full Phase 1 + Phase 2 + Phase 3 test suite passes"
    - "tests/integration/gbrain/inprocess-query.test.ts: queryInProcess returns >5 results for the demo question (expansion active) and >0 results with noExpand:true"
    - "The demo flow works end-to-end: the seed tenant's chat route returns an answer SSE frame without child_process.spawn anywhere in the query or think paths"
    - "No spawn('gbrain') call exists in lib/gbrain/client.ts's query or think functions (static grep gate)"
  artifacts:
    - path: "tests/integration/gbrain/inprocess-query.test.ts"
      provides: "INPROC-03 regression guard: result count parity between in-process and expected CLI behavior"
  key_links:
    - from: "tests/integration/gbrain/inprocess-query.test.ts"
      to: "lib/gbrain/engine.ts (queryInProcess)"
      via: "direct import"
      pattern: "queryInProcess"
---

<objective>
Write the INPROC-06 regression test suite and run the full verification that the
Phase 3 refactor is complete: the Phase 1 tests still pass, the new in-process
integration test confirms result-count parity, and the demo flow works end-to-end.

Purpose: Plans 03-01 and 03-02 rewrote the engine layer and query/think paths.
This plan creates the integration test that validates result quality (the critical
Gotcha 1 from Spike 006: bare hybridSearch returned 1 result; the CLI returned 21)
and runs the full suite to confirm no regressions.

Output:
- tests/integration/gbrain/inprocess-query.test.ts (new — INPROC-03/06 quality guard)
- tests/integration/gbrain/concurrent-smoke.test.ts (updated if needed for in-process)
- Phase 3 complete: all 6 INPROC requirements satisfied
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/spikes/006-gbrain-in-process/README.md
@.planning/phases/03-in-process-gbrain-refactor/03-01-SUMMARY.md
@.planning/phases/03-in-process-gbrain-refactor/03-02-SUMMARY.md
@lib/gbrain/engine.ts
@lib/gbrain/client.ts
@tests/integration/gbrain/concurrent-smoke.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From lib/gbrain/engine.ts (03-01):
  queryInProcess(tenantId: string, question: string, opts?: { noExpand?: boolean }): Promise<SearchResult[]>
  SearchResult: { slug: string; score: number; chunk_text?: string; }

From lib/gbrain/client.ts (03-02):
  query(tenantId: string, question: string, opts?: ...): Promise<string>
  think(tenantId: string, question: string, opts?: { model?: string }): Promise<GBrainResult>

Seed tenant ID: "seed" (from lib/gbrain/paths.ts SEED_TENANT_ID)

Spike 006 baseline:
  CLI `gbrain query "what was weird about last month?"` → 21 results
  Bare hybridSearch (no expansion) → 1 result
  In-process with expandFn: expandQuery → should match CLI range (15-25 results)

Integration test guard:
  describe.skipIf(!process.env.SUPABASE_DB_URL_POOLER)
  Requires SUPABASE_DB_URL_POOLER + OPENAI_API_KEY (for embedding)
  Optionally requires ANTHROPIC_API_KEY (for expansion; test degrades gracefully without it)

Execution environment note:
  This plan REQUIRES .env.local (main working tree, not a worktree without secrets).
  The integration test is guarded by describe.skipIf so it is safe to run in CI
  without secrets — it simply skips. For local validation it requires .env.local.

Existing concurrent-smoke test:
  tests/integration/gbrain/concurrent-smoke.test.ts currently tests concurrent
  spawnGBrain calls. After Phase 3, the query path no longer spawns — update
  the concurrent test to use queryInProcess if it tests the query path.
  If it only tests mutex behavior (withTenantLock), it may be unchanged.
  Read the file before deciding.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write in-process query integration test (INPROC-03/06 quality guard)</name>
  <files>tests/integration/gbrain/inprocess-query.test.ts</files>
  <behavior>
    - Test 1 (integration, skips if SUPABASE_DB_URL_POOLER unset): queryInProcess("seed", "what was weird about last month?") returns >= 10 results when ANTHROPIC_API_KEY is set (expansion active); the top result slug is one of the known seed brain slugs (e.g. "march-anomaly-summary")
    - Test 2 (integration, skips if SUPABASE_DB_URL_POOLER unset): queryInProcess("seed", "what was weird about last month?", { noExpand: true }) returns >= 1 result (vector search alone has at least 1 hit)
    - Test 3 (integration, skips if SUPABASE_DB_URL_POOLER unset): queryInProcess("seed", "top vendors by total spend") returns results whose slugs include at least one vendor-related page from the seed brain
    - Test 4 (unit): disconnectEngine("seed") resolves without throwing; calling queryInProcess after disconnectEngine reconnects and returns results (mock-based unit test, no live Supabase needed)
  </behavior>
  <action>
Create tests/integration/gbrain/inprocess-query.test.ts.

Structure:
  import { describe, it, expect, afterAll } from "vitest";
  import { queryInProcess, disconnectEngine } from "../../lib/gbrain/engine.ts";

  const hasSupabase = !!process.env.SUPABASE_DB_URL_POOLER;
  const hasOpenAI = !!process.env.OPENAI_API_KEY;
  const hasAnthropic = !!process.env.ANTHROPIC_API_KEY;

  describe("queryInProcess — in-process hybrid search", { timeout: 30_000 }, () => {
    describe.skipIf(!hasSupabase || !hasOpenAI)("live Supabase integration", () => {
      afterAll(async () => {
        await disconnectEngine("seed").catch(() => {});
      });

      it("returns >=10 results with expansion (INPROC-03)", async () => {
        const results = await queryInProcess("seed", "what was weird about last month?");
        if (hasAnthropic) {
          // Full expansion pipeline: should match CLI's ~21 results
          expect(results.length).toBeGreaterThanOrEqual(10);
        } else {
          // Degraded (no expansion): at least 1 result from vector search alone
          expect(results.length).toBeGreaterThanOrEqual(1);
        }
        // Top result must have a slug
        expect(results[0]?.slug).toBeTruthy();
      });

      it("returns >=1 result with noExpand:true (INPROC-02 basic retrieval)", async () => {
        const results = await queryInProcess("seed", "what was weird about last month?", { noExpand: true });
        expect(results.length).toBeGreaterThanOrEqual(1);
      });

      it("vendor query returns vendor-related pages (INPROC-03 topic relevance)", async () => {
        const results = await queryInProcess("seed", "top vendors by total spend");
        expect(results.length).toBeGreaterThanOrEqual(1);
        // At least one result should be from the seed brain's vendor or expense pages
        const slugs = results.map(r => r.slug);
        console.log("[inprocess-query test] vendor query slugs:", slugs.slice(0, 5));
        // Don't hard-code slug names — just verify we get relevant results
        expect(results[0]?.score).toBeGreaterThan(0);
      });
    });
  });

  // Pure unit test (no live Supabase)
  describe("disconnectEngine — unit", () => {
    it("resolves without throwing when engine not connected", async () => {
      await expect(disconnectEngine("nonexistent-tenant")).resolves.toBeUndefined();
    });
  });

NOTES:
  - The describe.skipIf pattern is established by Phase 2 tests (seed-config.test.ts, storage.test.ts)
  - Do NOT hardcode result count thresholds that would fail if ANTHROPIC_API_KEY is absent
  - Log the actual result count and top slug in the test output (console.log) so the
    executor running the test can verify the expansion is working
  - The test is tagged as an integration test by its path (tests/integration/); the vitest
    config may run integration tests only when RUN_INTEGRATION=1 — check vitest.config.ts
    and add the appropriate condition if integration tests are opt-in
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun run test -- --reporter=verbose tests/integration/gbrain/inprocess-query.test.ts</automated>
  </verify>
  <done>
    tests/integration/gbrain/inprocess-query.test.ts exists and passes.
    With SUPABASE_DB_URL_POOLER set: integration tests run and result count assertions pass.
    Without SUPABASE_DB_URL_POOLER: tests skip cleanly (no errors).
    Pure unit test (disconnectEngine nonexistent) always passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full suite regression + end-to-end demo verification</name>
  <files>tests/integration/gbrain/concurrent-smoke.test.ts</files>
  <action>
THREE VERIFICATION STEPS:

STEP 1 — Review and update concurrent-smoke.test.ts if needed:
  Read tests/integration/gbrain/concurrent-smoke.test.ts.
  If it tests concurrent spawnGBrain calls in the query path, update those calls
  to use queryInProcess instead (the concurrent query path is now in-process).
  If it only tests the mutex/lock behavior using withTenantLock directly, it needs
  no changes — withTenantLock is unchanged.
  If the test is guarded by RUN_INTEGRATION=1 or a similar env var, keep that guard.
  After any changes, run: bun run test -- tests/integration/gbrain/concurrent-smoke.test.ts

STEP 2 — Full test suite:
  Run: bun run test
  All tests must exit 0. Pay particular attention to:
  - tests/unit/gbrain/mutex.test.ts (INPROC-05: mutex-smoke must pass unchanged)
  - tests/unit/gbrain/client-inprocess.test.ts (INPROC-02/04: new tests from 03-02)
  - tests/unit/gbrain/engine.test.ts (INPROC-01/02: new tests from 03-01)
  - tests/integration/gbrain/inprocess-query.test.ts (INPROC-06: new integration test)
  - All Phase 1 tests (audit, insights, infra) — these must not regress

  If any test fails:
  - Unit test failures: fix the implementation (do not weaken the test assertions)
  - Integration test failures due to missing env vars: verify the test uses skipIf guard
  - Type errors: run bunx tsc --noEmit and fix

STEP 3 — Grep gate: no child_process spawn of gbrain in query/think paths:
  Run each of these; they must return 0 matches:
    grep -n "spawn.*gbrain" lib/gbrain/client.ts
    -- If spawnGBrain appears in client.ts, verify it is ONLY in the spawnGBrain
       function definition and runOnce (not in query() or think())

  The ALLOWED spawns after Phase 3:
    - spawnGBrain / runOnce in client.ts (used by onboard.ts)
    - Any spawn in onboard.ts (untouched)
    - Any spawn in orchestrator.ts (untouched)

  The FORBIDDEN spawns after Phase 3:
    - Any spawn inside the query() function body
    - Any spawn inside the think() function body
    - Any spawn inside app/api/tenants/[id]/chat/route.ts

  Run: grep -n "child_process\|spawnGBrain" app/api/tenants/\[id\]/chat/route.ts
  Result must be: 0 matches (PASS) or file not found.
  If matches exist: they must be COMMENTS only, not live imports or calls.

STEP 4 — INPROC requirements checklist (confirm each before writing SUMMARY):
  INPROC-01: grep -c '"gbrain".*garrytan/gbrain' package.json should be >= 1
  INPROC-02: grep -L "spawn" lib/gbrain/engine.ts should return the file (no spawn in engine.ts)
  INPROC-03: integration test shows >= 10 results with expansion (or log count if ANTHROPIC unavailable)
  INPROC-04: grep -L "spawnGBrain\|buildThinkArgs" app/api/tenants/\[id\]/chat/route.ts returns file
  INPROC-05: bun run test -- tests/unit/gbrain/mutex.test.ts exits 0
  INPROC-06: bun run test exits 0

  Document the actual result count seen for INPROC-03 in the SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun run test 2>&1 | tail -20</automated>
  </verify>
  <done>
    bun run test exits 0 — all Phase 1, Phase 2, and Phase 3 tests pass (INPROC-06).
    Grep gate: no spawn("gbrain") in query() or think() function bodies, no spawnGBrain in chat route.
    INPROC-01..06 checklist passes as documented above.
    Concurrent-smoke test updated if needed and passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test → Supabase Postgres | Test reads live data; credentials from .env.local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Information Disclosure | .env.local credentials in test process | accept | Tests run locally with developer credentials. CI skips integration tests (no SUPABASE_DB_URL_POOLER in CI env). describe.skipIf guard is the control. |
| T-03-08 | Repudiation | In-process test asserts result count without baseline | mitigate | Test logs actual result count + top slug. Executor documents observed count in SUMMARY for future comparison. Known baseline: Spike 006 CLI = 21 results; threshold = 10 (half) to allow for model/expansion variance. |
| T-03-SC | Tampering | No new package installs in this plan | accept | N/A — no new dependencies. |
</threat_model>

<verification>
Final Phase 3 verification checklist (run on main working tree with .env.local):

```bash
# 1. Full test suite
bun run test

# 2. No spawn of gbrain in query/think paths
echo "=== Grep gate ===" && \
grep -n "spawn" lib/gbrain/client.ts | grep -v "spawnGBrain\|runOnce\|//\|function spawnGBrain" && echo "FAIL: unexpected spawn" || echo "PASS: no unexpected spawn" && \
grep -c "spawnGBrain\|buildThinkArgs" "app/api/tenants/[id]/chat/route.ts" && echo "FAIL: route has old imports" || echo "PASS: route clean"

# 3. INPROC-01: gbrain SHA-pinned
grep '"gbrain".*garrytan' package.json && echo "PASS: gbrain pinned" || echo "FAIL: gbrain not in package.json"

# 4. INPROC-03: result count (requires .env.local)
set -a && . .env.local && set +a && \
bun run test -- --reporter=verbose tests/integration/gbrain/inprocess-query.test.ts

# 5. TypeScript clean
bunx tsc --noEmit 2>&1 | grep "error TS" | wc -l
```
</verification>

<success_criteria>
- bun run test exits 0 — full Phase 1 + Phase 2 + Phase 3 suite passes (INPROC-06)
- Grep gate: spawn("gbrain") does not appear in query() or think() function bodies; chat route has no spawnGBrain import
- Integration test: queryInProcess("seed", demo-question) returns >= 10 results with expansion (INPROC-03)
- mutex-smoke test passes unchanged (INPROC-05)
- INPROC requirements 01-06 all satisfied (verified by checklist in Task 2 Step 4)
</success_criteria>

<output>
Create `.planning/phases/03-in-process-gbrain-refactor/03-03-SUMMARY.md` when done.
Follow the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
Include the INPROC-01..06 checklist results and the observed result count from the integration test.
</output>
