# Phase 3 — Continue Here (handoff 2026-05-20)

Paused mid-Phase-3 at the Wave 2 / Wave 3 boundary. Context budget ran low.

## Where Phase 3 stands

| Wave | Plan | State |
|------|------|-------|
| 1 | 03-01 | ✅ Done — in-process gbrain engine (`lib/gbrain/engine.ts`), gbrain SHA-pinned dep, `queryInProcess` returns ~25 results (CLI parity). Pushed. |
| 2 | 03-02 | ⚠️ Committed + pushed (`94fa5f5`, `b765ae7`, `7c7927a`) but **NOT clean** — see blocker below. |
| 3 | 03-03 | ❌ Not started — integration test + demo-flow regression (INPROC-06). |

`origin/main` is at `7c7927a` — everything through Wave 2 is pushed.

## Critical Anti-Patterns

| # | Pattern | Severity |
|---|---------|----------|
| 1 | Trusting unit-test-green as "plan done" — 03-02's 113 unit tests passed but the live in-process `think` returns 0 chars. Always live-verify the query/think path against the real brain (`set -a && . ./.env.local && set +a`). | blocking |

## BLOCKER — fix first next session

**In-process `think` returns an empty answer.** Live check:
`bun -e 'const {think}=await import("./lib/gbrain/client.ts"); const r=await think({tenantId:"seed",question:"what was weird about last month?"}); ...'`
→ `[400ms] think OK — 0 chars`.

- 400ms is far too fast for real LLM synthesis → `think`/`runThink` is not actually synthesizing.
- 03-02 shimmed `runThink` via `_load("core/think/index")` (that subpath is absent from gbrain's `package.json` exports map — suspect). Verify the real gbrain `think` entry point + result shape.
- The chat route (`app/api/tenants/[id]/chat/route.ts`) now depends on this `think` → chat would render an empty answer.
- Related Wave 2 deviation: `MARAS_COFFEE_SYSTEM_PROMPT` is no longer forwarded (`RunThinkOpts` v0.28 has no `systemPrompt` field). For v2.0 the per-business system prompt matters — revisit when fixing `think`.

Reference: gbrain CLI `think` impl at `/Users/abdulhannankanji/Git repos/gbrain/src/cli.ts` case `'think'` (~line 1154). Trace how the CLI invokes synthesis and produces non-empty output; replicate that in-process.

## Then: Wave 3 (03-03)

`.planning/phases/03-in-process-gbrain-refactor/03-03-PLAN.md` — integration test + demo-flow regression (INPROC-06). Needs `.env.local` (live query). Run on the main tree.

## Verified-good state (don't redo)

- `bunx tsc --noEmit` clean. `bun run test` (no creds) green: 113 passed / 4 skipped.
- `queryInProcess` (search/retrieval) works in-process — 25 results, expansion fires (gateway init via `configureGateway` in `createGBrainEngine`).
- The gbrain shim: `types/gbrain.ts` (typed wrappers + computed `import("gbrain/"+x)`); never `import from "gbrain/*"` statically. `@/*` alias is wired in `vitest.config.ts`.

## Bigger picture

v2.0 = 10 phases. 1-2 shipped. Phase 3 in progress (this handoff). Phases 4-10 not started; Phase 4 (Vercel deploy) needs `vercel link` (done — project `quickbrain`), Phase 6 (auth) needs `RESEND_API_KEY` + verified domain, Phase 7 (QBO) needs an Intuit dev app.

Security note carried from the session: the Supabase `service_role` key and `OPENAI_API_KEY` were printed in a chat transcript — rotate if that transcript left the local machine.
