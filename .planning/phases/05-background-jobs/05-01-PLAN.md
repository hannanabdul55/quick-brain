---
phase: 05-background-jobs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - lib/inngest/client.ts
  - app/api/inngest/route.ts
  - .env.example
autonomous: false
requirements: [JOBS-01]
user_setup:
  - service: inngest
    why: "Background-job runner — production execution + dashboard. Local dev uses bunx inngest-cli dev with no cloud keys."
    env_vars:
      - name: INNGEST_EVENT_KEY
        source: "Inngest Dashboard -> the quickbrain app -> Event Keys (server-only, never NEXT_PUBLIC_)"
      - name: INNGEST_SIGNING_KEY
        source: "Inngest Dashboard -> the quickbrain app -> Signing Key (server-only; serve() verifies request HMAC signatures with it)"
    dashboard_config:
      - task: "Create an Inngest cloud account and an app named quickbrain"
        location: "inngest.com -> sign up -> create app"
      - task: "After Vercel deploy, register the deployed serve route URL (https://<deploy-url>/api/inngest) so Inngest can invoke functions"
        location: "Inngest Dashboard -> Apps -> Sync (manual env-var path, NOT the Vercel Marketplace integration)"

must_haves:
  truths:
    - "D-01: Inngest is the chosen background-job runner — the inngest package is installed and pinned in package.json"
    - "An Inngest client singleton is importable by both the serve route and the trigger route"
    - "GET /api/inngest returns a 200 JSON introspection payload locally"
  artifacts:
    - path: "lib/inngest/client.ts"
      provides: "Inngest client singleton exported as `inngest`"
      contains: "new Inngest"
    - path: "app/api/inngest/route.ts"
      provides: "Inngest App Router serve handler exporting GET/POST/PUT"
      contains: "serve"
    - path: ".env.example"
      provides: "INNGEST_EVENT_KEY + INNGEST_SIGNING_KEY documented"
      contains: "INNGEST_SIGNING_KEY"
  key_links:
    - from: "app/api/inngest/route.ts"
      to: "lib/inngest/client.ts"
      via: "import inngest"
      pattern: "from .@/lib/inngest/client."
---

<objective>
Install Inngest (the D-01-locked job runner) and stand up its serve route plus client singleton. This is the foundation every other Phase 5 plan builds on — the trigger route, the generic job function, and the deploy smoke test all consume the artifacts created here.

Purpose: Inngest is the only sanctioned new vendor for Phase 5. A clean, verified install + a working serve route is the prerequisite for routing any operation through the job path.
Output: `inngest` in `package.json`, `lib/inngest/client.ts`, `app/api/inngest/route.ts`, two new env vars documented in `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-background-jobs/05-CONTEXT.md
@.planning/phases/05-background-jobs/05-RESEARCH.md
@.planning/phases/05-background-jobs/05-PATTERNS.md

<interfaces>
<!-- Codebase conventions the executor must replicate. cclsp MCP is NOT connected
     this session — inspect .ts/.tsx via plain-text cat/grep, never LSP. -->

vercel.json (current — DO NOT edit):
  { "bunVersion": "1.x", "functions": { "app/api/**/*.ts": { "runtime": "bun@1.2.0" } } }
  -> the new app/api/inngest/route.ts is caught by this glob. It runs under the
     bun@1.2.0 Vercel function runtime. RESEARCH Pitfall 5 flags this as LOW risk
     but unverified; the deploy smoke test is in plan 05-05, not here.

next.config.ts: serverExternalPackages: ["gbrain"]; outputFileTracingIncludes maps
  "app/api/**/route.ts" to node_modules/{gbrain,postgres,...}/**. Inngest is a normal
  ESM package with static imports — the Vercel tracer follows it; no extra
  outputFileTracingIncludes entry is expected. DO NOT edit next.config.ts in this plan.

Route segment config convention (every API route — onboard/route.ts, chat/route.ts):
  export const dynamic = "force-dynamic";
  export const runtime = "nodejs";   // Next.js runtime segment, DISTINCT from the
                                     // bun@1.2.0 Vercel function runtime in vercel.json

Module-singleton convention (lib/gbrain/engine.ts lines 53-54): a single
  module-level export imported by multiple consumers.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify inngest package legitimacy before install</name>
  <read_first>
    - .planning/phases/05-background-jobs/05-RESEARCH.md (Package Legitimacy Audit table + Assumptions Log A2)
  </read_first>
  <what-built>
    Nothing built yet. RESEARCH tagged `inngest` as `[ASSUMED]` because slopcheck could
    not run this session. Per the package-legitimacy gate, a human must confirm the
    package is legitimate before `bun add inngest` runs.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/inngest in a browser.
    2. Confirm: the package is published by the `inngest` org; the repository link is
       github.com/inngest/inngest-js; the license is Apache-2.0; the latest version is
       v4.x (research saw 4.4.0, published 2026-05-19); weekly downloads are high
       (hundreds of thousands — a well-known background-job product, not a niche package).
    3. Confirm there is no `postinstall` script listed (research ran `npm view inngest
       scripts.postinstall` and it returned empty).
    4. This package is the one explicitly named in `.planning/ROADMAP.md` Phase 5
       ("route long work through Inngest").
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the npmjs.com page matches the four checks above.
    - Human types "approved" (or describes a discrepancy, which blocks the phase).
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow `bun add inngest`, or describe the discrepancy.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install inngest and create the client singleton</name>
  <read_first>
    - package.json (current dependencies block — inngest is absent)
    - lib/gbrain/engine.ts (module-singleton convention, lines ~48-60)
    - .planning/phases/05-background-jobs/05-RESEARCH.md (Pattern 2 — lib/inngest/client.ts shape)
    - .planning/phases/05-background-jobs/05-PATTERNS.md (`lib/inngest/client.ts` section)
  </read_first>
  <action>
    Run `bun add inngest` (only after Task 1's checkpoint is approved). This updates
    `package.json` and `bun.lock`. Do NOT install `inngest-cli` — it is run on demand
    via `bunx inngest-cli@latest dev`, never added as a dependency.

    Create `lib/inngest/client.ts`: a single module-level export named `inngest`,
    constructed as `new Inngest({ id: "quickbrain" })`, imported from the `inngest`
    package. This mirrors the `enginePool` module-level singleton in `lib/gbrain/engine.ts`
    — one export, imported by multiple consumers (the serve route and, in plan 05-04,
    the trigger route). No other exports in this file.
  </action>
  <verify>
    <automated>grep -q '"inngest"' package.json && bun -e "const m=require('./lib/inngest/client.ts'); if(!m.inngest) throw new Error('inngest export missing'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains an `inngest` entry with a `^4.` version.
    - `bun.lock` is updated and committed.
    - `lib/inngest/client.ts` exports a const named `inngest` that is an `Inngest`
      instance constructed with `id: "quickbrain"`.
    - `inngest-cli` does NOT appear anywhere in `package.json`.
    - `bunx tsc --noEmit` reports no errors introduced by the new file.
  </acceptance_criteria>
  <done>The inngest package is installed and pinned; `lib/inngest/client.ts` exports a usable client singleton.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Inngest serve route and document env vars</name>
  <read_first>
    - app/api/tenants/[id]/onboard/route.ts (route segment config: `dynamic`, `runtime`)
    - lib/inngest/client.ts (created in Task 2 — the client to pass to serve())
    - .env.example (current key inventory — add the two INNGEST_ vars)
    - .planning/phases/05-background-jobs/05-RESEARCH.md (Pattern 2 — serve route; Security Domain — INNGEST_SIGNING_KEY)
    - .planning/phases/05-background-jobs/05-PATTERNS.md (`app/api/inngest/route.ts` section)
  </read_first>
  <action>
    Create `app/api/inngest/route.ts`. It imports `serve` from `inngest/next` and the
    `inngest` client from `@/lib/inngest/client`. It exports `export const runtime =
    "nodejs"` (codebase convention — matches onboard/chat routes; this is the Next.js
    runtime segment, distinct from the bun@1.2.0 Vercel function runtime). Then it
    exports a destructured `{ GET, POST, PUT }` from `serve({ client: inngest, functions: [] })`.

    Pass an empty `functions` array for now — the generic `runJob` function is created
    in plan 05-04 and added to this array there. The route must still respond to
    `GET /api/inngest` with a 200 introspection JSON even with zero functions registered.

    Add two entries to `.env.example` under a new `### Inngest (background jobs)` section,
    each with a one-line comment: `INNGEST_EVENT_KEY=` (Inngest SDK -> cloud event auth;
    server-only, never NEXT_PUBLIC_) and `INNGEST_SIGNING_KEY=` (serve() verifies the HMAC
    signature on every inbound request with this; MUST be set in production so the route
    rejects unsigned/forged requests — RESEARCH Security Domain, threat T-05-01).
    Also note in a comment that `INNGEST_DEV=1` is set only for local `bun run dev`.
  </action>
  <verify>
    <automated>grep -q 'INNGEST_SIGNING_KEY' .env.example && grep -q 'INNGEST_EVENT_KEY' .env.example && grep -Eq 'serve\(' app/api/inngest/route.ts && grep -q 'runtime = "nodejs"' app/api/inngest/route.ts && bunx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/inngest/route.ts` exports `GET`, `POST`, and `PUT` (destructured from `serve(...)`).
    - The route declares `export const runtime = "nodejs"`.
    - `serve()` is called with `{ client: inngest, functions: [] }` (empty array — plan 05-04 fills it).
    - `.env.example` contains `INNGEST_EVENT_KEY=` and `INNGEST_SIGNING_KEY=` each with a
      comment marking them server-only (no NEXT_PUBLIC_ prefix).
    - `bunx tsc --noEmit` passes.
    - Behavior: with `bun run dev` running, `curl -s localhost:3000/api/inngest` returns
      HTTP 200 and a JSON body (Inngest's introspection payload). Recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The Inngest serve route responds 200 to GET locally; both INNGEST_ env vars are documented as server-only secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest cloud -> /api/inngest serve route | Inngest's cloud invokes the public serve route to run functions; the request crosses from an external service into the app |
| npm registry -> repo dependency tree | `bun add inngest` pulls third-party code into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | `app/api/inngest/route.ts` serve handler | mitigate | `INNGEST_SIGNING_KEY` set in production env -> `serve()` from `inngest/next` automatically verifies the HMAC signature on every inbound request. Documented as a required server-only secret in `.env.example` (Task 3). Without it the route accepts unsigned requests — flagged in the env comment |
| T-05-02 | Information Disclosure | `INNGEST_EVENT_KEY` / `INNGEST_SIGNING_KEY` | mitigate | Both vars documented in `.env.example` with no `NEXT_PUBLIC_` prefix; comment marks them server-only. Stored in Vercel encrypted env config + gitignored `.env.local` (matches DEPLOY-02). Never referenced in client components |
| T-05-SC | Tampering | `bun add inngest` (npm install) | mitigate | Task 1 is a blocking `checkpoint:human-verify` — a human verifies `inngest` on npmjs.com (official org repo, Apache-2.0, no postinstall script) before install. `inngest` is `[ASSUMED]` per RESEARCH A2 because slopcheck was unavailable; the checkpoint is the substitute gate and is never auto-approvable |
</threat_model>

<verification>
- `bunx tsc --noEmit` passes for the whole repo.
- `bun run dev` + `curl -s localhost:3000/api/inngest` returns 200 JSON.
- `package.json` carries `inngest` ^4.x; `inngest-cli` is absent.
- `bunx eslint app/api/inngest/route.ts lib/inngest/client.ts` reports no errors.
</verification>

<success_criteria>
- The `inngest` dependency is installed only after a human legitimacy check.
- `lib/inngest/client.ts` exports a reusable `inngest` client singleton.
- `app/api/inngest/route.ts` serves GET/POST/PUT and returns 200 on GET locally.
- Both Inngest secrets are documented as server-only in `.env.example`.
</success_criteria>

<output>
Create `.planning/phases/05-background-jobs/05-01-SUMMARY.md` when done.
</output>
</content>
