---
phase: 01-test-harness-ci
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests/unit/audit/anomaly-detector.test.ts
  - tests/unit/insights/anomalies.test.ts
  - tests/unit/insights/pnl.test.ts
  - tests/unit/insights/top-vendors.test.ts
  - tests/unit/insights/frontmatter.test.ts
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "All four anomaly-detection functions (detectPriceHikes, detectDuplicates, detectGhosts, detectMissingInvoices) are covered by unit tests"
    - "The bulletRegex output contract is verified — a known bullet string is matched by the regex"
    - "lib/insights parsers (computePnl, computeTopVendors, parseFrontmatter, coerceValue) have unit tests"
    - "All unit tests pass under `bun run test` without any file system access to brains/ or API keys"
    - "The four anomaly types each have at least one happy-path test and one edge-case test"
  artifacts:
    - path: "tests/unit/audit/anomaly-detector.test.ts"
      provides: "Unit tests for detectPriceHikes, detectDuplicates, detectGhosts, detectMissingInvoices, and pure helpers"
      contains: "detectPriceHikes"
    - path: "tests/unit/insights/anomalies.test.ts"
      provides: "Unit tests for computeAnomalies (via inline fixture) and bulletRegex contract"
      contains: "bulletRegex"
    - path: "tests/unit/insights/pnl.test.ts"
      provides: "Unit tests for computePnl and parseMonthlyClose via temp file fixture"
      contains: "computePnl"
    - path: "tests/unit/insights/top-vendors.test.ts"
      provides: "Unit tests for computeTopVendors via temp directory fixtures"
      contains: "computeTopVendors"
    - path: "tests/unit/insights/frontmatter.test.ts"
      provides: "Unit tests for parseFrontmatter and coerceValue"
      contains: "parseFrontmatter"
  key_links:
    - from: "tests/unit/audit/anomaly-detector.test.ts"
      to: "lib/audit/anomaly-detector.ts"
      via: "named import of detect* and helper functions"
      pattern: "detectPriceHikes|detectDuplicates|detectGhosts|detectMissingInvoices"
    - from: "tests/unit/insights/anomalies.test.ts"
      to: "lib/insights/anomalies.ts bulletRegex"
      via: "inline reconstruction of the regex constant"
      pattern: "bulletRegex"
---

<objective>
Write unit tests for all four pure anomaly detectors in `lib/audit/anomaly-detector.ts`
and for the parser functions in `lib/insights/`. All tests are filesystem-free
(in-memory fixtures only) or use `node:fs/promises` to write a temp file and
clean up, so they run safely in CI with no brains/ directory and no API keys.

Purpose: Fulfill TEST-04 — provide a regression net for the core business logic
(anomaly rules + insight parsers) before Phase 2 changes the data layer under them.

Output:
- tests/unit/audit/anomaly-detector.test.ts
- tests/unit/insights/anomalies.test.ts
- tests/unit/insights/pnl.test.ts
- tests/unit/insights/top-vendors.test.ts
- tests/unit/insights/frontmatter.test.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-test-harness-ci/01-01-SUMMARY.md
@lib/audit/anomaly-detector.ts
@lib/insights/anomalies.ts
@lib/insights/pnl.ts
@lib/insights/top-vendors.ts
@lib/insights/frontmatter.ts

<interfaces>
<!-- Extracted from the codebase. Executor uses these directly — no codebase exploration needed. -->

From lib/audit/anomaly-detector.ts — INTERNAL (not exported) functions to test.
The detect* functions are private; the test file must be in the SAME unit as the
module OR the functions must be exported. Since they are currently private, the
executor MUST add named exports for the pure functions before writing tests.
Add these exports to lib/audit/anomaly-detector.ts (they are pure, no side-effects):

  export function detectPriceHikes(invoices: Doc[]): PriceHike[]      // line ~143
  export function detectDuplicates(debits: BankDebit[]): Duplicate[]  // line ~207
  export function detectGhosts(companies: Doc[], debits: BankDebit[]): Ghost[]        // line ~235
  export function detectMissingInvoices(invoices: Doc[], debits: BankDebit[]): MissingInvoice[] // line ~276
  export function parseFrontmatter(raw: string): { frontmatter: Frontmatter; body: string }     // line ~66
  export function parseDebits(bankStatements: Doc[]): BankDebit[]     // line ~187
  export function ym(date: string): string                            // line ~116 → "YYYY-MM"
  export function usd(n: number): string                              // line ~128 → "$X.XX"
  export function severityFor(dollarImpact: number, anomalyType: string): string     // line ~132
  export { DEMO_TODAY, GHOST_THRESHOLD_DAYS, PRICE_HIKE_THRESHOLD_PCT, DUPLICATE_WINDOW_DAYS }

Types (add to exports of anomaly-detector.ts):
  export type { Doc, Frontmatter, BankDebit, PriceHike, Duplicate, Ghost, MissingInvoice }

Constants in anomaly-detector.ts:
  DEMO_TODAY = new Date("2026-04-05T00:00:00Z")
  GHOST_THRESHOLD_DAYS = 90
  PRICE_HIKE_THRESHOLD_PCT = 20  (threshold is > 20%, so a 21% hike triggers)
  DUPLICATE_WINDOW_DAYS = 7

Doc shape:
  { path: string; slug: string; frontmatter: Frontmatter; body: string }
  where Frontmatter = Record<string, string | number | string[]>

BankDebit shape:
  { vendor: string; date: string; amount: number; sourceSlug: string }

PriceHike shape (approximate — verify with get_hover before writing):
  { vendor: string; prevMonth: string; curMonth: string; prevTotal: number;
    curTotal: number; dollarDelta: number; pctChange: number }

Duplicate shape (approximate):
  { vendor: string; amount: number; date1: string; date2: string; sourceSlug: string }

Ghost shape (approximate):
  { vendor: string; slug: string; lastEventDate: string; daysSinceEvent: number;
    monthlyAmount: number; activeMonths: number }

MissingInvoice shape (approximate):
  { vendor: string; slug: string; month: string; debitAmount: number; sourceSlug: string }

From lib/insights/anomalies.ts:
  bulletRegex = /^- (\d{4}-\d{2}-\d{2}):\s+\[\[([^\]]+)\]\]\s+(.+?)$/
  export async function computeAnomalies(fixturesDir: string): Promise<AnomalyRow[]>
  AnomalyRow = { date: string; vendorSlug: string; description: string;
                  dollarImpact: number; sourcePath: string }
  Note: computeAnomalies reads concepts/march-anomaly-summary.md from the dir.
  Test it by writing a temp file using node:os.tmpdir() + node:fs/promises.

From lib/insights/pnl.ts:
  export async function computePnl(fixturesDir: string): Promise<PnlSnapshot>
  PnlSnapshot shape (from lib/insights/types.ts):
    { revenue: number; expenses: number; net: number; revenueLabel: string;
      expensesLabel: string; prevNet: number | null }
  parseMonthlyClose is internal (not exported) — test via computePnl only.
  computePnl reads originals/monthly-close-2026-03.md and originals/monthly-close-2026-02.md.
  The line format: "- Revenue: $X,XXX.XX" or "- Expenses: $X,XXX.XX"

From lib/insights/top-vendors.ts:
  export async function computeTopVendors(fixturesDir: string, topN?: number): Promise<TopVendorRow[]>
  TopVendorRow = { vendor: string; total: number; invoiceCount: number; lastDate: string }
  computeTopVendors reads originals/invoice-*.md files.

From lib/insights/frontmatter.ts:
  export function parseFrontmatter(text: string): Record<string, unknown>
  export function coerceValue(raw: string): unknown  (may be internal — check before exporting)
  coerceValue handles: numbers, booleans, quoted strings, arrays, plain strings

Note: lib/insights/frontmatter.ts parseFrontmatter is DIFFERENT from
lib/audit/anomaly-detector.ts parseFrontmatter — they have different signatures.
Keep imports scoped to avoid confusion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export pure functions from lib/audit/anomaly-detector.ts and write unit tests</name>
  <files>lib/audit/anomaly-detector.ts, tests/unit/audit/anomaly-detector.test.ts</files>
  <behavior>
    - detectPriceHikes([invoice for vendor A at $100 in Jan, $130 in Feb]) returns 1 PriceHike (30% > 20% threshold)
    - detectPriceHikes([invoice for vendor A at $100 in Jan, $110 in Feb]) returns [] (10% under threshold)
    - detectDuplicates([debit vendor A $50 on day 1, debit vendor A $50 on day 5]) returns 1 Duplicate (within 7d window)
    - detectDuplicates([debit vendor A $50 on day 1, debit vendor A $50 on day 8]) returns [] (outside window)
    - detectDuplicates([debit vendor A $50, debit vendor B $50 same day]) returns [] (different vendors)
    - detectGhosts([company with no body events, debit for that company in last 3 months]) returns 1 Ghost (>90d since last event)
    - detectMissingInvoices([debit for vendor X in March, no invoice for vendor X in March]) returns 1 MissingInvoice
    - detectMissingInvoices([debit for vendor X in March, invoice for vendor X in March]) returns []
    - ym("2026-03-15") returns "2026-03"
    - usd(12.5) returns "$12.50"
    - severityFor(150, "price-hike") returns "high" (>$100)
    - severityFor(50, "duplicate") returns "medium" ($30-$100)
    - severityFor(20, "ghost-saas") returns "high" (ghost-saas type overrides)
    - severityFor(20, "missing-invoice") returns "high" (missing-invoice type overrides)
    - parseFrontmatter("---\ntype: invoice\namount: 123.45\n---\nbody") returns { frontmatter: {type:"invoice", amount:123.45}, body:"body" }
  </behavior>
  <action>
    First, modify `lib/audit/anomaly-detector.ts` to export the pure functions
    and types. Add `export` keyword before each of these declarations (they are
    currently unexported private functions and types):
    - function parseFrontmatter → export function parseFrontmatter
    - function ym → export function ym
    - function usd → export function usd
    - function severityFor → export function severityFor
    - function parseDebits → export function parseDebits
    - function detectPriceHikes → export function detectPriceHikes
    - function detectDuplicates → export function detectDuplicates
    - function detectGhosts → export function detectGhosts
    - function detectMissingInvoices → export function detectMissingInvoices
    - const DEMO_TODAY → export const DEMO_TODAY
    - const GHOST_THRESHOLD_DAYS → export const GHOST_THRESHOLD_DAYS
    - const PRICE_HIKE_THRESHOLD_PCT → export const PRICE_HIKE_THRESHOLD_PCT
    - const DUPLICATE_WINDOW_DAYS → export const DUPLICATE_WINDOW_DAYS
    - type Doc, Frontmatter, BankDebit, PriceHike, Duplicate, Ghost, MissingInvoice → export type

    Use LSP `get_hover` to confirm each type shape before writing tests.
    Use `find_definition` on detectPriceHikes, detectDuplicates, detectGhosts,
    detectMissingInvoices to see the exact parameter types.

    Then create `tests/unit/audit/anomaly-detector.test.ts`. Import all pure
    functions from "../../lib/audit/anomaly-detector.ts" using named imports.

    Build in-memory fixture Doc arrays to pass to each detector. Do NOT read from
    disk — all fixtures are inline objects. Follow the behavior block above.

    Structure the test file with one describe block per function:
    - "detectPriceHikes" — 2 tests (triggers, does not trigger)
    - "detectDuplicates" — 3 tests (same vendor within window, outside window, different vendors)
    - "detectGhosts" — 1 happy-path test (company with no events + recent debit = ghost)
    - "detectMissingInvoices" — 2 tests (missing invoice, matching invoice)
    - "ym / usd / severityFor" — 6 tests covering the behavior block above
    - "parseFrontmatter (audit)" — 1 test with the frontmatter + body format

    For detectGhosts: the ghost rule checks that the company body has NO event
    timeline bullets within GHOST_THRESHOLD_DAYS (90 days) of DEMO_TODAY
    (2026-04-05). Construct a company Doc with body "" (no events) and a debit
    within the last 3 months. Use DEMO_TODAY from the import.

    Run `bunx tsc --noEmit` after adding exports — it must pass.
  </action>
  <verify>
    <automated>bun run test tests/unit/audit/anomaly-detector.test.ts --reporter=verbose 2>&1 | grep -E "PASS|passed|failed"</automated>
  </verify>
  <done>
    - lib/audit/anomaly-detector.ts has export keyword on all 9 pure functions + constants + types
    - tests/unit/audit/anomaly-detector.test.ts has >= 15 test cases covering all four detect* functions and helpers
    - All tests pass under `bun run test`
    - `bunx tsc --noEmit` remains clean after exports are added
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for lib/insights parsers (anomalies, pnl, top-vendors, frontmatter)</name>
  <files>
    tests/unit/insights/anomalies.test.ts,
    tests/unit/insights/pnl.test.ts,
    tests/unit/insights/top-vendors.test.ts,
    tests/unit/insights/frontmatter.test.ts
  </files>
  <behavior>
    anomalies.test.ts:
    - bulletRegex matches "- 2026-03-15: [[companies/beanstalk-roasters]] Price hike: $330 ($150 more this month)"
    - bulletRegex does NOT match "- 2026-03-15: [[beanstalk-roasters]] Bad format" (missing companies/ prefix)
    - bulletRegex does NOT match "- Detection method: hike" (starts with "Detection method")
    - computeAnomalies(tempDir) with a valid march-anomaly-summary.md returns AnomalyRow[] with correct fields
    - computeAnomalies(tempDir) with a march-anomaly-summary.md containing < 3 valid bullets throws

    pnl.test.ts:
    - computePnl(tempDir) with valid monthly-close-2026-03.md returns { revenue, expenses, net } with correct arithmetic
    - computePnl(tempDir) with both current + prev monthly-close returns prevNet populated
    - computePnl(tempDir) where prev file is missing returns prevNet: null (no throw)

    top-vendors.test.ts:
    - computeTopVendors(tempDir) with 3 invoice files returns sorted array by total descending
    - computeTopVendors(tempDir, 2) returns at most 2 entries
    - Invoice files without type: invoice frontmatter are ignored

    frontmatter.test.ts (lib/insights/frontmatter.ts):
    - parseFrontmatter("---\ntype: invoice\namount: 99.5\n---\nbody") returns {type:"invoice", amount:99.5}
    - parseFrontmatter("no frontmatter") returns {} (no crash)
    - coerceValue("123") returns 123 (number)
    - coerceValue("true") returns true (boolean)
    - coerceValue('"hello"') returns "hello" (string, quotes stripped)
    - coerceValue("plain") returns "plain" (plain string)
  </behavior>
  <action>
    Use `node:fs/promises` (mkdtemp, writeFile, rm) to create temp directories
    for any test that requires filesystem access. Always clean up in afterEach or
    after() to avoid leaving artifacts. Use afterEach with try/finally or
    Vitest's onTestFinished.

    Use LSP `find_definition` on computeAnomalies, computePnl, computeTopVendors
    to confirm their exact signatures before writing tests.

    For tests/unit/insights/anomalies.test.ts:
    - Reconstruct bulletRegex inline in the test file as a const (it is not exported
      from anomalies.ts). Use the exact regex from the codebase:
      `/^- (\d{4}-\d{2}-\d{2}):\s+\[\[([^\]]+)\]\]\s+(.+?)$/`
    - For computeAnomalies tests: write a temp `concepts/march-anomaly-summary.md`
      with YAML frontmatter + body bullets. Structure: `---\ntype: concept\n---\n<bullets>`
    - Write one happy-path test with >= 3 valid bullets and one test with < 3 bullets
      that expects the call to throw.

    For tests/unit/insights/pnl.test.ts:
    - The monthly-close format: YAML frontmatter + body with lines like:
      `- Revenue: $12,500.00` and `- Expenses: $8,200.00`
    - computePnl expects files at `originals/monthly-close-2026-03.md` and
      `originals/monthly-close-2026-02.md` inside the given fixturesDir.
    - Create both files in a temp dir for the happy-path test.
    - For the "missing prev" test, create only the current month file.

    For tests/unit/insights/top-vendors.test.ts:
    - computeTopVendors reads all `originals/invoice-*.md` files.
    - Create 3 invoice markdown files with valid frontmatter:
      `---\ntype: invoice\nvendor: Beanstalk Roasters\ndate: 2026-03-01\namount: 500\n---`
    - Create one file without `type: invoice` to verify it is skipped.

    For tests/unit/insights/frontmatter.test.ts:
    - Check if coerceValue is exported from lib/insights/frontmatter.ts.
    - If it is NOT exported, add `export` before `function coerceValue` in
      lib/insights/frontmatter.ts before writing the test.
    - All parseFrontmatter + coerceValue tests are pure string-in/value-out — no I/O.

    All test files import from relative paths (e.g., "../../../lib/insights/anomalies.ts").
    Use explicit `import { describe, it, expect, afterEach } from "vitest"`.
  </action>
  <verify>
    <automated>bun run test tests/unit/insights/ --reporter=verbose 2>&1 | grep -E "passed|failed"</automated>
  </verify>
  <done>
    - All four insight test files exist with >= 3 tests each
    - computeAnomalies happy-path and throw-on-insufficient-rows tests pass
    - bulletRegex is validated against a known-good bullet string (matches) and known-bad strings (no match)
    - computePnl returns correct arithmetic from temp file fixtures
    - computeTopVendors returns sorted descending results from temp invoice fixtures
    - parseFrontmatter and coerceValue cover all coercion types (number, boolean, string)
    - `bun run test` exits 0 with all tests passing
    - No test leaves temp files in /tmp after completion
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Full test suite passes:
   ```
   bun run test --reporter=verbose
   ```
   Expected: all tests pass; no failures.

2. Count test cases (basic sanity):
   ```
   bun run test --reporter=verbose 2>&1 | grep -c "✓\|√\| pass"
   ```
   Expected: >= 25 tests passing across all files.

3. TypeCheck remains clean:
   ```
   bunx tsc --noEmit
   ```
   Expected: exits 0, no errors.

4. Confirm detect* functions are now exported:
   ```
   grep -c "^export function detect" lib/audit/anomaly-detector.ts
   ```
   Expected: 4 (detectPriceHikes, detectDuplicates, detectGhosts, detectMissingInvoices).

5. Confirm anomaly types covered:
   ```
   grep -c "price-hike\|duplicate\|ghost-saas\|missing-invoice" tests/unit/audit/anomaly-detector.test.ts
   ```
   Expected: >= 4.
</verification>

<success_criteria>
- All four anomaly types (price-hike, duplicate, ghost-saas, missing-invoice) are covered by named unit tests that pass (TEST-04 satisfied)
- bulletRegex output contract is verified by at least 3 test cases (matches valid, rejects missing companies/ prefix, rejects "Detection method" lines)
- lib/insights parsers have unit tests: computeAnomalies, computePnl, computeTopVendors, parseFrontmatter, coerceValue
- `bun run test` exits 0 on a clean codebase with no brains/ directory seeded
- No test requires gbrain CLI, OPENAI_API_KEY, ANTHROPIC_API_KEY, or any external service
</success_criteria>

<output>
Create `.planning/phases/01-test-harness-ci/01-02-SUMMARY.md` when done.
</output>
