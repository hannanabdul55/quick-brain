---
phase: 07-quickbooks-online-ingest
plan: 09
type: execute
wave: 5
depends_on: ["07-06", "07-07", "07-08"]
files_modified:
  - tests/integration/qbo/ingest-pipeline.test.ts
  - tests/unit/connectors/token-logging.test.ts
  - lib/sentry/connectors-strip.ts
autonomous: true
requirements: [QBO-02, QBO-03, QBO-04, QBO-05, QBO-06]
tags: [qbo, integration-test, token-logging, sentry, acceptance]
must_haves:
  truths:
    - "Token logging discipline test: grep of built output for 'access_token'/'refresh_token'/'qbo_tokens_encrypted' in console calls returns zero matches in lib/connectors/qbo/ and app/api/connections/quickbooks/"
    - "Transformer fixture round-trip test passes: billToMarkdown output parsed by top-vendors parser without throwing"
    - "Sentry beforeSend connector strip registered and strips token fields before any event reaches Sentry"
    - "Integration test validates the full pipeline end-to-end against fixtures (no live QBO calls)"
    - "D-13: token-logging unit test grep + Sentry beforeSend regex strip"
  artifacts:
    - path: "tests/integration/qbo/ingest-pipeline.test.ts"
      provides: "Fixture-based integration test for the full ingest pipeline (no live QBO API)"
    - path: "tests/unit/connectors/token-logging.test.ts"
      provides: "Structural assertion test: zero console.log/console.error calls referencing token fields in connector and route files"
    - path: "lib/sentry/connectors-strip.ts"
      provides: "Sentry beforeSend callback snippet that strips token fields; registered in instrumentation"
  key_links:
    - from: "tests/unit/connectors/token-logging.test.ts"
      to: "lib/connectors/qbo/"
      via: "readFileSync + grep pattern for access_token in console calls"
      pattern: "access_token.*console|console.*access_token"
    - from: "lib/sentry/connectors-strip.ts"
      to: "instrumentation.ts"
      via: "beforeSend hook registered in Sentry.init"
      pattern: "beforeSend"
---

<objective>
The acceptance validation layer: a fixture-based integration test for the full ingest pipeline, a structural token-logging enforcement test, and the Sentry beforeSend connector-token strip. This plan closes the SPEC acceptance criteria around token hygiene and verifies the end-to-end pipeline works against committed fixtures.

Purpose: These tests enforce the D-13 token logging discipline structurally (not by reviewer diligence) and give CI a signal that the transformer + writer pipeline produces valid brain-schema output without a live QBO API call.
Output: 2 new test files + lib/sentry/connectors-strip.ts + instrumentation.ts update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-quickbooks-online-ingest/07-SPEC.md
@.planning/phases/07-quickbooks-online-ingest/07-CONTEXT.md
@.planning/phases/07-quickbooks-online-ingest/07-RESEARCH.md

<interfaces>
<!-- From tests/unit/connectors/transformer.test.ts (Plan 03 output) -->
// The transformer test already verifies the fixture round-trip.
// This plan's integration test extends that with a full pipeline mock.

<!-- From lib/connectors/qbo/transformer.ts (Plan 03 output) -->
export function billToMarkdown(bill: QBOBill): { slug, path, content }
export function vendorToMarkdown(vendor: QBOVendor): { slug, path, content }
export function purchaseToMarkdown(purchase: QBOPurchase): { slug, path, content }

<!-- From lib/connectors/writer.ts (Plan 04 output) -->
export function writeConnectorDocs(brainSlug, docs): Promise<{ written, paths }>

<!-- From lib/insights/top-vendors.ts (existing) -->
// The top-vendors parser that transformer output must not break.
// Import and call on transformer output to verify round-trip.

<!-- From tests/fixtures/qbo/*.json (Plan 03 output) -->
// Committed fixture snapshots used in integration test (no live API calls)

<!-- From CONTEXT.md D-13 — structural token-logging enforcement -->
The token logging test must grep the SOURCE FILES (not built output) for:
  Pattern 1: console.log/console.error calls containing "access_token" or "refresh_token"
  Pattern 2: any usage of qbo_tokens_encrypted in log-producing code paths
  Zero matches required in: lib/connectors/qbo/*.ts and app/api/connections/quickbooks/**/*.ts

<!-- From lib/insights/anomalies.ts (existing, immutable) -->
// bulletRegex is the canonical parsing contract; transformer output must satisfy it.
// The integration test calls anomalies parser on transformer output.

<!-- From instrumentation.ts (Phase 4 Sentry — read before modifying) -->
// Phase 4 set up instrumentation.ts with Sentry.init.
// Add beforeSend callback using the lib/sentry/connectors-strip.ts helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token-logging enforcement test + Sentry connector-token strip</name>
  <files>
    tests/unit/connectors/token-logging.test.ts
    lib/sentry/connectors-strip.ts
  </files>
  <read_first>
    - tests/auth/cross-tenant-isolation.test.ts (Phase 6 structural source-code assertion pattern using readFileSync — follow this pattern)
    - .planning/phases/07-quickbooks-online-ingest/07-CONTEXT.md (D-13: three enforcement layers; exact grep pattern)
    - instrumentation.ts (read before next task modifies it — understand existing Sentry.init structure)
  </read_first>
  <behavior>
    token-logging.test.ts assertions:
    - For each file in lib/connectors/qbo/*.ts: readFileSync content must NOT match /console\.(log|error|warn).*access_token|console\.(log|error|warn).*refresh_token/
    - For each file in app/api/connections/quickbooks/**/*.ts (start, callback, resync, disconnect): same pattern
    - For each file: content must NOT match /qbo_tokens_encrypted.*console|console.*qbo_tokens_encrypted/
    - Test verifies zero matches — uses structural source assertions (same pattern as cross-tenant-isolation.test.ts)
    - Test must PASS (not RED) — it enforces an existing invariant, not a future one

    connectors-strip.ts:
    - Export function stripConnectorTokens(event: SentryEvent): SentryEvent | null
    - Strip: any string value on event.extra, event.contexts, or event.exception that matches /access_token|refresh_token|qbo_tokens_encrypted/ — replace with "[REDACTED]"
    - Return modified event (never null — Sentry should still capture the event, just with token values stripped)
    - Import stripTokenFields from lib/connectors/redact.ts for the actual stripping logic
  </behavior>
  <action>
    Create tests/unit/connectors/token-logging.test.ts using Vitest and the node:fs readFileSync pattern from cross-tenant-isolation.test.ts. Define the list of files to check: all .ts files under lib/connectors/qbo/ (use readdirSync recursively) and all route.ts files under app/api/connections/quickbooks/ (start, callback, resync, disconnect). For each file, read it as text. Assert that no line matches the forbidden console+token patterns. Structure as one describe block per directory; each file is a test case. Use descriptive test names like "lib/connectors/qbo/client.ts must not log token fields". The test suite should run without live network calls or env vars.

    Create lib/sentry/connectors-strip.ts exporting stripConnectorTokens. The function accepts a Sentry event object (typed using @sentry/nextjs types if available, otherwise typed as `any` to avoid import complexity). It walks the event's string values and strips any that contain "access_token", "refresh_token", or "qbo_tokens_encrypted" as substrings, replacing with "[REDACTED]". Import stripTokenFields from lib/connectors/redact.ts for the field-level stripping. The function returns the modified event (not null — dropping events silently is bad for observability; stripping is the correct pattern).
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun test tests/unit/connectors/token-logging.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/connectors/token-logging.test.ts exists and PASSES (all assertions green)
    - The test checks at minimum: lib/connectors/qbo/oauth.ts, lib/connectors/qbo/client.ts, app/api/connections/quickbooks/callback/route.ts
    - Assertions fail when a console.log(access_token) call is added to any of those files (verify this is true by understanding the regex)
    - lib/sentry/connectors-strip.ts exports stripConnectorTokens
    - stripConnectorTokens imports from lib/connectors/redact.ts (grep confirms)
    - `bun x tsc --noEmit --skipLibCheck` exits 0
  </acceptance_criteria>
  <done>Token-logging enforcement tests pass; Sentry strip helper created; token hygiene is enforced structurally by CI.</done>
</task>

<task type="auto">
  <name>Task 2: Fixture-based ingest pipeline integration test + Sentry beforeSend registration</name>
  <files>
    tests/integration/qbo/ingest-pipeline.test.ts
    instrumentation.ts
  </files>
  <read_first>
    - tests/fixtures/qbo/vendor.json (Plan 03: fixture data)
    - tests/fixtures/qbo/bill.json (Plan 03: fixture data)
    - tests/fixtures/qbo/purchase.json (Plan 03: fixture data)
    - lib/connectors/qbo/transformer.ts (Plan 03: functions under test)
    - lib/insights/top-vendors.ts (existing: the parser called in the round-trip test)
    - instrumentation.ts (MUST read before modifying — add beforeSend import without breaking existing Sentry.init)
    - .planning/phases/07-quickbooks-online-ingest/07-SPEC.md (Acceptance criteria: "round-trips through lib/insights/anomalies.ts and lib/insights/top-vendors.ts without throwing")
  </read_first>
  <action>
    Create tests/integration/qbo/ingest-pipeline.test.ts. This is a fixture-only integration test (no live API calls, no Postgres, no Inngest). The test:

    1. Imports the transformer functions (vendorToMarkdown, billToMarkdown, purchaseToMarkdown) from lib/connectors/qbo/transformer.
    2. Loads the three fixture JSON files using JSON.parse(readFileSync(...)).
    3. Applies the transformer to each entity.
    4. Verifies that billToMarkdown output satisfies brain-schema.md field contract: frontmatter contains "type: invoice", "vendor_slug: qbo-", "date:", "amount:", "currency:", wikilink body contains "[[companies/qbo-".
    5. Verifies that vendorToMarkdown output contains "type: vendor", "slug: qbo-", and vendor_email if present in fixture.
    6. Verifies that the fixture bill markdown does NOT throw when its content is passed to the top-vendors parser (import parseTopVendors or equivalent from lib/insights/top-vendors.ts — check the actual exported function name before using it).
    7. Verifies that purchaseToMarkdown on the fixture entry missing VendorRef produces vendor_slug: "qbo-unknown".

    The test does NOT test the Inngest function itself (that requires a running Inngest server). It tests the transformer + insights parser contract — the key correctness invariant that proves QBO ingest data will be readable by the existing insights infrastructure.

    Modify instrumentation.ts: after the existing Sentry.init call (read the file first to find the exact location), add beforeSend: stripConnectorTokens to the Sentry.init options object. Import stripConnectorTokens from "@/lib/sentry/connectors-strip". If beforeSend is already configured (Phase 4 may have added it), wrap the existing callback: `beforeSend: (event, hint) => { const stripped = stripConnectorTokens(event); return existingBeforeSend ? existingBeforeSend(stripped, hint) : stripped; }`. Do not remove or disable any existing Phase 4 Sentry configuration.
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun test tests/integration/qbo/ingest-pipeline.test.ts 2>&1 | tail -15 && bun x tsc --noEmit --skipLibCheck 2>&1 | grep "instrumentation\|sentry/connectors" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/integration/qbo/ingest-pipeline.test.ts exists and passes (all assertions green)
    - Test imports billToMarkdown, vendorToMarkdown, purchaseToMarkdown (no live API/DB/Inngest calls)
    - Test verifies brain-schema field mapping: "type: invoice", "vendor_slug: qbo-", wikilink present in bill output
    - Test verifies top-vendors parser does not throw on bill markdown output (round-trip assertion per SPEC)
    - Test verifies "qbo-unknown" fallback for purchase with missing VendorRef
    - instrumentation.ts: grep "stripConnectorTokens" confirms import and beforeSend registration
    - `bun x tsc --noEmit --skipLibCheck` exits 0
    - `bun test tests/` (full suite) exits 0 (no regressions to existing tests)
  </acceptance_criteria>
  <done>Integration test passes against fixtures; Sentry beforeSend registers token strip; full test suite green; type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sentry event pipeline → instrumentation.ts | Sentry events may contain error.extra fields with token values from caught exceptions — beforeSend strips them before upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-32 | Information Disclosure | Sentry event → cloud | mitigate | stripConnectorTokens registered in beforeSend; strips access_token/refresh_token/qbo_tokens_encrypted from event before transmission to Sentry cloud |
| T-07-33 | Information Disclosure | Token logging enforcement | mitigate | Structural test (readFileSync + regex) runs in CI; fails if any console.log(token) is added in the connector or route files; cannot be bypassed by reviewer oversight |
| T-07-34 | Tampering | Integration test (no live calls) | accept | Fixture-based test; deterministic; no network or DB — cannot be confused by sandbox data changes |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed |
</threat_model>

<verification>
1. `bun test tests/unit/connectors/token-logging.test.ts` exits 0
2. `bun test tests/integration/qbo/ingest-pipeline.test.ts` exits 0
3. `grep "stripConnectorTokens" instrumentation.ts` confirms registration
4. `grep "stripConnectorTokens" lib/sentry/connectors-strip.ts` confirms export
5. `bun test tests/` exits 0 (full suite — no regressions)
6. `bun x tsc --noEmit --skipLibCheck` exits 0
</verification>

<success_criteria>
- Token-logging enforcement test passes in CI without env vars
- Fixture-based integration test proves transformer + insights parser round-trip
- Sentry beforeSend strips connector token fields before any event reaches Sentry cloud
- Full test suite passes with no regressions to Phase 1-6 tests
- Zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/07-quickbooks-online-ingest/07-09-SUMMARY.md` when done
</output>
