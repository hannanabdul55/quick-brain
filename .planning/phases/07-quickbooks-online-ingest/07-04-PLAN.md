---
phase: 07-quickbooks-online-ingest
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - lib/connectors/writer.ts
  - types/gbrain.ts
autonomous: true
requirements: [QBO-03, QBO-06]
tags: [qbo, gbrain-shim, writer, import-from-content]
must_haves:
  truths:
    - "writeConnectorDocs writes markdown files to the correct brainHome subdirectories"
    - "importFromContent is exported from types/gbrain.ts and callable in-process"
    - "Both originals/ and companies/ paths are resolved through brainHome(slug)"
    - "D-06: connector-blind writer.ts in lib/connectors/"
    - "D-13: writer must not echo token-bearing values into markdown"
  artifacts:
    - path: "lib/connectors/writer.ts"
      provides: "writeConnectorDocs — connector-blind markdown file writer for originals/ and companies/"
    - path: "types/gbrain.ts"
      provides: "importFromContent added to the existing gbrain shim"
  key_links:
    - from: "lib/connectors/writer.ts"
      to: "lib/gbrain/paths.ts"
      via: "brainHome(slug) resolves brain directory; never hardcoded path"
      pattern: "brainHome"
    - from: "types/gbrain.ts"
      to: "node_modules/gbrain/src/core/import-file.ts"
      via: "dynamic _load('import-file') pattern matching existing shim exports"
      pattern: "importFromContent"
---

<objective>
Two leaf files that the Inngest ingest job (Plan 05) imports: the connector-blind markdown writer that physically writes files to the tenant's brain directory, and the gbrain shim extension that exposes importFromContent for in-process document indexing.

Purpose: The Inngest job's "writing" step calls writeConnectorDocs; the "indexing" step calls importFromContent. Both must exist before Plan 05 can be written. These files have no dependency on Plans 02 or 03 (only on Plan 01 types and Plan 01 brainHome path utility).
Output: lib/connectors/writer.ts, updated types/gbrain.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-quickbooks-online-ingest/07-SPEC.md
@.planning/phases/07-quickbooks-online-ingest/07-CONTEXT.md
@.planning/phases/07-quickbooks-online-ingest/07-RESEARCH.md

<interfaces>
<!-- From lib/gbrain/paths.ts (existing) -->
export function brainHome(tenantId: string): string;
// Returns resolve(BRAINS_ROOT, tenantId)
// Caller appends: join(brainHome(slug), "brain-repo", "originals") etc.

<!-- From lib/connectors/qbo/transformer.ts (Plan 03 output) -->
export type { ConnectorDocument }
// { slug: string; path: string; content: string }
// path is relative: "originals/qbo-bill-25.md" or "companies/qbo-books-by-bessie.md"

<!-- From lib/gbrain/engine.ts (existing) -->
export async function createGBrainEngine(): Promise<BrainEngine>
export type { BrainEngine }
// The shared engine pool is already initialized at module level
// importFromContent must receive the engine from this pool

<!-- From RESEARCH.md Domain 5 — importFromContent API -->
// node_modules/gbrain/src/core/import-file.ts (verified)
importFromContent(engine, slug, content, opts?):
  opts.sourceId: string  -- REQUIRED for tenant isolation (RESEARCH Pitfall 3)
  opts.forceRechunk?: boolean  -- use true on resync
  opts.filename?: string
Returns: ImportResult { slug, status, chunks, error? }

<!-- From types/gbrain.ts (existing — read actual file before modifying) -->
// The existing shim uses _load(moduleName) pattern for dynamic gbrain imports
// Existing exports: createEngine, hybridSearch, expandQuery, configureGateway, runThink
// New export needed: importFromContent — follow the same _load() pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connector-blind markdown file writer</name>
  <files>lib/connectors/writer.ts</files>
  <read_first>
    - lib/gbrain/paths.ts (brainHome function — must use this; never hardcode path)
    - .planning/phases/07-quickbooks-online-ingest/07-CONTEXT.md (D-06: writer is connector-blind; D-08: re-sync wipe deletes qbo-*.md before re-write)
    - .planning/phases/07-quickbooks-online-ingest/07-RESEARCH.md (Architecture diagram showing originals/ and companies/ subpaths)
  </read_first>
  <action>
    Create lib/connectors/writer.ts. Import node:fs/promises (mkdir, writeFile, rm, readdir) and brainHome from lib/gbrain/paths.ts.

    Export writeConnectorDocs(brainSlug: string, docs: Array<{ path: string; content: string }>): Promise<{ written: number; paths: string[] }> — iterates the docs array, resolves each path relative to join(brainHome(brainSlug), "brain-repo") (e.g. "originals/qbo-bill-25.md" resolves to join(brainHome(slug), "brain-repo", "originals", "qbo-bill-25.md")), mkdir({ recursive: true }) for parent directory, writeFile with utf-8 encoding. Returns { written: docs.length, paths: absolute paths of files written }.

    Export wipeQBODocs(brainSlug: string): Promise<{ wiped: number }> — globs originals/qbo-*.md and companies/qbo-*.md under join(brainHome(brainSlug), "brain-repo") using readdir with recursive:false on each subdir, filters filenames starting with "qbo-" and ending with ".md", calls rm on each matched path. Returns { wiped: count }. Throws if brainSlug resolves to a path outside the BRAINS_ROOT (security guard: verify the resolved path starts with BRAINS_ROOT). This function is called by the resync route before re-enqueuing the ingest job (D-08).

    The writer has no QBO-specific knowledge. It receives connector-agnostic { path, content } pairs and writes them. This is what makes a Xero connector trivial to add: it just calls writeConnectorDocs with different paths and content strings.
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun x tsc --noEmit --skipLibCheck 2>&1 | grep "connectors/writer" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/connectors/writer.ts exports writeConnectorDocs and wipeQBODocs
    - writeConnectorDocs resolves all paths through brainHome(brainSlug) — grep "brainHome" confirms it is called
    - wipeQBODocs has a path-traversal guard: resolved path must start with BRAINS_ROOT (grep confirms the check)
    - No QBO-specific imports (no import from lib/connectors/qbo/) — connector-blind as per D-06
    - Zero TypeScript errors
  </acceptance_criteria>
  <done>Connector-blind writer with writeConnectorDocs and wipeQBODocs implemented; path-traversal guard present; type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend gbrain shim with importFromContent</name>
  <files>types/gbrain.ts</files>
  <read_first>
    - types/gbrain.ts (MUST read before modifying — understand _load() pattern, existing exports, BrainEngine type)
    - .planning/phases/07-quickbooks-online-ingest/07-RESEARCH.md (Domain 5: importFromContent API signature, opts shape, ImportResult type, and the exact module path "import-file")
    - node_modules/gbrain/src/core/import-file.ts (if accessible via Bash cat — read the actual export to confirm function signature before writing the shim)
  </read_first>
  <action>
    Read the existing types/gbrain.ts file completely before making any changes. Understand the _load(moduleName) pattern used for all existing exports (createEngine, hybridSearch, etc.). Then add importFromContent following the identical pattern.

    Add to types/gbrain.ts:
    - Export interface ImportResult { slug: string; status: "imported" | "skipped" | "error"; chunks: number; error?: string }
    - Export interface ImportFromContentOptions { noEmbed?: boolean; sourceId?: string; filename?: string; sourcePath?: string; forceRechunk?: boolean }
    - Export async function importFromContent(engine: BrainEngine, slug: string, content: string, opts?: ImportFromContentOptions): Promise<ImportResult> — implementation uses _load("import-file") and calls m.importFromContent(engine, slug, content, opts) following the exact pattern of existing exports like hybridSearch.

    Do NOT modify any existing exports. This is an additive change only. The BrainEngine type is already exported by the shim — use it.

    After adding the export, verify by running bun x tsc --noEmit --skipLibCheck and confirming zero errors. Also verify that the existing exports (createEngine, hybridSearch, runThink) are still callable (no regressions from the additive change).
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun x tsc --noEmit --skipLibCheck 2>&1 | grep "gbrain" | head -10 && grep "importFromContent" types/gbrain.ts | head -5</automated>
  </verify>
  <acceptance_criteria>
    - types/gbrain.ts exports importFromContent, ImportResult, ImportFromContentOptions
    - importFromContent has the correct signature: (engine: BrainEngine, slug: string, content: string, opts?: ImportFromContentOptions) => Promise<ImportResult>
    - ImportFromContentOptions includes sourceId?: string (required for tenant isolation per RESEARCH Pitfall 3)
    - The implementation uses the _load("import-file") pattern (grep "import-file" confirms)
    - Existing exports (createEngine, hybridSearch, runThink) are unchanged (grep confirms they still exist)
    - `bun x tsc --noEmit --skipLibCheck` exits 0
  </acceptance_criteria>
  <done>importFromContent added to the gbrain shim; ImportResult and ImportFromContentOptions exported; existing shim exports unmodified; type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inngest job → lib/connectors/writer.ts | brainSlug comes from session-resolved context (not user input at this point); still guards against path traversal |
| gbrain importFromContent → sourceId | sourceId must be session-derived brain_id to maintain tenant isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Elevation of Privilege | lib/connectors/writer.ts path resolution | mitigate | wipeQBODocs verifies resolved path starts with BRAINS_ROOT before any rm call — prevents path traversal if brainSlug contains ".." or absolute path segments |
| T-07-14 | Elevation of Privilege | importFromContent sourceId | mitigate | The Inngest job (Plan 05) passes sourceId=user.brain_id derived from session context only — documented as REQUIRED parameter; the shim's ImportFromContentOptions makes it visibly optional but the job enforces it (RESEARCH Pitfall 3) |
| T-07-15 | Tampering | types/gbrain.ts shim | mitigate | Additive change only; existing exports unmodified; _load("import-file") matches the gbrain module that Phase 3 pinned at a specific commit SHA |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed |
</threat_model>

<verification>
1. `grep "importFromContent" types/gbrain.ts` returns at least 2 lines (interface + function)
2. `grep "import-file" types/gbrain.ts` confirms _load("import-file") pattern
3. `grep "brainHome" lib/connectors/writer.ts` confirms path resolution goes through brainHome
4. `grep "BRAINS_ROOT" lib/connectors/writer.ts` confirms the path-traversal guard
5. `bun x tsc --noEmit --skipLibCheck` exits 0
</verification>

<success_criteria>
- Connector-blind writer with writeConnectorDocs and wipeQBODocs present with path guard
- importFromContent added to gbrain shim following existing _load() pattern
- All existing gbrain shim exports unchanged
- Zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/07-quickbooks-online-ingest/07-04-SUMMARY.md` when done
</output>
