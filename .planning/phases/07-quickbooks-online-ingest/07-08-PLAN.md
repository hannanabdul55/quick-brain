---
phase: 07-quickbooks-online-ingest
plan: 08
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - app/api/tenants/[id]/insights/route.ts
  - app/api/tenants/[id]/chat/route.ts
autonomous: true
requirements: [QBO-05, QBO-06]
tags: [qbo, connector-revoked, insights, chat, 409]
must_haves:
  truths:
    - "GET /api/tenants/<slug>/insights returns 409 connector_revoked when qbo_connection_status='revoked' AND qbo_last_synced_at IS NOT NULL"
    - "POST /api/tenants/<slug>/chat returns 409 connector_revoked under the same conditions"
    - "Fresh tenants (never synced) with revoked status still serve empty insights (200 + empty bundle) — not 409"
    - "Non-revoked authenticated tenants are unaffected by this change"
  artifacts:
    - path: "app/api/tenants/[id]/insights/route.ts"
      provides: "Early 409 connector_revoked gate when connection revoked AND data exists"
    - path: "app/api/tenants/[id]/chat/route.ts"
      provides: "Early 409 connector_revoked gate when connection revoked AND data exists"
  key_links:
    - from: "app/api/tenants/[id]/insights/route.ts"
      to: "app.users.qbo_connection_status"
      via: "SELECT qbo_connection_status, qbo_last_synced_at from app.users WHERE id=userId — after resolveTenant()"
      pattern: "connector_revoked"
    - from: "app/api/tenants/[id]/chat/route.ts"
      to: "app.users.qbo_connection_status"
      via: "Same SELECT pattern"
      pattern: "connector_revoked"
---

<objective>
Add the typed 409 connector_revoked early-exit to the insights and chat routes. This satisfies SPEC requirement #8 (QBO-05): when a connected tenant's QBO credentials are revoked, their insight and chat routes must return a typed error — not 500, not an empty bundle — so the frontend can surface the reconnect banner correctly.

Purpose: These two API routes are the tenant's primary data access paths. After QBO ingest, they contain real QBO data. When credentials are revoked, a 409 with a typed error body signals to the frontend to surface the reconnect UX.
Output: Two modified route files with early 409 gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-quickbooks-online-ingest/07-SPEC.md
@.planning/phases/07-quickbooks-online-ingest/07-CONTEXT.md

<interfaces>
<!-- From lib/auth/resolve-tenant.ts (existing) -->
export async function resolveTenant(): Promise<TenantContext>
// Returns { authenticated: true; userId, sourceId, brainSlug } | { authenticated: false }
// Already called at the top of both routes (Phase 6 work)

<!-- From 06-05-SUMMARY.md — existing route structure -->
app/api/tenants/[id]/insights/route.ts:
  - Lines 1-30: imports + runtime/dynamic exports
  - AUTH_ENABLED=0 seed bypass (lines ~53-72): serves FIXTURES_ROOT — do NOT touch
  - Authenticated branch (lines ~80+): resolveTenant() → stat guard (06-06) → computeAndCache → catch

app/api/tenants/[id]/chat/route.ts:
  - resolveTenant() at top → 401 if unauthenticated → 403 on slug mismatch
  - Then calls think(brainSlug, question, { sourceId })

<!-- The 409 gate logic -->
The gate must fire ONLY when ALL of:
  1. The tenant is authenticated (resolveTenant returned authenticated:true)
  2. qbo_connection_status = 'revoked'
  3. qbo_last_synced_at IS NOT NULL (the tenant has synced data — they depend on QBO)

Fresh tenants (qbo_last_synced_at IS NULL) with revoked status must still get 200 + empty bundle
from insights (06-06 stat guard handles this) and empty chat responses — NOT 409.
Rationale: a tenant who never synced QBO data does not depend on QBO; a 409 would be wrong.

SELECT qbo_connection_status, qbo_last_synced_at FROM app.users WHERE id = $userId
→ if status='revoked' AND last_synced_at IS NOT NULL → 409 { error: "connector_revoked" }
→ else → proceed normally

<!-- CONTEXT.md D-09 exact 409 shape -->
return NextResponse.json({ error: "connector_revoked" }, { status: 409 })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 409 connector_revoked gate to insights and chat routes</name>
  <files>
    app/api/tenants/[id]/insights/route.ts
    app/api/tenants/[id]/chat/route.ts
  </files>
  <read_first>
    - app/api/tenants/[id]/insights/route.ts (MUST read fully before modifying — understand the AUTH_ENABLED=0 bypass, the stat guard from 06-06, and the authenticated branch structure; the gate goes AFTER resolveTenant + BEFORE computeAndCache)
    - app/api/tenants/[id]/chat/route.ts (MUST read fully before modifying — understand resolveTenant + think call; gate goes after resolveTenant, before think)
    - .planning/phases/07-quickbooks-online-ingest/07-CONTEXT.md (D-09: exact 409 shape; the "connected BUT never synced" exclusion logic)
  </read_first>
  <behavior>
    Insights route 409 gate (inserted after resolveTenant + before stat guard):
    - SELECT qbo_connection_status, qbo_last_synced_at FROM app.users WHERE id=userId (use existing sql singleton in the route file)
    - if status='revoked' AND last_synced_at IS NOT NULL → return NextResponse.json({ error: "connector_revoked" }, { status: 409 })
    - else → fall through to existing stat guard → computeAndCache → etc.

    Chat route 409 gate (inserted after resolveTenant + before think call):
    - Same SELECT pattern
    - Same condition: status='revoked' AND last_synced_at IS NOT NULL → 409
    - else → proceed with think()

    Fresh tenants (last_synced_at IS NULL, any connection status) must NOT get 409 — they fall through to the existing empty-bundle path (insights) or get an empty chat response.
  </behavior>
  <action>
    Modify app/api/tenants/[id]/insights/route.ts: after the existing resolveTenant() call (in the authenticated branch only, not in the AUTH_ENABLED=0 bypass branch), add the connector-revoked check using a SELECT on the existing module-level sql singleton. The check is a single async SQL call returning one row. The 409 return must be before the stat check (06-06) so revoked+synced tenants see the typed error, not an empty bundle. Insert the check precisely in the authenticated branch, not in the bypass branch — test that `bun test tests/unit/insights/route-empty-bundle.test.ts` still passes after the change.

    Modify app/api/tenants/[id]/chat/route.ts: after resolveTenant() and the slug-mismatch 403 check, add the same SELECT + 409 gate before the think() call. The chat route does NOT have the AUTH_ENABLED bypass, so the gate always applies in the authenticated path.

    In both files: the SELECT must use the existing module-level sql import (not create a new postgres instance). The SQL is a single tagged-template query — no string concatenation, no prepared statements (prepare:false is inherited from the module-level singleton).
  </action>
  <verify>
    <automated>cd "/Users/abdulhannankanji/Git repos/quick-brain" && bun test tests/unit/insights/ 2>&1 | tail -10 && bun x tsc --noEmit --skipLibCheck 2>&1 | grep "tenants.*insights\|tenants.*chat" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - app/api/tenants/[id]/insights/route.ts: grep "connector_revoked" returns at least 1 match in the authenticated branch
    - app/api/tenants/[id]/chat/route.ts: grep "connector_revoked" returns at least 1 match
    - Both routes: the 409 gate fires ONLY when status='revoked' AND last_synced_at IS NOT NULL (grep shows both conditions in the SQL or application code)
    - Existing insight tests still pass (bun test tests/unit/insights/ exits 0)
    - AUTH_ENABLED=0 bypass in insights route is unchanged (grep "AUTH_ENABLED" shows same bypass block)
    - Zero TypeScript errors
  </acceptance_criteria>
  <done>Both routes return 409 { error: "connector_revoked" } when connection is revoked and data exists; fresh tenants unaffected; existing tests pass; type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → insights/chat routes | qbo_connection_status must be read from session-resolved userId, never from URL params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-29 | Information Disclosure | 409 connector_revoked response | mitigate | Response body is only { error: "connector_revoked" } — no user data, no token values, no stack traces. Status 409 not 500 — distinguishes "connector problem" from "server error" |
| T-07-30 | Elevation of Privilege | Connector revoked gate in insights route | accept | Gate reads from app.users using session-derived userId (resolveTenant() chokepoint already enforced before this point — D-11 maintained) |
| T-07-31 | Information Disclosure | AUTH_ENABLED=0 bypass preserved | accept | The bypass is for seed/dev use only (Phase 9 removes it per CLEAN-03); preserving it avoids breaking the dev workflow before Phase 9 |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed |
</threat_model>

<verification>
1. `grep "connector_revoked" app/api/tenants/[id]/insights/route.ts` returns ≥1 match in authenticated branch
2. `grep "connector_revoked" app/api/tenants/[id]/chat/route.ts` returns ≥1 match
3. `grep "last_synced_at" app/api/tenants/[id]/insights/route.ts` shows the "never synced" exclusion condition
4. `bun test tests/unit/insights/` exits 0 (existing tests unbroken)
5. `bun x tsc --noEmit --skipLibCheck` exits 0
</verification>

<success_criteria>
- insights and chat routes return 409 { error: "connector_revoked" } when revoked + previously synced
- Fresh (never synced) tenants get 200 + empty bundle from insights, not 409
- Existing Phase 6 insight tests pass
- AUTH_ENABLED=0 bypass unchanged
- Zero TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/07-quickbooks-online-ingest/07-08-SUMMARY.md` when done
</output>
