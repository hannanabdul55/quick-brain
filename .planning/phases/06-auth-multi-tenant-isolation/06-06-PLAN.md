---
phase: 06-auth-multi-tenant-isolation
plan: 06
type: execute
wave: 5
gap_closure: true
closes_gaps:
  - uat_test: 11
    truth: "Insights cards render empty (or 200 with empty data) for a fresh authenticated tenant with no ingested data — D-02 accepted no-data case"
    severity: major
    debug_session: ".planning/debug/06-insights-enoent-fresh-tenant.md"
depends_on: ["06-05"]
files_modified:
  - app/api/tenants/[id]/insights/route.ts
  - lib/insights/types.ts
  - components/insights/pnl-card.tsx
  - tests/unit/insights/route-empty-bundle.test.ts
  - tests/unit/insights/pnl-card-null.test.tsx
autonomous: true
requirements: [AUTH-05]
tags: [auth, multi-tenant, insights, gap-closure, D-02]

must_haves:
  truths:
    - "GET /api/tenants/<slug>/insights returns 200 with an empty bundle (topVendors=[], pnl=null, anomalies=[]) for an authenticated tenant whose <brainHome(slug)>/brain-repo/originals directory does not exist"
    - "GET /api/tenants/<slug>/insights still returns 500 compute_failed when originals exists but contains corrupt/parse-error fixtures (defensive — only the ENOENT case is rewritten to 200/empty)"
    - "AUTH_ENABLED=0 + id=SEED_TENANT_ID seed branch continues to call computeAndCache(SEED_TENANT_ID, FIXTURES_ROOT) and serves Mara's coffee insights unchanged (no regression in demo path)"
    - "InsightCardsRow on the dashboard for a fresh authenticated tenant renders three cards with empty/no-data states — no 'Could not load insights' error and no client console error 500"
    - "PnlCard renders a 'No P&L data yet' empty state when pnl is null instead of crashing on snapshot.revenue access"
  artifacts:
    - path: "app/api/tenants/[id]/insights/route.ts"
      provides: "no-data guard before computeAndCache in the authenticated branch"
      contains: "stat"
    - path: "lib/insights/types.ts"
      provides: "InsightBundle.pnl type widened to PnlSnapshot | null"
      contains: "pnl: PnlSnapshot | null"
    - path: "components/insights/pnl-card.tsx"
      provides: "PnlCard accepts and renders snapshot: PnlSnapshot | null"
      contains: "snapshot: PnlSnapshot | null"
    - path: "tests/unit/insights/route-empty-bundle.test.ts"
      provides: "RED→GREEN test asserting the no-data guard returns 200 + empty bundle"
      contains: "describe"
    - path: "tests/unit/insights/pnl-card-null.test.tsx"
      provides: "PnlCard renders without throwing when snapshot is null"
      contains: "snapshot: null"
  key_links:
    - from: "app/api/tenants/[id]/insights/route.ts"
      to: "node:fs/promises::stat"
      via: "directory-existence check before computeAndCache"
      pattern: "stat\\(join\\(sourceDir, .originals.\\)\\)"
    - from: "components/insights/insight-cards-row.tsx"
      to: "components/insights/pnl-card.tsx"
      via: "bundle.pnl prop (now nullable)"
      pattern: "snapshot: rowState\\.data\\.pnl"
---

<objective>
Close UAT test 11 gap: fresh authenticated tenants must see empty insights (200), not a 500 `compute_failed` ENOENT.

Purpose: restore the D-02 invariant ("real tenants get empty insights until Phase 7 QBO ingest"). Currently `app/api/tenants/[id]/insights/route.ts:97` calls `computeAndCache(brainSlug, sourceDir)` which fans out to `lib/insights/top-vendors.ts:17` `readdir(originalsDir)` — unguarded ENOENT propagates through `Promise.all` and surfaces as 500 instead of the empty bundle the dashboard expects.

Output:
  - A single 10-line `fs.stat` guard in the authenticated branch of the insights route.
  - Type widening: `InsightBundle.pnl: PnlSnapshot | null` so the empty bundle can carry `pnl: null` without violating the contract.
  - `PnlCard` updated to render a "no P&L data yet" empty state when `snapshot` is null (instead of crashing on `snapshot.revenue`).
  - RED→GREEN unit tests for the route's no-data branch and the PnlCard null branch.
  - Seed/AUTH_ENABLED=0 path untouched. `computeAndCache` and the three compute functions unchanged (per debug-session recommendation "Ship Fix 1a only").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/06-auth-multi-tenant-isolation/06-UAT.md
@.planning/debug/06-insights-enoent-fresh-tenant.md
@.planning/phases/06-auth-multi-tenant-isolation/06-05-SUMMARY.md

# Files this plan modifies — read before editing
@app/api/tenants/[id]/insights/route.ts
@lib/insights/types.ts
@lib/insights/cache.ts
@lib/insights/top-vendors.ts
@components/insights/insight-cards-row.tsx
@components/insights/pnl-card.tsx
@components/insights/insight-card.tsx
@lib/gbrain/paths.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

From lib/insights/types.ts (current, before this plan):
```typescript
export type PnlSnapshot = {
  month: string;        // "2026-03"
  revenue: number;
  cogs: number;
  opex: number;
  net: number;
  prevMonth?: { month: string; revenue: number; cogs: number; opex: number; net: number };
};

export type InsightBundle = {
  topVendors: TopVendorRow[];
  pnl: PnlSnapshot;      // ← this plan widens to: PnlSnapshot | null
  anomalies: AnomalyRow[];
  computedAt: number;
};
```

From lib/insights/cache.ts:
```typescript
export function getCachedInsights(tenantId: string): InsightBundle | undefined;
export function computeAndCache(tenantId: string, fixturesDir: string): Promise<InsightBundle>;
```

From lib/gbrain/paths.ts:
```typescript
export const FIXTURES_ROOT: string;     // resolve(REPO_ROOT, "data", "maras-coffee")
export const SEED_TENANT_ID: "seed";
export function brainHome(tenantId: string): string;  // resolve(BRAINS_ROOT, tenantId)
```

From components/insights/pnl-card.tsx (current props):
```typescript
export interface PnlCardProps {
  state:
    | { kind: "loading" }
    | { kind: "data"; snapshot: PnlSnapshot }            // ← widen to PnlSnapshot | null
    | { kind: "error"; message: string; onRetry: () => void };
}
```

From components/insights/insight-card.tsx (re-use the existing kind:"data" path with a custom node):
```typescript
export type InsightCardState =
  | { kind: "loading" }
  | { kind: "data"; node: React.ReactNode }
  | { kind: "error"; message: string; onRetry: () => void };
```

Authenticated branch of insights route (current behavior — verbatim from app/api/tenants/[id]/insights/route.ts:86-110):
```typescript
const sourceDir = join(brainHome(brainSlug), "brain-repo");

let bundle = getCachedInsights(brainSlug);
if (!bundle) {
  try {
    bundle = await computeAndCache(brainSlug, sourceDir);   // ← throws ENOENT for fresh tenants
  } catch (err: unknown) {
    console.error("[insights] compute failed for", brainSlug, err);
    return Response.json(
      { error: "compute_failed", message: err instanceof Error ? err.message : String(err) },
      { status: 500 },
    );
  }
}

return Response.json(bundle, { status: 200 });
```

This is the only block that changes. The AUTH_ENABLED=0 seed branch (lines 53-72) is NOT modified.
</interfaces>

<scope_boundary>
DO NOT:
- Modify `lib/insights/cache.ts` (debugger's Fix 1b is rejected — "Ship Fix 1a only").
- Modify `lib/insights/top-vendors.ts`, `lib/insights/pnl.ts`, or `lib/insights/anomalies.ts`. Fix 2 (per-function ENOENT guards) is explicitly out of scope per the gap_summary scope constraints.
- Touch the AUTH_ENABLED=0 seed branch in the insights route (lines 53-72).
- Redesign the insights compute pipeline or add new compute functions.
- Rewrite the existing top-vendors / pnl / anomalies tests.
- Touch other tenant routes (chat / onboard / reset) — they have their own gap status, this is not their plan.

ONLY:
- Add a `stat`-based directory-existence guard in the authenticated branch of `app/api/tenants/[id]/insights/route.ts`.
- Widen the `InsightBundle.pnl` type to `PnlSnapshot | null`.
- Update `PnlCard` to render a "no P&L data yet" empty state for `snapshot: null`.
- Add two small unit tests (one for the route, one for the PnlCard null branch).
</scope_boundary>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing route + PnlCard tests for the no-data path</name>
  <files>tests/unit/insights/route-empty-bundle.test.ts, tests/unit/insights/pnl-card-null.test.tsx</files>
  <behavior>
    Two RED tests that fail against the current code and pass once Task 2 lands.

    `tests/unit/insights/route-empty-bundle.test.ts` — tests the route handler in isolation by mocking auth + filesystem:
      - Test A: GET on an authenticated tenant whose `<brainHome(slug)>/brain-repo/originals` directory does NOT exist returns 200 with body `{ topVendors: [], pnl: null, anomalies: [], computedAt: <number> }`.
      - Test B: GET on an authenticated tenant whose `<brainHome(slug)>/brain-repo/originals` directory DOES exist but is empty (no invoice files) returns 200 (no 500) — the existing compute pipeline returns an empty top-vendors list naturally; this test guards against accidentally widening the no-data guard. Acceptable to pass via Test A's branch if `originals` happens to exist as an empty dir AND the other compute functions also tolerate it; if they ENOENT-throw (they will, on `monthly-close-2026-03.md`), this assertion may need to also create the dir as missing OR be reframed to "directory missing → 200". Pick whichever framing matches the implemented guard.
      - Test C: Seed-bypass branch (`AUTH_ENABLED=0`, id=SEED_TENANT_ID) is NOT regressed — calls `computeAndCache(SEED_TENANT_ID, FIXTURES_ROOT)` and returns 200 with a non-empty `topVendors` (the fixture has invoices).

      Mocking strategy:
        - Use `vi.mock("@/lib/auth/resolve-tenant", () => ({ resolveTenant: vi.fn() }))` and configure per-test return values: authenticated `{ authenticated: true, brainSlug: "u-test-fresh", sourceId: "..." }` for A/B; bypass test sets `process.env.AUTH_ENABLED = "0"` and passes `id = "seed"`.
        - For Test A: use a non-existent slug under `os.tmpdir()` so `brainHome(slug)/brain-repo/originals` is guaranteed missing. OR mock `node:fs/promises` `stat` to throw ENOENT.
        - For Test C: clear the in-process insights cache before the call (call `invalidate(SEED_TENANT_ID)` from `lib/insights/cache.ts`) so the test exercises the compute path, not a stale prewarm cache.
        - Import the route under test as `import { GET } from "@/app/api/tenants/[id]/insights/route"` and invoke it as `await GET(new Request("http://localhost/api/tenants/u-test-fresh/insights"), { params: Promise.resolve({ id: "u-test-fresh" }) })`. Assert `response.status` and `await response.json()`.

    `tests/unit/insights/pnl-card-null.test.tsx` — tests the React component renders without throwing when snapshot is null:
      - Test D: `render(<PnlCard state={{ kind: "data", snapshot: null }} />)` does NOT throw. The rendered output includes copy indicating no data (e.g. "No P&L data yet" — pick a literal string that Task 2 will also use). Use `@testing-library/react`.
      - Test E (regression guard): `render(<PnlCard state={{ kind: "data", snapshot: <full PnlSnapshot> }} />)` still shows the formatted dollar amounts (e.g. screen.getByText with a currency-formatted string). This proves the non-null path is untouched.

      The test file uses `.tsx` extension. Ensure the `unit` vitest project's `include` glob captures it — current config (`tests/**/*.test.ts`) matches `.test.ts` only. If `.tsx` is not picked up, either:
        (a) widen the include glob in `vitest.config.ts` to `["tests/**/*.test.ts", "tests/**/*.test.tsx"]` AND add `environment: "jsdom"` for the React test, OR
        (b) name the test file `pnl-card-null.test.ts` and use `React.createElement` directly (no JSX) with a manual jsdom environment.

      Recommended: option (a) with a per-test `// @vitest-environment jsdom` directive at the top of `pnl-card-null.test.tsx`. This keeps the rest of the suite on `node` and only this file on `jsdom`. Verify `jsdom` is available — if `@testing-library/react` and `jsdom` are NOT installed, fall back to option (b) and assert via `React.createElement(PnlCard, { state: { kind: "data", snapshot: null } })` followed by `renderToStaticMarkup` from `react-dom/server` (no jsdom dependency, still proves render doesn't throw and string contains "No P&L data yet").

    Run all four tests and confirm Tests A, C, D, E all FAIL with the current code (no-data guard doesn't exist; type rejects `snapshot: null`). Test B may pass or fail depending on how `pnl.ts` reacts to an empty `originals/` dir — that's acceptable; document the actual result in the commit message.
  </behavior>
  <action>
    Create the two test files exactly as described in `<behavior>`. Match the existing test style in `tests/unit/insights/top-vendors.test.ts` (vitest globals=false, explicit imports, `afterEach` cleanup).

    Before writing, run a quick check: does the repo have `@testing-library/react` and `jsdom`?
    ```bash
    grep -E '"@testing-library/react"|"jsdom"' package.json
    ```
    If both present → use option (a) (`.tsx` + jsdom). If absent → use option (b) (`renderToStaticMarkup`, no new deps). Do NOT add new dependencies for this task — the gap closure must stay tight.

    For the route test, follow the AUTH_ENABLED-aware test pattern from existing tests in `tests/auth/cross-tenant-isolation.test.ts` for inspiration on mocking `resolveTenant`. Note: use `vi.mock` at the top of the file, and use `vi.mocked(resolveTenant).mockResolvedValueOnce(...)` per test.

    Run the failing tests:
    ```bash
    bun run test tests/unit/insights/route-empty-bundle.test.ts tests/unit/insights/pnl-card-null.test.tsx
    ```
    Expect Tests A, C, D, E to FAIL. Commit as:
    ```
    test(06-06): add failing tests for fresh-tenant empty insights + null PnlCard

    Tests cover the D-02 invariant gap surfaced in UAT test 11:
    - Route returns 200 with empty bundle when originals dir is missing.
    - Seed bypass branch still computes from FIXTURES_ROOT.
    - PnlCard renders 'No P&L data yet' empty state when snapshot is null.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>bun run test tests/unit/insights/route-empty-bundle.test.ts tests/unit/insights/pnl-card-null.test.tsx 2>&1 | grep -E "(FAIL|✗|failed)" | head -10</automated>
  </verify>
  <done>
    Two test files exist on disk at the paths above. Running them with the current (pre-Task-2) code produces at least 3 failing assertions (A, C or D, E — D-only might pass on option-b string match). Test file content matches the existing `tests/unit/insights/*.test.ts` style (explicit imports, no globals, `afterEach` cleanup if a tempdir is created). RED commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — add no-data guard + widen pnl type + PnlCard null state</name>
  <files>app/api/tenants/[id]/insights/route.ts, lib/insights/types.ts, components/insights/pnl-card.tsx</files>
  <behavior>
    After this task, all four tests from Task 1 pass. Specifically:

    1. `lib/insights/types.ts`: change `pnl: PnlSnapshot;` to `pnl: PnlSnapshot | null;` in the `InsightBundle` type. No other type changes. This is the contract change that unblocks the empty bundle.

    2. `app/api/tenants/[id]/insights/route.ts`:
       - Add `import { stat } from "node:fs/promises";` to the imports.
       - In the authenticated branch (currently lines 86-108), wrap the `computeAndCache` call with a pre-check:
         - Compute `sourceDir = join(brainHome(brainSlug), "brain-repo");` as today.
         - If cache miss, check `await stat(join(sourceDir, "originals")).then((s) => s.isDirectory()).catch(() => false)`.
         - If `false` (missing or not-a-directory): construct an empty bundle `{ topVendors: [], pnl: null, anomalies: [], computedAt: Date.now() }`, return it as `Response.json(bundle, { status: 200 })`. Do NOT call `cache.set` — leaves the cache empty so a refresh after Phase 7 ingest does a fresh compute (per debugger's Open Question 2).
         - If `true`: call `computeAndCache(brainSlug, sourceDir)` inside the existing try/catch. Other errors (corrupt fixtures, parse errors) still surface as 500 `compute_failed` — that's intentional.
       - Add a top-of-block code comment referencing D-02 + the debug session: `// D-02 (gap closed 06-06): fresh tenants with no originals/ dir return empty bundle (not 500). See .planning/debug/06-insights-enoent-fresh-tenant.md.`
       - The AUTH_ENABLED=0 seed branch (current lines 53-72) is UNTOUCHED.

    3. `components/insights/pnl-card.tsx`:
       - Widen `PnlCardProps.state` `data` variant to `{ kind: "data"; snapshot: PnlSnapshot | null }`.
       - In the data branch, if `snapshot === null`, set `cardState = { kind: "data", node: <div className="text-sm text-muted-foreground">No P&L data yet</div> }` (use the exact string Task 1's test asserts on — keep them in sync). Otherwise the existing dollar-formatting JSX is used unchanged.
       - Do NOT change the loading or error branches.
       - Do NOT change the `InsightCardsRow` consumer — it already passes `snapshot: rowState.data.pnl` which is now nullable through the widened type.

    Run the tests:
    ```bash
    bun run test tests/unit/insights/route-empty-bundle.test.ts tests/unit/insights/pnl-card-null.test.tsx
    ```
    Expect all four tests to PASS.

    Also run the existing insights suite to confirm no regression:
    ```bash
    bun run test tests/unit/insights/
    ```
    All pre-existing tests still pass (top-vendors, pnl, anomalies, frontmatter).

    Run typecheck:
    ```bash
    bun x tsc --noEmit
    ```
    Exits 0. The `pnl: PnlSnapshot | null` widening MUST NOT introduce new errors elsewhere — if `lib/insights/pnl.ts::computePnl` or `lib/insights/prewarm.ts` returns a non-null `PnlSnapshot`, the widening is a strict supertype change and is type-safe. The InsightCardsRow consumer passes `rowState.data.pnl` through to PnlCard which now accepts null — type-safe. No other consumers of `InsightBundle.pnl` exist outside these three files (verify with `grep -rn "\\.pnl" lib/ components/ app/` and confirm only `insight-cards-row.tsx` and `pnl-card.tsx` access the field).
  </behavior>
  <action>
    Edit the three files per `<behavior>`. Order:
      1. `lib/insights/types.ts` — one-character change: add `| null` to the `pnl` field.
      2. `app/api/tenants/[id]/insights/route.ts` — add `stat` import + replace the authenticated cache-fill block (lines ~86-108). Preserve the AUTH_ENABLED=0 seed branch verbatim. Preserve the slug-mismatch 403 guard verbatim. Preserve the `runtime = "nodejs"` and `export const dynamic = "force-dynamic"` directives.
      3. `components/insights/pnl-card.tsx` — widen the props type; add the null branch rendering "No P&L data yet" (must match the literal Task 1 asserts on).

    After edits, run:
    ```bash
    bun run test tests/unit/insights/
    bun x tsc --noEmit
    ```
    Both should pass cleanly.

    Commit as:
    ```
    fix(06-06): return empty bundle for fresh tenants without originals dir

    Closes the UAT test 11 gap. Adds a 10-line fs.stat guard in the
    authenticated branch of the insights route so a missing
    <brainHome(slug)>/brain-repo/originals returns 200 with
    { topVendors: [], pnl: null, anomalies: [] } instead of 500
    compute_failed. Honors D-02 (real tenants get empty insights until
    Phase 7 QBO ingest).

    Type change: InsightBundle.pnl widened to PnlSnapshot | null.
    UI change: PnlCard renders 'No P&L data yet' when snapshot is null.
    Seed/AUTH_ENABLED=0 path unchanged.

    Refs: .planning/debug/06-insights-enoent-fresh-tenant.md (Fix 1a)

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>bun run test tests/unit/insights/ 2>&1 | tail -20 && bun x tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    All four Task-1 tests pass. All pre-existing tests in `tests/unit/insights/` still pass (zero regression). `bun x tsc --noEmit` exits 0. The `app/api/tenants/[id]/insights/route.ts` file contains `stat(join(sourceDir, "originals"))` (or equivalent — the literal `stat` call) and the AUTH_ENABLED=0 seed branch is byte-identical except for any minor surrounding-context edits. `lib/insights/types.ts` line containing `pnl:` reads `pnl: PnlSnapshot | null;`. `components/insights/pnl-card.tsx` contains the literal string `No P&L data yet`. GREEN commit landed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify the live route end-to-end (local dev server smoke check)</name>
  <files>(no file changes — verification only)</files>
  <action>
    Start the dev server and prove the fix end-to-end against an authenticated request path. This is the AUTH-05 / D-02 acceptance check that UAT test 11 was looking for.

    Step 1: Start the dev server:
    ```bash
    bun run dev
    ```
    (Background; let it boot for ~5s.)

    Step 2: Create a synthetic authenticated session for a fake fresh tenant. The cleanest way is to reuse the existing session + tenant created during the user's UAT session — `u-748ccc135b0073` from the UAT report. Confirm it does NOT have a `brains/u-748ccc135b0073/brain-repo/originals` directory:
    ```bash
    ls -la "brains/u-748ccc135b0073/brain-repo/originals" 2>&1 | head -3
    ```
    Expected: `No such file or directory`. (If the directory now exists because of unrelated work, pick another `u-*` slug from `brains/` that has no `brain-repo/originals`, or remove it: `rm -rf brains/u-748ccc135b0073/brain-repo/originals` only if the operator confirms it's safe.)

    Step 3: Reproduce the original 500 (sanity check the fix is being exercised) by sending an UNAUTHENTICATED request first:
    ```bash
    curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:3000/api/tenants/u-748ccc135b0073/insights"
    ```
    Expected: `401` (resolveTenant gate fires before the new no-data guard). Good — this proves auth is still in front.

    Step 4: Hit the route with the live user's `qb_session` cookie. The operator-friendly way: sign in via the dev server (visit `http://localhost:3000/sign-in`, send magic link, click) and then in the browser DevTools Network tab inspect the dashboard's `/api/tenants/<slug>/insights` request. The expected outcomes after this plan ships:
      - HTTP status: `200`
      - Response body: `{"topVendors":[],"pnl":null,"anomalies":[],"computedAt":<number>}`
      - No `compute_failed` error
      - The dashboard's three insight cards render: top-vendors empty list, P&L card shows "No P&L data yet", anomalies empty list, no "Could not load insights" error banner

    If the operator is not available to perform the browser-side step, an alternative is a one-shot curl with a known session cookie from `.env.local` or a dev test cookie — if no such cookie is documented, fall back to the structural verification: confirm the test from Task 2 passes (`bun run test tests/unit/insights/route-empty-bundle.test.ts`) AND grep the route file for the `stat` guard:
    ```bash
    grep -n "stat(join(sourceDir" app/api/tenants/[id]/insights/route.ts
    ```
    Expect 1 match. This counts as "guard wired into the authenticated branch."

    Step 5: Regression-check the seed path:
    ```bash
    AUTH_ENABLED=0 curl -s "http://localhost:3000/api/tenants/seed/insights" | head -c 200
    ```
    Expected: a JSON response with a non-empty `topVendors` array (Mara's coffee invoices). If the dev server isn't started with `AUTH_ENABLED=0`, restart it: `AUTH_ENABLED=0 bun run dev`. This proves the seed branch still computes from FIXTURES_ROOT and serves real data.

    Step 6: Kill the dev server.

    Record actual observed status codes and bundle shapes in the Summary file written at the end of execution.
  </action>
  <verify>
    <automated>grep -c "stat(join(sourceDir" "/Users/abdulhannankanji/Git repos/quick-brain/app/api/tenants/[id]/insights/route.ts" | grep -v '^0$' && bun run test tests/unit/insights/route-empty-bundle.test.ts 2>&1 | grep -E "(passed|✓)" | head -5</automated>
  </verify>
  <done>
    Either: (a) operator-confirmed live request returns 200 with `{topVendors:[],pnl:null,anomalies:[],computedAt:<n>}` for a fresh authenticated tenant; OR (b) structural verification — `stat(join(sourceDir, "originals"))` appears exactly once in `app/api/tenants/[id]/insights/route.ts`, all Task 2 tests pass, and the seed-path regression smoke (Step 5) returns a non-empty bundle. Either path is sufficient — the UAT gap is resolved when D-02's "empty insights for fresh tenants" invariant is actually enforced by code (verified by Task 2 tests) and the seed path is provably untouched (verified by Step 5 or by the pre-existing seed tests still passing).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/tenants/[id]/insights | URL slug is untrusted; identity comes from session cookie via `resolveTenant()` (unchanged from 06-05) |
| route handler → filesystem (`stat` + `readdir`) | `stat` on `<brainHome(brainSlug)>/brain-repo/originals` — path is composed from session-derived brainSlug (trusted) joined with hard-coded literal `"originals"` (trusted). No user-provided path component. No traversal risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-27 | Information disclosure | Empty bundle returned to wrong tenant | accept | The empty bundle is constant `{topVendors:[],pnl:null,anomalies:[],computedAt:Date.now()}`. It contains no per-tenant data. The brainSlug used for the `stat` path is session-derived (06-05 D-11 chokepoint), unchanged. |
| T-06-28 | Denial of service | Per-request `stat` call on every miss | accept | One `stat` syscall per cache miss; negligible. Once cache warms (post-Phase-7 ingest), the path falls through `getCachedInsights` and never re-stats. Documented in route comment. |
| T-06-29 | Tampering | URL-supplied id could try to trigger a stat on a foreign path | mitigate | The slug-mismatch 403 guard (06-05) fires BEFORE the new `stat` call. The new `stat` uses `brainSlug` (session-derived), never `id` (URL). Verified by reading `app/api/tenants/[id]/insights/route.ts:82` (the `if (id !== brainSlug) return 403` guard sits between resolveTenant and computeAndCache). |
| T-06-30 | Elevation of privilege | Empty bundle bypasses auth | accept | The `resolveTenant()` 401 gate is unchanged and still fires before the empty-bundle branch is reachable. UAT test 7 ("Unauthenticated /dash Redirect") proves this — `/api/tenants/anything/insights` returns 401 unauthenticated. Verified by Task 3 Step 3 sanity check. |
</threat_model>

<verification>
Phase-level checks after the plan executes:

1. The UAT-test-11 specific failure no longer reproduces:
   ```bash
   # With a fresh authenticated tenant, the insights route returns 200 with an empty bundle.
   # (See Task 3 for the live-verification commands.)
   ```

2. The full insights unit suite stays green (zero regression):
   ```bash
   bun run test tests/unit/insights/
   ```

3. Cross-tenant isolation test (06-05 invariant) stays green:
   ```bash
   bun run test tests/auth/cross-tenant-isolation.test.ts
   ```

4. Typecheck clean:
   ```bash
   bun x tsc --noEmit
   ```

5. Seed path regression-free: `AUTH_ENABLED=0 curl http://localhost:3000/api/tenants/seed/insights` returns a non-empty `topVendors` array.
</verification>

<success_criteria>
Plan is complete when:
- [ ] `app/api/tenants/[id]/insights/route.ts` contains a single `stat(join(sourceDir, "originals"))` call wired into the authenticated branch
- [ ] When that stat throws/returns false, the route returns `200 {topVendors:[], pnl:null, anomalies:[], computedAt:<n>}` without calling `computeAndCache`
- [ ] `lib/insights/types.ts` declares `pnl: PnlSnapshot | null` in `InsightBundle`
- [ ] `components/insights/pnl-card.tsx` renders "No P&L data yet" when `snapshot` is null and renders dollar amounts when `snapshot` is non-null (existing path)
- [ ] Two new test files (`tests/unit/insights/route-empty-bundle.test.ts`, `tests/unit/insights/pnl-card-null.test.tsx`) pass
- [ ] `bun run test tests/unit/insights/` shows zero failures (existing tests + new tests all green)
- [ ] `bun x tsc --noEmit` exits 0
- [ ] AUTH_ENABLED=0 seed branch is byte-identical (verified by diff: the only changes in the route file are the new `stat` import and the authenticated branch)
- [ ] Two clean commits: one `test(06-06)` (RED) and one `fix(06-06)` (GREEN) — debugger's "Ship Fix 1a only" recommendation honored, no Fix 1b/2/3 creep
- [ ] The UAT-test-11 truth holds: a fresh authenticated tenant's dashboard renders three insight cards in their empty/no-data state, no 500
</success_criteria>

<output>
Create `.planning/phases/06-auth-multi-tenant-isolation/06-06-SUMMARY.md` when done. Include:
- which 2 commit SHAs were produced (RED test commit + GREEN fix commit)
- the exact response shape returned by the new no-data branch (paste the literal JSON)
- confirmation that the seed branch was not modified (diff line count for the AUTH_ENABLED=0 block)
- any deviation noted under Auto-fixed Issues
- a note that the UAT test 11 gap is closed and the operator can re-run `/dash/<slug>` on a fresh tenant to confirm
</output>
