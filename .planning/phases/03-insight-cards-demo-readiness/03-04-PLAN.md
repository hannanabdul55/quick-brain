---
phase: 03-insight-cards-demo-readiness
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - lib/gbrain/abort-tracker.ts
  - app/api/tenants/[id]/reset/route.ts
  - components/insights/reset-button.tsx
  - components/insights/insight-cards-row.tsx
  - scripts/panic-reset.sh
requirements:
  - DEMO-01
  - DEMO-02
autonomous: true

must_haves:
  truths:
    - "POST /api/tenants/<slug>/reset → 200 in <10s on the seed-cloned tenant; tenant brain dir is re-copied from seed; cache entry is invalidated"
    - "Concurrent spawns for a tenant can be tracked and aborted by the reset endpoint"
    - "scripts/panic-reset.sh kills next dev + gbrain processes + removes all non-seed brains/ entries in <15s without rebuilding seed"
    - "Reset button requires a 2-second press-and-hold before firing; releasing early aborts; visual progress fill during the hold"
  artifacts:
    - path: lib/gbrain/abort-tracker.ts
      provides: "Map<tenantId, AbortController> registry so reset can SIGKILL in-flight spawns"
      exports: ["registerAbortable", "unregisterAbortable", "abortTenant"]
    - path: app/api/tenants/[id]/reset/route.ts
      provides: "POST handler — kill spawns, rm -rf brains/<id>/, cp -r brains/seed/, invalidate cache"
      exports: ["POST", "dynamic"]
    - path: components/insights/reset-button.tsx
      provides: "Press-and-hold (2s) button with progress fill, calls reset endpoint, triggers router.refresh()"
      exports: ["ResetButton"]
    - path: scripts/panic-reset.sh
      provides: "Bash terminal panic-reset — kills processes + wipes all tenants except seed"
  key_links:
    - from: app/api/tenants/[id]/reset/route.ts
      to: lib/insights/cache.ts (invalidate)
      via: "import + invalidate(tenantId) after re-copy"
      pattern: "invalidate"
    - from: components/insights/reset-button.tsx
      to: app/api/tenants/[id]/reset/route.ts
      via: "POST fetch + router.refresh()"
      pattern: "fetch.*tenants.*reset"
    - from: scripts/panic-reset.sh
      to: "filesystem brains/ + processes"
      via: "pkill -f patterns + find … -exec rm -rf"
      pattern: "pkill|rm -rf"
---

<objective>
Reset path end-to-end: backend endpoint + abort tracker + press-and-hold UI + panic-reset bash script. Per D-Reset-Flow + D-Panic-Reset-Script.

Purpose: DEMO-01 (reset button under 10s) + DEMO-02 (panic-reset.sh under 15s). Together they form the safety net for the 3 back-to-back rehearsals (DEMO-04 — operator-verified).

Output: 4 new files + 1 modified (the cards-row gets a `<ResetButton>` mounted in its top-right corner so the dashboard owns the reset trigger).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-insight-cards-demo-readiness/03-CONTEXT.md
@.planning/phases/03-insight-cards-demo-readiness/03-01-PLAN.md
@.planning/phases/03-insight-cards-demo-readiness/03-03-PLAN.md

<interfaces>
<!-- From lib/insights/cache.ts (Plan 03-01) -->

```typescript
export function invalidate(tenantId: string): void;
```

<!-- From lib/gbrain/* (Phase 1, do not modify) -->

```typescript
// lib/gbrain/paths.ts
export const BRAINS_ROOT: string;
export const SEED_TENANT_ID: "seed";
export function brainHome(tenantId: string): string;
export function seedBrainHome(): string;

// lib/gbrain/slug.ts
export const tenantSlugSchema: ZodString;

// lib/gbrain/tenants.ts
export function get(tenantId: string): TenantRecord | undefined;
export async function init(): Promise<void>;
export function isSeed(tenantId: string): boolean;
```

The reset endpoint must reject `tenantId === SEED_TENANT_ID` (do not wipe the seed brain).
</interfaces>

<existing_patterns>
- File-copy pattern: `cp` from `node:fs/promises` with `{ recursive: true }` — see `lib/onboarding/create-tenant.ts` step 3 ("Copy seed → new brain dir").
- Removal pattern: `rm` from `node:fs/promises` with `{ recursive: true, force: true }`.
- Spawn lifecycle: every `spawnGBrain` call goes through `withTenantLock` (Phase 1). To support abort, the spawn helper does NOT currently expose its child process — but the abort-tracker tracks AbortControllers that route handlers register manually for their own in-flight spawns. For Phase 3, we register/unregister around `spawnGBrain` calls so the chat handler's spawn can be aborted; if no chat is in flight when reset is pressed, the tracker is empty and reset just nukes the dir.
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abort tracker module + reset Route Handler</name>
  <files>lib/gbrain/abort-tracker.ts, app/api/tenants/[id]/reset/route.ts</files>
  <read_first>
    - lib/gbrain/mutex.ts (Map<tenantId, ...> pattern reference; same module style)
    - lib/onboarding/create-tenant.ts (cp pattern step 3, error class style)
    - app/api/tenants/route.ts (Route Handler pattern: dynamic = "force-dynamic", zod validation, Response.json)
    - app/api/tenants/[id]/insights/route.ts (slug + tenant.get + 400/404 pattern from plan 03-02)
    - lib/gbrain/paths.ts (BRAINS_ROOT, brainHome, seedBrainHome, SEED_TENANT_ID, isSeed)
    - lib/insights/cache.ts (invalidate)
  </read_first>
  <action>
    Create `lib/gbrain/abort-tracker.ts`:

    Module-scoped `const trackers = new Map<string, Set<AbortController>>();`

    Exports:
    - `function registerAbortable(tenantId: string): AbortController` — create new AbortController, push into `trackers.get(tenantId)` (lazy init the Set), return it. Callers MUST also call `unregisterAbortable` in a finally.
    - `function unregisterAbortable(tenantId: string, ctrl: AbortController): void` — remove from the Set; delete the Set if empty.
    - `function abortTenant(tenantId: string): number` — for each AbortController in the tenant's Set, call `.abort()`. Return the count aborted. Then clear the Set.

    NO direct child_process.kill here — the spawn helper does that on `SIGKILL` from `withTenantLock`'s timeout. The reset endpoint calls `abortTenant` AND deletes the directory; spawns that were in flight have their `await spawnGBrain(...)` promise reject when the timeoutMs fires OR they finish naturally before reset proceeds. Since reset is bounded by the mutex (HARN-04), the reset will WAIT for in-flight spawns to finish or be killed. To shortcut this: reset proceeds OUTSIDE the mutex; the directory rm is independent of the mutex. (Trade-off accepted: a chat spawn in flight when reset fires may write to a dir that's about to be rm'd; that's fine — the spawn will exit non-zero and the chat surface shows an error, which is acceptable demo behavior.)

    Create `app/api/tenants/[id]/reset/route.ts`:

    Top of file:
    ```typescript
    export const dynamic = "force-dynamic";
    ```

    Imports: `tenantSlugSchema`, `tenants` (namespace), `SEED_TENANT_ID`, `brainHome`, `seedBrainHome`, `invalidate` from `@/lib/insights/cache`, `abortTenant` from `@/lib/gbrain/abort-tracker`, `cp` + `rm` from `node:fs/promises`.

    Export `async function POST(_req: Request, ctx: { params: Promise<{ id: string }> }): Promise<Response>`:
    1. `const { id } = await ctx.params;`
    2. Slug validate → 400 on failure.
    3. **If `tenants.isSeed(id)` (or `id === SEED_TENANT_ID`) → 403 `{ error: "cannot_reset_seed" }`**. The seed brain is a build artifact; resetting it would destroy the source-of-truth for every other tenant.
    4. `await tenants.init();` → if `tenants.get(id) === undefined`, return 404.
    5. Abort in-flight spawns: `const killed = abortTenant(id);` → log `[reset] aborted=${killed} for ${id}`.
    6. `try { await rm(brainHome(id), { recursive: true, force: true }); } catch (err) { /* log; do not fail — empty dir is fine */ }`.
    7. `try { await cp(seedBrainHome(), brainHome(id), { recursive: true }); } catch (err) { return Response.json({ error: "copy_failed", message: err.message }, { status: 500 }); }`.
    8. `invalidate(id);` (insight cache).
    9. Re-register tenant: `tenants.upsert({ id, brainHome: brainHome(id), status: "ready", createdAt: Date.now() });` (preserves the in-memory Map even if `remove()` had been called).
    10. Log `[reset] complete tenant=${id} duration_ms=${...}` (track `t0 = Date.now()` at start).
    11. Return `Response.json({ ok: true, durationMs: Date.now() - t0, abortedSpawns: killed }, { status: 200 })`.

    Per D-Reset-Flow: "Total budget: <10s, typically ~2s." The `cp` of ~50MB takes well under a second locally.
  </action>
  <verify>
    <automated>cd /Users/abdulhannankanji/Git\ repos/quick-brain && bunx tsc --noEmit 2>&1 | grep -E 'abort-tracker|/reset/' | grep error && exit 1; bun -e 'import { registerAbortable, unregisterAbortable, abortTenant } from "./lib/gbrain/abort-tracker.ts"; const c1 = registerAbortable("a"); const c2 = registerAbortable("a"); const c3 = registerAbortable("b"); const k = abortTenant("a"); if (k !== 2) throw new Error("expected 2, got " + k); if (!c1.signal.aborted || !c2.signal.aborted) throw new Error("not aborted"); if (c3.signal.aborted) throw new Error("wrong tenant aborted"); unregisterAbortable("b", c3); console.log("ABORT_TRACKER_OK");' && (bun run dev > /tmp/qb-dev.log 2>&1 &); DEV_PID=$!; sleep 6; curl -sS -X POST http://localhost:3000/api/tenants/seed/reset -w 'HTTP=%{http_code}\n' -o /tmp/r1.json; STATUS=$(cat /tmp/r1.json | jq -r '.error // "ok"'); if [ "$STATUS" != "cannot_reset_seed" ]; then echo "Seed should be rejected with cannot_reset_seed; got: $(cat /tmp/r1.json)"; kill $DEV_PID; exit 1; fi; curl -sS -X POST http://localhost:3000/api/tenants/test-cafe/reset -w 'HTTP=%{http_code}\n' -o /tmp/r2.json; cat /tmp/r2.json; if curl -sS -X POST -H "Content-Type: application/json" -d '{"businessName":"Reset Test", "businessType":"cafe", "ownerName":"Tester"}' http://localhost:3000/api/tenants > /tmp/created.json; then SLUG=$(jq -r '.slug' /tmp/created.json); T0=$(date +%s%3N); curl -sS -X POST http://localhost:3000/api/tenants/$SLUG/reset -o /tmp/r3.json; T1=$(date +%s%3N); DUR=$((T1 - T0)); echo "reset_duration_ms=$DUR"; jq -e '.ok == true' /tmp/r3.json > /dev/null || (echo "reset failed: $(cat /tmp/r3.json)" && kill $DEV_PID && exit 1); if [ $DUR -gt 10000 ]; then echo "reset >10s: $DUR"; kill $DEV_PID; exit 1; fi; ls brains/$SLUG/companies/ > /dev/null || (echo "brain dir not rebuilt"; kill $DEV_PID; exit 1); rm -rf brains/$SLUG; fi; kill $DEV_PID 2>/dev/null; echo RESET_OK</automated>
  </verify>
  <done>
    - `abort-tracker.ts` exports the 3 functions; bun smoke test passes (`abortTenant("a")` returns 2, both controllers `.aborted === true`).
    - `POST /api/tenants/seed/reset` → 403 `{ error: "cannot_reset_seed" }`.
    - `POST /api/tenants/<real-tenant>/reset` → 200 `{ ok: true, durationMs: <10000 }` in <10s.
    - After reset, the tenant brain dir exists with `companies/` subdir present (proves `cp -r` from seed worked).
  </done>
</task>

<task type="auto">
  <name>Task 2: Press-and-hold ResetButton + mount in InsightCardsRow</name>
  <files>components/insights/reset-button.tsx, components/insights/insight-cards-row.tsx</files>
  <read_first>
    - components/ui/button.tsx (Button primitive)
    - components/insights/insight-cards-row.tsx (existing — extend with reset button in top-right; from plan 03-03)
    - components/chat/chat-surface.tsx (Phase 2 useState/useRef hook style — match)
    - app/api/tenants/[id]/reset/route.ts (POST contract: 200 { ok, durationMs, abortedSpawns })
  </read_first>
  <action>
    Create `components/insights/reset-button.tsx` — client component (`"use client"`):

    Imports: `import * as React from "react"; import { Button } from "@/components/ui/button"; import { useRouter } from "next/navigation";`.

    Props: `{ tenantId: string; onResetComplete?: () => void }`.

    State:
    - `holding: boolean` — true while pointer is down.
    - `progress: number` (0..1) — fill fraction during hold.
    - `resetting: boolean` — true after fire, false on response.
    - `error: string | null` — last-error display.

    Refs:
    - `pointerDownAt: number | null` — timestamp when pointerdown fired.
    - `rafRef: number | null` — requestAnimationFrame handle for the progress tick.

    Pointer handlers (`onPointerDown`, `onPointerUp`, `onPointerLeave`, `onPointerCancel`):
    - On `pointerdown`: capture pointer (`e.currentTarget.setPointerCapture(e.pointerId)`), set `pointerDownAt = Date.now()`, set `holding = true`, kick a `requestAnimationFrame` loop that on each frame computes `progress = Math.min(1, (Date.now() - pointerDownAt) / 2000)` and setState; when `progress >= 1`, cancel RAF and call `fireReset()`.
    - On `pointerup` / `pointerleave` / `pointercancel`: if `holding && progress < 1` → ABORT (reset state, no firing). If `progress === 1` already fired → no-op.

    `fireReset()`:
    - Set `resetting = true`, `error = null`.
    - `const res = await fetch(`/api/tenants/${tenantId}/reset`, { method: "POST" });`
    - On non-ok: parse JSON best-effort, set `error = "Reset failed"`, log full error, set `resetting = false`, reset `holding/progress`.
    - On ok: call `onResetComplete?.()`, then `router.refresh()` to reload the dashboard server component (which re-fetches tenant + cards row remounts → fresh fetch from `/api/.../insights`). Set `resetting = false`.

    Render: A `<Button variant="destructive" size="sm">` with inline styles for the progress fill — use a CSS gradient or a positioned overlay:
    ```
    <Button
      onPointerDown={...} onPointerUp={...} onPointerLeave={...} onPointerCancel={...}
      style={{ position: "relative", overflow: "hidden" }}
      disabled={resetting}
    >
      <span style={{ position: "absolute", inset: 0, width: `${progress * 100}%`, background: "rgba(255,255,255,0.25)", transition: "width 16ms linear" }} />
      <span style={{ position: "relative" }}>{resetting ? "Resetting…" : holding ? "Hold to reset…" : "Reset"}</span>
    </Button>
    ```
    If `error`: render a small `<div className="text-xs text-destructive mt-1">{error}</div>` next to the button.

    Use `useRouter()` from `next/navigation`. Pointer capture is critical for press-and-hold reliability on touch + mouse alike.

    Modify `components/insights/insight-cards-row.tsx`:
    - Add `<ResetButton tenantId={tenantId} onResetComplete={() => setReloadKey(k => k + 1)} />` mounted in a top-right header row above the cards grid. Wrap: `<div className="max-w-3xl mx-auto py-4 px-4"><div className="flex justify-end mb-2"><ResetButton .../></div><div className="grid grid-cols-1 md:grid-cols-3 gap-3">{...3 cards...}</div></div>`.
    - Keep the existing fetch/state logic from plan 03-03 untouched.
  </action>
  <verify>
    <automated>cd /Users/abdulhannankanji/Git\ repos/quick-brain && bunx tsc --noEmit 2>&1 | grep -E 'reset-button|insight-cards-row' | grep error && exit 1; grep -q 'setPointerCapture' components/insights/reset-button.tsx || (echo "missing pointer capture" && exit 1); grep -qE 'onPointerDown|onPointerUp' components/insights/reset-button.tsx || (echo "missing pointer handlers" && exit 1); grep -q 'requestAnimationFrame' components/insights/reset-button.tsx || (echo "missing RAF progress" && exit 1); grep -q '2000' components/insights/reset-button.tsx || (echo "missing 2-second threshold (literal 2000)" && exit 1); grep -q 'ResetButton' components/insights/insight-cards-row.tsx || (echo "ResetButton not mounted in cards row" && exit 1); (bun run dev > /tmp/qb-dev.log 2>&1 &); DEV_PID=$!; sleep 6; curl -sS http://localhost:3000/dash/seed > /tmp/dash.html; grep -q 'Reset' /tmp/dash.html || (echo 'Reset button text missing'; kill $DEV_PID; exit 1); kill $DEV_PID 2>/dev/null; echo RESET_BUTTON_OK</automated>
  </verify>
  <done>
    - `components/insights/reset-button.tsx` uses pointerdown/up + setPointerCapture + RAF + 2000ms threshold.
    - The cards row mounts the button in a top-right header.
    - "Reset" appears in `/dash/seed` server-rendered HTML.
    - `bunx tsc --noEmit` clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: scripts/panic-reset.sh (terminal nuke)</name>
  <files>scripts/panic-reset.sh</files>
  <read_first>
    - scripts/seed.sh (existing bash script style + shebang convention)
    - scripts/demo-check.sh (existing — exit codes + echo conventions)
  </read_first>
  <action>
    Create `scripts/panic-reset.sh`. Steps verbatim per D-Panic-Reset-Script:

    Shebang `#!/usr/bin/env bash` and `set -uo pipefail` (NOT `set -e` — pkill returning 1 when there's nothing to kill is expected and must not abort the script).

    Body:
    ```
    #!/usr/bin/env bash
    set -uo pipefail

    SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
    REPO_ROOT="$( cd "$SCRIPT_DIR/.." >/dev/null 2>&1 && pwd )"

    echo "[panic-reset] starting…"
    T0=$(date +%s)

    # 1. Kill the Next.js dev server.
    pkill -9 -f "next dev" 2>/dev/null
    pkill -9 -f "next-server" 2>/dev/null
    echo "[panic-reset] killed next dev/next-server (if running)"

    # 2. Kill orphaned gbrain spawns. Two patterns: `gbrain` binary and `bun ... gbrain`.
    pkill -9 -f "gbrain " 2>/dev/null
    pkill -9 -f "bun .*gbrain" 2>/dev/null
    echo "[panic-reset] killed gbrain processes (if running)"

    # 3. Wipe all tenant brain dirs EXCEPT seed. Do NOT rebuild seed.
    if [ -d "$REPO_ROOT/brains" ]; then
      # Iterate immediate children only; skip "seed" and any dotfiles.
      while IFS= read -r -d '' entry; do
        name="$(basename "$entry")"
        if [ "$name" != "seed" ]; then
          rm -rf "$entry"
          echo "[panic-reset] wiped brains/$name"
        fi
      done < <(find "$REPO_ROOT/brains" -mindepth 1 -maxdepth 1 -type d -print0)
    fi

    # 4. Done. NO `bun run seed` — seed is pre-baked.
    T1=$(date +%s)
    echo "[panic-reset] complete in $((T1 - T0))s"
    echo "[panic-reset] NEXT: re-run \`bun run dev\` to restart Next.js"
    exit 0
    ```

    Make executable in the action: `chmod +x scripts/panic-reset.sh`.

    Add to `package.json` scripts: `"panic-reset": "bash scripts/panic-reset.sh"`. (Use the existing pattern: `"seed": "bash scripts/seed.sh"`.) The `bun run panic-reset` shortcut keeps the CLI consistent.

    Per D-Panic-Reset-Script: "DOES NOT re-run `bun run seed`". The script preserves `brains/seed/`. Operator can run `bun run dev` immediately afterwards.
  </action>
  <verify>
    <automated>cd /Users/abdulhannankanji/Git\ repos/quick-brain && test -x scripts/panic-reset.sh || (echo "panic-reset.sh not executable" && exit 1); grep -q 'pkill -9 -f "next dev"' scripts/panic-reset.sh || (echo "missing pkill next dev" && exit 1); grep -q 'gbrain' scripts/panic-reset.sh || (echo "missing gbrain kill" && exit 1); grep -qE 'seed' scripts/panic-reset.sh || (echo "missing seed exclusion" && exit 1); grep -q 'bun run seed' scripts/panic-reset.sh && (echo "panic-reset MUST NOT rebuild seed!" && exit 1) || true; grep -q '"panic-reset"' package.json || (echo "package.json missing panic-reset script" && exit 1); mkdir -p brains/dummy-tenant-for-panic-reset-test/companies && echo test > brains/dummy-tenant-for-panic-reset-test/companies/x.md; T0=$(date +%s); bash scripts/panic-reset.sh > /tmp/panic.log 2>&1; T1=$(date +%s); DUR=$((T1 - T0)); echo "panic_duration_s=$DUR"; cat /tmp/panic.log; if [ -d brains/dummy-tenant-for-panic-reset-test ]; then echo "dummy tenant not wiped"; exit 1; fi; if [ ! -d brains/seed ]; then echo "SEED WIPED! catastrophic"; exit 1; fi; if [ $DUR -gt 15 ]; then echo "panic-reset >15s: $DUR"; exit 1; fi; echo PANIC_RESET_OK</automated>
  </verify>
  <done>
    - `scripts/panic-reset.sh` is executable.
    - `bun run panic-reset` shortcut wired in package.json.
    - A pre-seeded dummy tenant dir is wiped after the script runs.
    - `brains/seed/` is preserved.
    - Script completes in <15s (typically <2s — just process kills + a few rm -rf).
    - Script does NOT contain `bun run seed` or `gbrain init` or `gbrain import` (verified via grep).
  </done>
</task>

</tasks>

<verification>
- `bunx tsc --noEmit` clean.
- Full live E2E for reset (POST endpoint):
  ```
  bun run dev &
  sleep 6
  # create a tenant
  curl -sS -X POST -H "Content-Type: application/json" -d '{"businessName":"E2E Reset", "businessType":"cafe", "ownerName":"E"}' http://localhost:3000/api/tenants > /tmp/c.json
  SLUG=$(jq -r .slug /tmp/c.json)
  # reset it, time it
  time curl -sS -X POST http://localhost:3000/api/tenants/$SLUG/reset
  # verify dir was rebuilt
  ls brains/$SLUG/companies/ | head
  # cleanup
  rm -rf brains/$SLUG
  pkill -f "next dev" || true
  ```
- Live E2E for panic-reset: pre-create `brains/junk-1/`, `brains/junk-2/`, run `bun run panic-reset`, verify `brains/seed/` still exists and `brains/junk-1`, `brains/junk-2` are gone.
- Dashboard renders the Reset button: `curl http://localhost:3000/dash/seed | grep -q Reset`.
- Press-and-hold UX is operator-verified in browser; not automatable in this plan (a Playwright test would be overkill given the 7.5h budget and operator-driven DEMO-04 acceptance).
</verification>

<success_criteria>
- Reset endpoint completes in <10s on the demo laptop (typically ~1-2s for a 50MB brain).
- Panic-reset script completes in <15s and leaves `brains/seed/` intact.
- Press-and-hold UI fires only after a 2s sustained hold; releasing aborts (operator visual verification).
- In-flight spawns can be aborted via `abortTenant(id)` (proof via the abort-tracker smoke test).
- Reset invalidates the insights cache → next dashboard load re-computes (verified by computedAt timestamp changing).
- Cannot reset the seed tenant (403).
</success_criteria>

<output>
Create `.planning/phases/03-insight-cards-demo-readiness/03-04-SUMMARY.md` documenting:
- The reset endpoint contract + measured duration on the demo laptop.
- The abort tracker pattern (and the deliberate trade-off that reset proceeds outside the mutex — in-flight chat may fail mid-reset, which is acceptable for demo).
- panic-reset.sh script duration + behavior (kills + wipes, no rebuild).
- Press-and-hold UI implementation notes (pointer capture, RAF tick, 2000ms threshold).
</output>
